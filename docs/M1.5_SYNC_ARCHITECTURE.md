# M1.5 — Sync Backbone Architecture

Backbone двостороннього обміну з 1С: outbound write-back з програми менеджерів у 1С через HTTP-proxy + persistent queue з retry/backoff.

**Status:**

- **M1.5** shipped: client-side queue + cron + proxy + `ОбновитиКлієнта` (mock).
- **M1.5b** shipped: client-side `СтворитиЗамовлення` + `СтворитиОплату` (mock) + UI form створення замовлення (`/manager/orders/new`) + admin retry UI (`/admin/sync-jobs`).
- **SOAP-канал ще не налаштований** з 1С-сторони. Усе працює у mock-режимі
  (`SYNC_MOCK_MODE=true` дефолт). Real SOAP wiring — після того як 1С-розробник
  реалізує BSL-модулі за `docs/1C_SYNC_MODULES_SPEC.md`.

---

## Goals

1. **Decoupling** — Next.js не блокується на повільних SOAP-сесіях
2. **Reliability** — будь-який збій 1С (downtime / network / DB) не втрачає write — retry з exponential backoff до 5 разів
3. **Idempotency** — повторні спроби з тим самим payload не створюють дублікатів у 1С
4. **Observability** — у будь-який момент видно скільки jobs у черзі, останній sentAt, які failed
5. **Mock-first development** — worker / CI можуть тестувати без живого 1С

---

## High-level дiаграма

```
┌────────────────────────────────┐
│  Next.js Manager App           │
│  (apps/store)                  │
│                                │
│  PATCH /api/v1/manager/        │
│    clients/[id]                │
│    ↓                           │
│  prisma.mgrClient.update()     │
│    ↓                           │
│  enqueueClientUpdate() ────────┐
└────────────────────────────────┘
                                 │
                                 ▼
                  ┌──────────────────────────┐
                  │  MgrSyncJob row created  │
                  │  status="pending"        │
                  │  nextAttemptAt=now       │
                  │  idempotencyKey=UUID     │
                  └────────────┬─────────────┘
                               │
            ┌──────────────────┘
            │  Windows Task Scheduler
            │  triggers each 1 minute
            ▼
┌─────────────────────────────────┐
│ POST /api/cron/                 │
│   process-sync-queue            │
│  (x-cron-secret header)         │
│                                 │
│  processSyncQueue(batchSize=20) │
│   findMany pending/retrying     │
│   for each:                     │
│     sendToProxy()               │
│     mark sent / failed / retry  │
└─────────────────────────────────┘
                │
                ▼ HTTP (X-Sync-Secret)
┌─────────────────────────────────┐
│  services/manager-sync          │
│  (PM2 instance, port 3001)      │
│                                 │
│  POST /sync/clients/:id         │
│  {idempotencyKey, payload}      │
│                                 │
│  In-memory idempotency cache    │
│   ↓ if known → return cached    │
│                                 │
│  if SYNC_MOCK_MODE=true:        │
│    updateClientMock()           │
│    100-500ms delay + ok:true    │
│                                 │
│  else:                          │
│    updateClientViaSoap()        │
│    → 1C MobileExchange.1cws     │
└─────────────────────────────────┘
                │
                ▼ SOAP (XML-over-HTTP)
┌─────────────────────────────────┐
│   1С (треба написати!)          │
│   MobileExchange.1cws           │
│   ОбновитиКлієнта(...)          │
│   See docs/1C_SYNC_MODULES_     │
│       SPEC.md                   │
└─────────────────────────────────┘
```

---

## Запитання якi ми вирiшили

### Why HTTP-proxy (services/manager-sync), not direct SOAP from Next.js?

- Next.js серверні route-handlers — short-running, не призначенi для довгих SOAP-sessions з connection pooling
- Окремий PM2-процес можна restart-нути без впливу на основний site (`pm2 restart ltex-manager-sync`)
- Proxy maintainit-ить in-memory state (idempotency cache, потенційно SOAP client connection)
- M1.6 inbound polling з 1С теж буде у тому ж сервісі — common SOAP layer

### Why in-memory idempotency cache?

- Single-instance proxy → Map TTL 5min достатньо для retry-dedup
- Multi-instance буде потребувати redis/DB — але це для M1.5b+ якщо горизонтально масштабуємо
- 1С зберігає власний idempotency registry (`СинкЛог`) для дублів що дiйшли — це primary guard

### Why exponential backoff 1m/5m/30m/2h/6h?

Збігається з `lib/email.ts` (S70 email DLQ) — однаковий UX для admin, що дивиться на failed jobs. Усього 5 attempts ≈ 8.5 годин total напіввідкритий retry-window, після чого job → `failed` і потребує manual intervention.

### Why `force-dynamic` cron endpoint?

Будь-який cron в Next.js, що пише в DB, має бути dynamic — щоб не cache-уватись. `runtime = "nodejs"` явно, бо prisma не працює в edge runtime.

---

## SyncJob lifecycle

```
            ┌────────────────────────┐
PATCH ────► │ pending (just created) │
            │ attempts=0             │
            │ nextAttemptAt=now      │
            └───────────┬────────────┘
                        │
                cron triggers
                        ▼
              ┌──────────────────┐
              │ sendToProxy()    │
              │ + idempotencyKey │
              └────────┬─────────┘
                       │
              ┌────────┴─────────┐
            success            failure
              │                  │
              ▼                  ▼
       ┌──────────┐    ┌───────────────────┐
       │ sent     │    │ attempts < max:   │
       │ sentAt   │    │   status=retrying │
       └──────────┘    │   nextAttemptAt=  │
                       │     now + backoff │
                       │ attempts ≥ max:   │
                       │   status=failed   │
                       │   lastError       │
                       └───────────────────┘
                                 │
                       retry round (next cron)
                                 │
                                 ▼
                  back to sendToProxy() loop
```

---

## Mock mode

`SYNC_MOCK_MODE=true` — manager-sync приймає request, симулює delay 100-500ms (random), повертає `{ok: true, code1C, mockMode: true}`. Це шлях за замовчуванням до моменту коли 1С-розробник реалізує SOAP-модулі.

`SYNC_MOCK_MODE=false` — manager-sync робить real SOAP-call до `ONEC_SOAP_URL`. У M1.5 цей шлях coded але не тестується (нема живого 1С). У M1.5b / окремiй follow-up — перший real handshake.

---

## Файли

| Файл                                                  | Призначення                              |
| ----------------------------------------------------- | ---------------------------------------- |
| `services/manager-sync/src/index.ts`                  | Fastify HTTP server entry                |
| `services/manager-sync/src/config.ts`                 | Env vars + validation                    |
| `services/manager-sync/src/auth.ts`                   | Shared-secret middleware                 |
| `services/manager-sync/src/soap/client.ts`            | Real SOAP wrapper (fetch + XML envelope) |
| `services/manager-sync/src/soap/mock.ts`              | Mock implementation                      |
| `services/manager-sync/src/idempotency.ts`            | In-memory TTL cache                      |
| `services/manager-sync/src/routes/sync-clients.ts`    | POST /sync/clients/:id                   |
| `services/manager-sync/src/routes/sync-orders.ts`     | POST /sync/orders/:id (M1.5b)            |
| `services/manager-sync/src/routes/sync-payments.ts`   | POST /sync/payments/:id (M1.5b)          |
| `apps/store/app/admin/sync-jobs/page.tsx`             | Admin queue browser + retry (M1.5b)      |
| `apps/store/app/api/v1/manager/orders/route.ts`       | POST для створення замовлення (M1.5b)    |
| `apps/store/app/api/v1/manager/payments/route.ts`     | POST для створення оплати (M1.5b)        |
| `apps/store/app/manager/(workstation)/orders/new/`    | UI form створення замовлення (M1.5b)     |
| `apps/store/lib/sync/enqueue.ts`                      | enqueueClientUpdate() helper             |
| `apps/store/lib/sync/proxy-client.ts`                 | HTTP-клієнт Next.js → proxy              |
| `apps/store/lib/sync/queue-processor.ts`              | Batch processor з backoff                |
| `apps/store/lib/validations/sync-job.ts`              | Zod schemas                              |
| `apps/store/app/api/cron/process-sync-queue/route.ts` | Cron entry                               |
| `apps/store/app/api/v1/manager/sync/status/route.ts`  | GET для UI sync indicator                |
| `packages/db/prisma/migrations/2026MMDD_sync_jobs/`   | DB migration (additive)                  |
| `docs/1C_SYNC_MODULES_SPEC.md`                        | Контракт BSL-модулів для 1С-розробника   |

---

## Env vars

| Name                         | Where        | Required        | Notes                                        |
| ---------------------------- | ------------ | --------------- | -------------------------------------------- |
| `MANAGER_SYNC_URL`           | apps/store   | yes             | `http://localhost:3001` локально             |
| `MANAGER_SYNC_SHARED_SECRET` | both         | yes (≥16 chars) | shared між proxy і Next.js                   |
| `CRON_SECRET`                | apps/store   | yes (≥16 chars) | already from S70 email queue                 |
| `MANAGER_SYNC_PORT`          | manager-sync | no (3001)       |                                              |
| `SYNC_MOCK_MODE`             | manager-sync | no (`true`)     | `true` для dev, `false` коли 1С готовий      |
| `ONEC_SOAP_URL`              | manager-sync | only mock=false | URL до `MobileExchange.1cws`                 |
| `ONEC_SOAP_PASSWORD`         | manager-sync | only mock=false | shared з 1С-стороною (constant `СинкПароль`) |

---

## Майбутнi розширення

- **M1.5b** shipped: POST orders / payments + UI form + admin retry UI ✓
- **M1.6** — inbound polling з 1С (snapshot оновлення `mgr_*` tables): новий route у manager-sync `/sync/snapshot/fetch` + cron `/api/cron/poll-1c-snapshot`
- **Real-connection wire-up** — після того як 1С-розробник реалізує BSL-модулі, перемкнути `SYNC_MOCK_MODE=false` + виставити `ONEC_SOAP_URL` / `ONEC_SOAP_PASSWORD` у production env
- **V2** — distributed idempotency cache (Redis / pg LISTEN)

---

## See also

- `docs/1C_SYNC_MODULES_SPEC.md` — повний контракт для 1С-розробника (BSL signatures + JSON shapes + error codes + XML examples)
- `docs/MANAGER_APP_STRATEGY.md` §3 — стратегiчний lock на hybrid integration
- `apps/store/lib/email.ts` + `docs/EMAIL_QUEUE.md` — reference pattern для queue з S70

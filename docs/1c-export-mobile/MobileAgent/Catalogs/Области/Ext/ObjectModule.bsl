Перем мЭтоОбмен Экспорт;


Процедура ПередЗаписью(Отказ)
	
	Если ЭтоНовый() И НЕ мЭтоОбмен Тогда 
		СправочникОбласти = Справочники.Области.НайтиПоНаименованию(ЭтотОбъект.Наименование);
		Если Не СправочникОбласти.Пустая() Тогда 
			Отказ = Истина;
			Сообщить("Область вже створена!");
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

мЭтоОбмен = Ложь;
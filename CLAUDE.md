# L-TEX Ecosystem — Project Context

## Business Overview

L-TEX is a Ukrainian wholesale business based in Піддубці, Луцький район, Волинська область.
They sell wholesale (від 10 кг) second-hand clothing, stock (new surplus), toys, Bric-a-Brac, shoes, accessories from England, Germany, Canada, Poland.
Contacts: Telegram @L_TEX, +380 67 671 05 15, +380 99 358 49 92, ltex.lutsk.ai@gmail.com

**IMPORTANT for SEO/copy:** L-TEX sells NOT ONLY секонд хенд but also сток (new surplus), іграшки, Bric-a-Brac. Always mention full assortment.

## Current Status

**⚡ АКТУАЛЬНО (2026-07-08, сесія 7.4 — Реалізація → регістри управлінського обліку + доробки форми продажу):** Гілка `claude/realizacii-functionality-audit-hg9den`, усе merged у **`main`**@`125f9a84` (коміт `e6ff2dc8` + merge). **Міграцій НЕМАЄ** — таблиці регістрів (`SalesMovement`/`StockMovement`/`CostMovement`/`MgrDebtMovement`) уже існують з 5.6. **Деплой: `git pull` → `deploy.ps1 -SkipInstall`** (без `prisma migrate deploy`). **Аудит-документ** (порівняння блоку «Реалізації» 1С vs наша система + питання про регістри/ризик втрати даних) + **ТЗ** — `docs/SALE_REGISTERS_SPEC.md`. **Контекст:** проведення реалізації в нашому UI раніше писало live лише рух боргу (`MgrDebtMovement`); склад/продажі/собівартість наповнювались ЛИШЕ історичним імпортом з 1С → нові продажі «не бачились» у звітах Залишки/Обороти/Маржа. **Рішення user:** бухгалтерію (ПДВ/каса регламент) + sync у 1С НЕ робимо (нема живого 1С), але управлінський облік (залишки/собівартість/обороти) — довести до паритету з 1С. **Зроблено:** **(A, головне)** новий `lib/manager/sale-movement-hooks.ts` (`applySaleMovements`/`removeSaleMovements` + чиста `buildSaleMovementRows`, дзеркалить `stock-movement-hooks.ts`): проведення реалізації (`status=posted`) пише `StockMovement` (розхід `recordKind=1`), `SalesMovement` (продаж `recordKind=0`), `CostMovement` — **реєстратор = `sale.code1C ?? sale.id`** (нові реалізації без code1C → sale.id; історичні hex не конфліктують), запис **delete-then-create** у `$transaction` (ідемпотентно, без stale-рядків); собівартість €/кг: `Lot.purchasePriceEur` → батч остання `PurchasePrice` по productId → 0; агент `assignedAgentUserId→User.code1C`. Підключено в `sale-create.ts` (обидва місця поряд з `applyDebtMovementSafe` — create `if(post)`, update `if(becomesArchived)`); реверс `removeSaleMovements(existing.code1C ?? existing.id)` у DELETE-роуті `sales/[id]/route.ts` (+code1C у select). **Правка `lib/reports/margin-flex.ts`** — recorder fallback `code1C ?? it.sale.id` (select +id, revenue-цикл + cost-цикл), щоб CostMovement НОВИХ реалізацій підтягувалась у звіт маржі (раніше `if(!code1C) continue` їх відкидав). `sales-flex` (Обороти) читає `SalesMovement` напряму + `stock-flex` (Залишки) `groupBy` зі знаком за recordKind — нові рухи з'являються автоматично, без змін. **(B)** контроль відхилення ціни >0.20€ при «Зберегти та провести»: чиста `collectPriceDeviations` (еталон `unitPriceForType(prices,"wholesale")`, відхилення округлене до копійок від float-шуму) → **портальний inline-діалог** (БЕЗ `window.confirm` — блокується в iframe-shell) зі списком позицій «Все одно провести»/«Скасувати»; для чернетки не діє. **(C)** перевірка чужої броні: чиста `isForeignActiveReservation(lot,currentUserId,now)` в `sale-form.tsx::onBarcode` (by-barcode вже повертає `reserved*`) — чужа активна бронь блокує додавання рядка «Активна бронь мішка до …»; своя/протермінована — не блокує; `currentUserId` прокинуто з `sales/new/page.tsx` + `sales/[id]/page.tsx` (`user.id`). **(D)** «Повторити ціну» (`repeatPriceForProduct` у `sale-items-editor.tsx`) — вже було з попередньої сесії, верифіковано + покрито тестами; ПКМ-контекст-меню НЕ додавали (inline-кнопки достатньо). **⚠️ Важливий нюанс семантики (підтверджено):** `SaleItem.weight` = **сумарна** вага рядка (мішки вбудовані через `bagWeightForQuantity`; `changeBags` пише `perBag×quantity`), `priceEur = pricePerKg × weight` (lineTotalEur з quantity=1 на додаванні/changeBags). Тому hook рахує `costEur = costPerKg × item.weight` (БЕЗ ×quantity) — щоб виручка й собівартість лежали на одній базі (маржа коректна); `quantity` пише лише в `qty` регістрів; `weightKg` = `weight` для `priceUnit==="kg"`, інакше null. **Відкритий хвіст (не блокує, pre-existing, не наш scope):** у формі продажу `changeUnitPrice`/`recalcAllRows` подекуди викликають `lineTotalEur(...,quantity)` замість `,1)` — потенційне подвоєння priceEur при ручній зміні ціни/типу цін коли quantity>1 (латентний баг форми з попередніх сесій; за потреби окремо узгодити/полагодити). **Тести:** typecheck чистий, prettier чистий, **2466 pass / 2 skip** (56 нових/розширених у `sale-movement-hooks.test.ts`/`margin-flex.test.ts`/`sale-types.test.ts`), 0 нових `any`. Магазин/історичний імпорт/блок Замовлень не зачеплені. **НЕ зроблено свідомо:** реальний sync/вивантаження в 1С (нема живого 1С); прапорець `exportTo1C` лишається як «проводити/не проводити», нікуди не передається.

**⚡ АКТУАЛЬНО (2026-07-07, сесія 7.2 — майстер-план 7.0 ЗАВЕРШЕНО: сайт→CRM повний цикл, Блоки 0–4 + редаговані довідники):** Гілка `claude/nomenclature-duplicates-f0wgce`, УСЕ merged у **`main`@`cbfd4508`** (fast-forward пуші після кожного кроку; гілка = main). Спека/журнал: `docs/SESSION_7.2_BLOCK1_SITE_ORDERS.md`. **⚠️ КОМУНІКАЦІЯ З USER: без технічної освіти** — проста людська мова; команди давати ГОТОВИМИ до копіювання, по одному рядку, у код-блоках ТІЛЬКИ команди (пояснення поза блоками; жодних «→» чи коментарів усередині команд). **Дублікати номенклатури (7.1) ✅ ЗАКРИТО:** apply злиття 491 групи виконано на проді (повторний прогін = 0 груп); tie-break фікс merge-dup (короткий code1C перемагає падений `00000001358`). **Блок 1 — замовлення з сайту→CRM ✅:** чекаут `/api/orders` створює чернетку (`Order.source="site"`, status=draft), маршрутизація 1A (phone-match→`MgrClient.agentUserId`; новий+область→`MgrRegionAgent`; інакше нагадування admin/owner); конкретні лоти бронюються + штрихкоди дублюються в notes «Склад — відвантажити лоти: …»; авто-нагадування `auto_site_order` (лінк `MgrReminder.orderId`, само закривається при posted/cancelled через `completeSiteOrderReminders` у PATCH); дропдаун «Область» у кошику; бейдж «Сайт» у списку/картці + фільтр Джерело (`buildOrdersWhere.source`); вкладку Замовлення ПРИБРАНО з адмінки (дзвіночок/Telegram/Viber-сповіщення → `/manager/orders?source=site`). **Блок 2 — клієнти/ліди ✅:** `lib/manager/site-client.ts::resolveOrCreateSiteClient` (наявному клієнту БЕКФІЛИТЬ region/agentUserId з даних сайту; новому — створює MgrClient+таймлайн); **видимість**: менеджер бачить замовлення OR(свої code1C, `assignedAgentUserId`=він) — `buildOrdersWhere.viewerUserId` через `where.AND`, знято short-circuit «0 клієнтів», `canViewOrder` поважає агента; вкладку Клієнти ПРИБРАНО з адмінки. **Ліди:** модель `MgrLead` + `/manager/customers/leads` (Активні/Конвертовані/Відхилені; «Створити клієнта» з дедупом по телефону; авто-конвертація при замовленні через `markLeadsConverted`); хук у реєстрацію `/api/auth/customer/login` → `createSiteLead`; бейдж «Ліди з сайту (N)» у Клієнтах. **Фікси за live-тестом:** лінк «Відкрити картку клієнта» в картці замовлення (резолв MgrClient за code1C АБО phonePrimary → `mgrClientId` в OrderForm); список замовлень: Область по телефону (fallback без code1C) + Агент = `assignedAgentName` (batch-lookup User); чекаут префілить Імʼя/Телефон з кабінету (`getCurrentCustomer`). **Блок 3 — каталог у CRM ✅:** (3.1) фото товару в CRM-картці — `lib/manager/catalog-permissions.ts::canManageCatalog` (**лише admin/owner/warehouse**), `prices/[id]/photo-actions.ts` (sharp+`/media`, дзеркало адмінки) + `ProductPhotoManager`; (3.2) категорії `/manager/categories` — ПОВНЕ рекурсивне дерево 1С (4 рівні: Тип→Сезон→Категорія→Підкат.; дерево в БД правильне, імпортер `--entity categories` Фаза A/B; user реімпортував на проді: 31 папка/25 звʼязків), CRUD (1С-категорії `code1C!=null` НЕ видаляються); **`CategoryCascader`** — каскадний вибір рівень-за-рівнем у 3 місцях (батько категорії/створення товару/картка товару); **`Category.hiddenFromCatalog`** + toggle «Видима/Прихована» — гілка ховається з каталогу/пошуку/прайсу агентів/лотів (`lib/catalog-visibility.ts::getHiddenCategoryIds`, закриття піддерева; ролі каталогу бачать); зміна категорії товару з картки (`changeProductCategory`) повертає видимість — user має позначити «Більше не брати/Перепаковка/Поки не буде» прихованими; (3.3) створення товару `/manager/products/new` (+кнопка на Прайсі): обовʼязкові назва/артикул/категорія/одиниця/ціна/опис/стать/розміри + quality/country; авто-slug; **авто-code1C формату 1С** (`MAX(числових code_1c)+1`, падінг 11 знаків — продовжує 1С-нумерацію); Price wholesale €; поля Виробник (select з довідника) + YouTube; (3.4) вкладки Товари/Лоти/Категорії ПРИБРАНО з адмінки (лишились Дашборд/Банери/Топ/Промо/Email). **Блок 4 — дашборд сайту ✅:** лічильник візитів БЕЗ кукі/PII — моделі `SiteVisitDay`/`SiteVisitor`, роут `/api/track/visit` (sha256 ip+ua+день+`MANAGER_JWT_SECRET`), бекон `VisitTracker` у `(store)/layout`; `lib/site-stats.ts::getSiteStats`; секція «Сайт» на `/admin` (візити/унікальні сьогодні+7дн, замовлення з сайту 30дн, активні ліди, топ переглядів ViewLog). **Довідники редаговані ✅:** `/manager/dictionaries/[type]` — 6 типів (`routes`/`client-statuses`/`search-channels`/`categories-tt`/`delivery-methods`/`producers`), єдиний редактор (додати/редагувати/видалити; **запис ЛИШЕ owner/admin**, перегляд усім); `lib/manager/simple-dict-{config,actions}.ts`; картки реєстру partial→ready; новий довідник **Виробники** (`MgrProducer`); форма товару: Виробник=select з довідника, Стать=фіксований список (Чоловіча/Жіноча/Дитяча/Унісекс/Мікс). Політика user: **усі поля карток — значення з довідників** (картка клієнта вже так з M1.3: statusId/channelId/categoryTTId/deliveryMethodId — FK-дропдауни). **6 міграцій 7.2:** `20260706_block1_site_orders`, `20260707_mgr_leads`, `20260708_category_hidden`, `20260709_product_producer`, `20260710_site_visits`, `20260711_mgr_producer`. **⚠️ Деплой-хвіст:** user підтвердив деплої приблизно до Блоку 3.3+каскадер+producer-поле; останні кроки (Блок 4 `20260710` + довідники `20260711`) МОГЛИ ще не деплоїтись — на старті нової сесії звірити `prisma migrate deploy` до `20260711` включно → `prisma generate` → `deploy.ps1 -SkipInstall`. **Мапа область→агент** заповнена user-ом (`/manager/admin/region-agents`). **Відкриті хвости (не блокують):** ~~приховані категорії на головній~~ ✅ ЗАКРИТО 7.3 (фільтр `getHiddenCategoryIds`/`hiddenCategoryProductFilter` на головній/`/top`/`/sale`/`/new`/mobile home/рекомендаціях/`/api/categories`); ~~замовлення site-клієнта без code1C на картці клієнта~~ ✅ ЗАКРИТО 7.3 (вкладка Замовлення матчить OR(code1C, phone=phonePrimary)); ~~pre-existing fail `sales/route.test`~~ ✅ ЗАКРИТО 7.3 (тест очікував 6 OR-гілок, а пошук з 5.4 має 7 — з number1C; оновлено тест); спосіб доставки у ФОРМІ ЗАМОВЛЕННЯ — фіксований мінісписок `ORDER_DELIVERY_METHODS` (не повний довідник) — за бажання user узгодити; Задача B (чистка не-1С товарів `delete-non-1c-products.ts`) чекає підтвердження цифр user; видалення старих `claude/*` гілок на GitHub. **1С НЕ зачеплено.**

**⚡ (2026-06-17, сесія 5.6 — повний паритет 1С: усі довідники/регістри/документи + перенос даних):** Гілка **`claude/charming-ptolemy-40syy0`**, усе merged у `main`@`0ccbd662`. Стратегія: відтворити КОЖЕН довідник/регістр/документ 1С + перенести 100% історії. **Інвентар:** `docs/1C_REGISTRY_INVENTORY.md` (3 паралельні ресьорч-агенти по мобільній+центральній конфізі). **План:** `docs/SESSION_5.6_FULL_PARITY_PLAN.md` (8 фаз). **Архітектура:** кожен 1С-регістр накопичення → таблиця рухів (патерн `MgrDebtMovement`), історія з MSSQL `_AccumRg…` напряму, нові документи пишуть рухи при проведенні, звіти читають з рухів. **Зроблено (усі 8 фаз через паралельні worktree-воркери + ручна інтеграція):** **Браузерні вкладки** менеджерки (iframe-shell `45e10375`); **компактний UI** списків/форм (`fcff2747`); **Фаза 0** каркас «Довідники та регістри» + хаб «Звіти» + переглядач регістру (борг) `c96d596e`; **Фаза 1** довідники (Unit/Region/Cityy/MgrTradeAgent/ViberContact, `--entity dictionaries-full`); **Фаза 2** регістри-обороти `SalesMovement`/`CashFlowMovement`/`StockMovement`/`OrderRemainderMovement` (`_AccumRg5604/5309/5788/5374`, `--entity sales-reg|cashflow-reg|stock-reg|orders-reg`) + звіти sales-summary/cashflow/stock-balance; **Фаза 3** `CostMovement` (ПродажиСебестоимость `_AccumRg5634`, `--entity cost-reg`) + звіт **Маржа/Валовий прибуток**; **Фаза 4** історичні курси (`КурсыВалют _InfoRg4655`, `--entity rates`, `getEurRateForDate`); **Фаза 5** 8 документів руху товару (ProductReturnFromCustomer пише борг-рух + Repacking/WriteOff/Inventory/Transfer/Returns/StockAdjustment, `--entity returns/repack/writeoff/stockadjust/inventory/transfer`); **Фаза 6** банк/каса (`BankPaymentIncoming/Outgoing/CashTransfer` + `MgrCashOrder.paymentMethod`, `--entity bankdocs/cashtransfers`) + звіт **Акт звірки**; **Фаза 7** **XLSX-експорт** (exceljs, `xlsx`-роут + resolve-report); **Фаза 8** дрібне (`AgentDayLog/StockNorm/SupplierReliability/ClientStatusHistory`, `--entity misc`). **7 нових міграцій:** `20260617_{dictionaries_full,accum_registers,cost_register,exchange_rate_series,stock_documents,financial_documents,misc_registers}`. **⚠️ КРИТИЧНО — коди MSSQL:** регістри з підтвердженими кодами — debt(`_AccumRg5269`), cost(`_AccumRg5634`); решта `*-reg`/`bankdocs`/`cashtransfers`/`returns…`/частина `misc` мають коди `_Fld`/`_DocumentNNN` як **TODO у коді** (пісочниця без живого MSSQL) — їх треба декодувати на сервері (`columns.tsv`/`dbnames.tsv` у `docs/1c-mssql-schema/` + `sp_help`) ПЕРЕД реімпортом; ці entity свідомо НЕ в `DEFAULT_ORDER`, лише ізольовано. **Деплой 5.6:** `prisma migrate deploy` (7 міграцій) → `prisma generate` → `pnpm install` (новий `exceljs`) → `deploy.ps1` → потім декодувати коди + ізольований реімпорт `--entity` по черзі зі звіркою. **MSSQL живий** (user підтвердив). **Відкрите:** тематичний хаб звітів/каталог за темами (полір Фази 7 відкладено); реконсиляція UI nav. **1С НЕ зачеплено.** **⚡ Автономний догін (2026-06-18, user away, `main`@`513e7e38`):** (A) **декодовано ВСІ коди MSSQL офлайн** з `docs/1c-mssql-schema/` → `docs/1C_MSSQL_CODES.md` (Viber `_Reference7527`, bankdocs `_Document170/171`, cashtransfers `_Document121`, stock-docs `_Document123/6631/193/156/140/162`, +підтверджено sales/stock/rates/dictionaries) — **2 тихі баг-фікси:** cashflow Контрагент (`_Fld5318RRef`=Проект→`_Fld5315_RRRef`) + Enum ПриходРасход (`_Enum225`), orders qty (`_Fld5380`=Цена→`_Fld5387`=Количество); (B) **hooks Фази 5→StockMovement** (`lib/manager/stock-movement-hooks.ts`: проведення складських документів пише рухи; ProductReturnFromCustomer пише і борг, і прихід-склад) + поле `unitCode1C/unitId` на OrderItem/SaleItem (**нова міграція `20260617_unit_links`**); (C) **полір звітів** (теми Продажі/Фінанси/Склад/Борг, catalog-derived nav, XLSX+CSV на всіх звітах через `resolveReport`+`registry-report-builders`). Усе merged. **Деплой догону:** +`20260617_unit_links` міграція. **Коди тепер у скрипті** — user робить лише `--entity <name> --dry-run --confirm-prod` → звірка → запис (декодування на сервері більше НЕ потрібне). Беклог: `docs/SESSION_5.6_AUTONOMOUS_BACKLOG.md`.

**⚡ Сесія 5.8 (2026-07-02, Крок-1 звірка + перенос складських документів + раунд правок звітів):** Гілка `claude/charming-ptolemy-40syy0`, усе merged у `main` (до `1aed8446`). Чек-лист звірки — `docs/STEP1_RECONCILIATION_CHECKLIST.md`. **B1 (борг) звірено ✅** (регістр ВзаиморасчетыСКонтрагентами `_AccumRg5269`, MgrClient.debt). **Перенос 6 складських документів (Фаза 5, реально залито на прод):** returns 60 / repack 3766 / writeoff 722 / stockadjust 16 / inventory 68 / transfer 0 (у 1С немає переміщень) + warehouses 1 (з 18 рядків 17 — папки) + qualities 1. **2 фікси кодів колонок** (декодовано з 1С-XML + `columns.tsv` anchors): returns Контрагент = `_Fld712RRef` (було `_Fld706`=СкладОрдер-поліморф → 60/60 unresolved); transfer склади = `_Fld2219RRef`/`_Fld2220RRef` (було `_Fld2211`/`_Fld2213`=ВнутреннийЗаказ/Организация); мапа в `docs/STOCK_DOCS_IMPORT_MAP.md`. **Нова міграція `20260701_fix_warehouse_code1c_unique`:** таблиці warehouses/suppliers/receivings мали ЧАСТКОВИЙ unique-індекс `WHERE code_1c IS NOT NULL` (зі старої `20260604`), а Prisma `@unique` очікує full → `upsert` (`INSERT … ON CONFLICT (code_1c)`) падав `PrismaClientUnknownRequestError` при імпорті складів; перестворено як full-unique (на nullable NULL-и все одно різні). **Раунд правок звітів (за фідбеком user, commit `1aed8446`):** (1) **ДДС мультивалютний** — `CashFlowMovement.currency_code` (міграція `20260702_cashflow_currency`), валюта резолвиться при `--entity cashflow-reg` з `БанковскийСчетКасса → ВалютаДенежныхСредств` (банк `_Reference29._Fld5873RRef` + каса `_Reference56._Fld6004RRef` → `_Reference30` classifyCurrency); звіт `cashflow-flex.ts` розкладає Сумму на окремі колонки ₴/€/$ (гейт по currencyCode) + «упр. €» (СуммаУпр, валюта управління); (2) **flex-tree** money-колонки тепер валютно-свідомі (`currency:"uah"|"eur"|"usd"` → символ ₴/€/$) — раніше завжди дописувався «€» (звідси баг «₴-заголовок, €-значення»); (3) **stock-flex** — «Найменування» окремою вимкненою колонкою (`ATTR_COLUMNS`+`DEFAULT_ATTRS`). **Видалення документів** (Замовлення/Реалізації/Оплати/Маршрути) — код коректний у всіх блоках з `window.confirm` + серверна транзакція з перерахунком боргу (`doc-delete-permission.ts`, DELETE-роути); скарга «не видаляється» = стара збірка на проді, лікується деплоєм. **⚠️ Деплой 5.8:** `git pull` → `prisma migrate deploy` (`20260701`+`20260702`) → реімпорт `--entity cashflow-reg` (currency_code) + `--entity dictionaries` (ієрархія папок ДДС) → `deploy.ps1 -SkipInstall`. Реімпорт складських документів (`returns/repack/writeoff/stockadjust/inventory`) уже виконано на проді. **Тести зелені** (reports+registry 165). **1С НЕ зачеплено.**

**⚡ Сесія 5.9 (2026-07-02, автономна код-робота — пункти 2–3 чек-листа звірки, `main`):** Гілка `claude/charming-ptolemy-40syy0`. Спека `docs/SESSION_5.9_AUTONOMOUS.md`, чек-лист `docs/STEP1_RECONCILIATION_CHECKLIST.md` (пункти 2–3 позначено «зроблено в коді, чекає реімпорту/деплою»). Пісочниця (без живого MSSQL/сервера) — лише код+typecheck+vitest+prettier. **Зроблено:** (1) **назви кас у ДДС** — новий `importCashRegisters` в `import-1c-historical.ts`: Кассы `_Reference56` → модель `MgrBankAccount` (без зміни схеми; каси і банк — одна мапа `mgrBankAccount` по code1C у `cashflow-flex.ts`), пропуск папок (`_Folder=0`, ІНВЕРСНА логіка 1С), реєстрація у `ctx.bankAccountByHex`, виклик у `--entity dictionaries` (commit `996aaf10`); (2) **вага stock-reg** — JOIN `_AccumRg6608` (ТовариНаСкладахУВазі) уже був реалізований у попередній сесії (`loadStockWeightMap`, ключ recorder|product|char|warehouse, `_Active=0x01`, ресурс `_Fld6612` numeric(10,2) — єдиний ресурс-вага, підтверджено по `columns.tsv`); оновлено застарілий коментар (той самий commit); (3) **друк касового ордера** КО-1/КО-2 — `payments/[id]/print/page.tsx` за патерном `sales/[id]/print` (A4+`@media print`+`COMPANY_REQUISITES`, Приход→КО-1/Расход→КО-2, кнопка «Друк» на картці, commit `5d0ac42b`); (4) **друк маршрутного листа** — `routes/[id]/print/page.tsx` (шапка №/дата/експедитор/маршрут/кілометраж + таблиці замовлень-зупинок і товарів, батч-резолв назв, `formatOrderNumber`, кнопка «Друк», commit `023df9e3`). **⚠️ User після мержу:** реімпорт `--entity dictionaries` (назви кас) + `--entity stock-reg` (вага) через обгортку `scripts/import-1c.ps1`; друк-сторінки — лише деплой (нових міграцій НЕМАЄ). typecheck чистий, registry-import-map+dictionaries-full тести зелені (21). **1С НЕ зачеплено.**

**⚡ Сесія 5.8+ follow-up (2026-07-03, правки за фідбеком + самохостинг медіа, `main`):** (1) **Видалення документів** — `window.confirm`/`alert` ТИХО блокуються у вкладках-iframe менеджерки (`iframe-host`), тому «Видалити» нічого не робило. Новий хук `useDocDelete` (портальний діалог підтвердження + inline серверна помилка) замінив `window.confirm` у всіх 4 блоках (sales/orders/payments/routes). (2) **Звіт «Залишки складу» — продуктивність:** `buildStockFlexReport` тягнув усі ~336к рухів у Node → гальмувало на групуванні Артикул/Найменування; тепер SQL-агрегація `prisma.stockMovement.groupBy` (by product/warehouse/quality/recordKind, `_sum` qty+weight) → у застосунок йдуть кілька тисяч згорнутих позицій, підсумки ті самі (`rowCount` = позиції балансу). (3) **Задача A — самохостинг медіа (прибрано Supabase Storage для нових фото):** `lib/media/storage.ts` (fs-хелпери + `resolveInsideRoot` захист від `..`, env `MEDIA_ROOT`/`MEDIA_PUBLIC_URL`), роут `app/media/[...path]/route.ts` (роздача з диску, immutable-кеш), переписані `uploadProductImage`/`deleteProductImage` (`app/admin/products/actions.ts`) + `uploadBannerImage` (banners) на запис `.webp` на диск (sharp-пайплайн незмінний), `next.config.js` +`new.ltex.com.ua` у remotePatterns+CSP (Supabase ЛИШЕНО до чистки Задачі B). **Наявні фото НЕ мігровано** (заміняться новими). Спека — `docs/SESSION_6.0_SELFHOST_MEDIA.md`. **⚠️ Деплой A:** створити папку `E:\ltex-storage\media`, додати `.env` `MEDIA_ROOT`+`MEDIA_PUBLIC_URL`, додати папку в бекап, `deploy.ps1` (без міграцій). **Відкрито — Задача B:** чистка не-1С товарів (`code1C IS NULL`) — чекає рішень user (порожні категорії? товари з історією продажів?) → аудит `scripts/audit-non-1c.ts` → скрипт `delete-non-1c-products.ts` (dry-run). typecheck+тести зелені.

**⚡ Сесія 6.1 (2026-07-03, адмін-логін з Supabase Auth → власний JWT, `main`):** Supabase-проєкт заснув (free-tier пауза) → адмін-логін «Failed to fetch». Рішення user: **повністю прибрати Supabase**. **База вже локальна** (Postgres на сервері; каталог працював навіть із заснулим Supabase). **Storage** прибрано у Задачі A (6.0). Тепер **Auth:** `/admin/*` автентифікується через нашу таблицю `users` (ролі `{admin,owner}`) перевикористовуючи менеджерський JWT (cookie `ltex_mgr_access`). Зроблено: `requireAdmin` → `getCurrentUser`+`ADMIN_ROLES` (18 call-sites без змін); новий `middleware-admin.ts::adminGuard` (fail-closed, дзеркало `managerGuard`) + `middleware.ts` `/admin`→adminGuard; `/admin/login` через `/api/v1/manager/auth/login`+`/me` (перевірка ролі, `?forbidden=1`); **фікс латентного бага** `verifyAccessToken` (приймав лише manager/senior_manager/admin → owner/warehouse/analyst/… не проходили верифікацію; тепер `VALID_ROLES` — усі ролі, авторизація й далі за downstream allow-list); `seed-admin-user.ts` +`SEED_ADMIN_RESET` (скидання пароля наявному + promote owner); **видалено** `lib/supabase/*` + депи `@supabase/ssr`/`@supabase/supabase-js`. `next.config.js` CSP/remotePatterns із Supabase ЛИШЕНО (старі фото ще на Supabase-URL → приберемо у Задачі B). Спека — `docs/SESSION_6.1_ADMIN_AUTH_OFF_SUPABASE.md`. **⚠️ Деплой 6.1:** `git pull` → `pnpm install --frozen-lockfile --prod=false` (змінено package.json) → сідінг нового пароля (`SEED_ADMIN_EMAIL/PASSWORD/RESET=1` + `tsx scripts/seed-admin-user.ts`) → `deploy.ps1` → вхід `/admin/login`. Supabase для входу більше не потрібен; **повне видалення проєкту Supabase — лише після Задачі B**. typecheck чистий, тести зелені (1 pre-existing fail у sales/route.test — не повʼязаний).

**⚡ Сесія 6.2 (2026-07-03, Задача B — скрипт чистки не-1С товарів, `main`):** Рішення user: лишаємо ЛИШЕ `code1C IS NOT NULL` (включно з видаленням Excel-вітрини S71 ~805 товарів — заміниться новими фото на 1С-товарах); товари з історією (OrderItem/SaleItem/**ReceivingItem**) — НЕ видаляти, а ховати (`inStock=false` + прибрати images/prices/lots/cart/favorites/featured); порожні не-1С категорії — видалити bottom-up (1С-дерево не чіпати). Новий `scripts/delete-non-1c-products.ts`: **dry-run за замовчуванням**, запис лише `--apply --confirm-prod` (+pg_dump-попередження+5с пауза), батчі 200 у транзакціях, класифікація keep1C/deleteFull/hideHistory (чистий `classifyProduct`+7 тестів), окремий список «hiddenDueToReceivingOnly» (без продажів, але з Поступленням — Restrict FK). FK-порядок: Product-каскади (images/prices/favorites/featured/viewlog/videosub/purchaseprices), Restrict → Lot/CartItem видаляються явно; Lot-діти SetNull (історія цілa). Пост-звіт: лишкові supabase-URL у ProductImage/Banner (очікуємо 0/0). `next.config.js`: `*.supabase.co` ПРИБРАНО з remotePatterns+CSP img-src (у тому ж коміті — **деплоїти ПІСЛЯ apply**, інакше старі фото тимчасово зникнуть). Спека — `docs/SESSION_6.2_TASK_B_CLEANUP.md`. **⚠️ Прогін user:** pg_dump → `tsx scripts/audit-non-1c.ts` → dry-run `tsx scripts/delete-non-1c-products.ts` → звірка цифр → `--apply --confirm-prod` → деплой → видалення Supabase-проєкту (після 0/0). Після цього Supabase зникає повністю (база локальна з 5.2, Storage у 6.0, Auth у 6.1).

**⚡ Сесія 7.0/7.1 (2026-07-03, переробка адмінки: Блок 0 знос 1С-синхронізації + інструмент злиття дублікатів, `main`@`da372f0c`):** Майстер-план — `docs/SESSION_7.0_ADMIN_OVERHAUL.md` (сайт=вітрина, система=джерело правди; рішення user: нові клієнти → мапа область→агент+дефолт; замовлення з кошика → чернетка+нагадування; фото → в картці товару системи; дашборд → власний лічильник візитів). **Блок 0 ✅ (`001fedc3`):** знесено ВСЮ живу 1С-синхронізацію — адмін-сторінки (Журнал синхр./Черга sync 1С/розділ «1С СИНХРОНІЗАЦІЯ»/Курси валют), `/api/sync/*`, sync-jobs API, крони process-sync-queue/pull-from-1c, `lib/sync/*`, сервіс `services/manager-sync/` цілком (+ecosystem.config), індикатор «Синхронізовано» в шапці, усі enqueue-виклики в документах; closures переведено на inline-тип (SOAP-клієнт видалено). **ЗАЛИШЕНО:** офлайн-імпорт (`import-1c-historical.ts`+`import-1c.ps1`) до фінальної звірки; таблиці mgr_sync_jobs/state у БД (без дропу). На сервері знято Windows-задачу «LTEX Process Sync Queue» + `pm2 delete ltex-manager-sync` (задачі «Pull From 1C» не існувало). **7.1 злиття дублікатів ✅ (`da372f0c`):** 1С сама містить дублікати номенклатури (один артикул, 2 code1C: старий запис з історією у «Різне»/«Акції» + актуальний; підтверджено SQL: 37047/37062/58058 обидва from_1c=t). Нова модель **`ProductMerge`** (міграція `20260703_product_merge`; oldCode1C→targetProductId) + скрипт `scripts/merge-duplicate-products.ts` (dry-run дефолт, `--apply --confirm-prod`, `--only <артикул>`, tx timeout 120с; survivor: більше вільних лотів→inStock→новіший; перенос Lot/OrderItem/SaleItem/ReceivingItem/PurchasePrice/ViewLog/VideoSub/Favorite+CartItem з дедупом/Price за семантичним ключем/Images append; рухи 4 регістрів repoint по productCode1C=oldHex) + **імпортер поважає мапу** (`ctx.mergedCode1C`: merged hex → survivor, skip у products). **Прогін user:** migrate deploy+generate → dry-run (переглянути survivor-ів!) → apply. **Довга черга (план 7.0):** Блок 1 замовлення з сайту→система (питання: дефолтний менеджер, бронь лотів, статус чернетки) → Блок 2 клієнти → Блок 3 товари/категорії/фото в системі → Блок 4 дашборд сайту. **Хвіст:** apply чистки Задачі B (user ще не підтвердив фінальні цифри).

**⚡ Сесія 5.7 (2026-06-18, доробка довідників за фідбеком user, `main`@`95d5e964`):** План `docs/SESSION_5.7_DICTIONARIES_PLAN.md` (3 аналіз-агенти). **Деплой 5.6 на проді ✅** (8 фаз + догін; фікс install: `deploy.ps1` тепер `--prod=false`, `prepare`=`husky||true` — повний реінстал під NODE_ENV=production пропускав devDeps). **Обгортка** `scripts/import-1c.ps1` (сама вантажить `.env`+`IMPORT_TARGET_DB_URL`). **Залито на проді:** `cost-reg` (77674), `dictionaries-full` (16472: units 3810/regions 3163/cities 8071/trade-agents 46 — усі зматчені з User/viber 1382). **Зроблено 5.7:** (1) **клієнт→менеджер** — імпортер заповнює `MgrClient.agentUserId` з `_Fld6889RRef`; (2) **агент↔User** — авто-лінк `MgrTradeAgent.userId` при ручному створенні User + показ у картках (канон: User=агент, MgrTradeAgent=дзеркало); (3) **Типи цін** — read-only сторінка `/manager/price-types` (довідкова характеристика клієнта, НЕ впливає на ціни — ті з `Price.priceType` опт/акція), `partial→ready`; (4-5) **категорії з 1С** — `Category.code1C`+`hiddenForRoles` (міграція `20260618_category_code1c`), 2-фазний `--entity categories` (окреме 1С-дерево груп), `--entity products --recategorize` (переносить ЛИШЕ осиротілих з `imported-1c`), фільтр по піддереву + повний шлях у картці + каркас доступів за групами (deny-list, server-side, поки no-op); (6) **статуси клієнтів** — порт 1С ScheduledJob: `scripts/seed-client-statuses.ts` (7 статусів) + `recompute-client-statuses.ts` (≥2 продажі/міс→Активний, =1→Малоактивний, =0→Неактивний, нові→Потенційний; «Статус»=попередній міс, «Оперативний»=поточний) + cron `/api/cron/recompute-client-statuses` + опер-статус read-only у картці; (7) **аудит не-1С** — `scripts/audit-non-1c.ts` (read-only цифри; чистка — після рішення user). **Рішення user:** окреме 1С-дерево категорій; recategorize лише «Імпортовано»; чистка поки лише аудит; статуси авто. **Деплой 5.7:** `git pull` → `pnpm install --frozen-lockfile --prod=false` → `prisma migrate deploy` (`20260618_category_code1c`) → `prisma generate` → `deploy.ps1 -SkipInstall`. **Реімпорти (обгорткою):** `--entity customers` (агент клієнта), `--entity categories` + `--entity products --recategorize` (дерево груп), seed `seed-client-statuses.ts` + Windows Task на cron статусів (раз/міс). **890 тестів зелені.**

**⚡ Сесія 5.5 (2026-06-16):** Робоча гілка **`claude/charming-ptolemy-40syy0`** (НЕ merged у main — orchestrator пушить у feature-гілку; мердж+деплой робить user). Стартувала від `main`@`174b9020` (увесь 5.4). Стратегія — **повна заміна 1С**. **Зроблено у 5.5:** Фаза 2 (ретро-агенти `c459d6bc`); **5.4.5 живий борг** (a/b/c: модель `MgrDebtMovement`+recompute, hooks проведення, корекція+вкладка «Рухи боргу» — `c517bb65`/`96fd0800`/`13759541`); **EUR головна валюта** (борг-баг ₴→€ `4ec3aeef`, документи EUR-головні `023691ae`); **звіт «Прострочені борги»** (помашинний борг з датами `3cd96b39` + FIFO-старіння `b1208921`); **списки**: Реалізації +сортування/фільтри/колонка «Торговий агент» `94c315bd`, Оплати +сортування/фільтри `edea36ac`, **контекстне меню ПКМ/long-press** у Замовлення/Реалізації/Оплати `589ffa97`. **Нова міграція 5.5:** `20260616_mgr_debt_movements`. **Знак боргу:** `DEBT_SIGN=1` ПІДТВЕРДЖЕНО (1С-звіт показав боржників +EUR). **Деплой 5.5:** `prisma migrate deploy` (`20260616`) → реімпорт `--entity orders,sales,debt` (orders/sales=ретро-агенти; debt=помашинні рухи, ~78к) → `deploy.ps1`. **Відкрите:** звіт-follow-up («Борг по наложці» зв'язка рух→COD-Sale + групування по папках контрагентів — TODO у `lib/reports/overdue-debts.ts`); зауваження user по блоках. Прямий контекст 5.5 — `docs/SESSION_5.5_PLAN.md`. **Реімпортер** `apps/store/scripts/import-1c-historical.ts`. **1С НЕ зачеплено**.

**⚡ Сесія 5.4 (2026-06-16):** Гілка `claude/modest-franklin-qvo4kk`, усе merged у `main` (до `1d82ab39`/`174b9020`). **Міграції на проді:** `20260603`…`20260615`. Деталі — у журналі сесій нижче (ORCHESTRATOR 5.2→5.4). `--entity dictionaries|debt|orders|sales|cashorders|routesheets|customers|lots|prices`.

**Branch:** `main` (S1-34 store + M1.x менеджер + блоки Прайс/Замовлення/Реалізація/Оплати/Маршрутний лист/**Нагадування** + переробка картки клієнта Ф1-4 + чат-inbox Ф1a+1b+Ф2 + **1С інтеграція M3.1-M3.4** — усе merged). **LIVE на проді:** до commit `03dba86` (deploy 2026-05-26, міграції до `20260529`). **⚠️ PENDING DEPLOY** (main `d5e9af6`+, ТРИ НЕ застосовані міграції: `20260530_mgr_client_keywords` + `20260531_chat_inbox` + `20260601_phase2_chat_registration` + `20260602_mgr_sync_state`): user готує великий деплой Фаз 3-4 картки клієнта + чат-inbox Ф1-Ф2 + 1С інтеграції разом. Робочі feature-гілки: `claude/charming-cerf-4067W` (блоки/чат), `claude/awesome-wright-fojWb` (1С orchestrator).

**L-TEX website is LIVE on self-hosted Windows Server:** https://new.ltex.com.ua (Cloudflare Tunnel)

- **Live production:** `https://new.ltex.com.ua` via Cloudflare Tunnel `ltex-prod` (UUID `1b604cd0-1beb-4b0a-897f-93d67e58357f`), Windows Server 2022 (i5-9600K, 32GB, 107 Mbit symmetric)
- **DNS:** `ltex.com.ua` на Cloudflare NS (`fiona` + `trey`). Hostiq hosting email/cPanel (non-web records як DNS-only).
- **Netlify** `stalwart-dango-04a9b9.netlify.app` deprecated але живий (fallback).
- **DB:** local PostgreSQL 16 на `E:\PostgreSQL\16` — primary (805 products, 725 lots, 49 categories). Next.js читає звідти у runtime (`apps/store/.env` → `localhost:5432`). Supabase PostgreSQL (Frankfurt) — cold backup mirror, **не active** (оновлюється тільки при exporting changes; migration `20260428_notifications` apply-ється на ньому тільки якщо активуємо Netlify fallback). Supabase Auth (admin login) + Supabase Storage (banners, product images, відео) **лишаються активні**.
- **Auto-start:** cloudflared + PostgreSQL як Windows services; PM2 через Scheduled Task "PM2 Resurrect" (60s delay).
- **Monitoring:** UptimeRobot Free, 3 monitors (`/`, `/catalog`, `/admin/login`), 5-min interval.
- **Backups:** Daily `pg_dump -Fc` at 03:00 → `E:\ltex-backups\` (14-day retention).
- **CI green:** 243 unit + 36 E2E tests, TypeScript strict, 0 `any`.
- **Security:** Sessions 16-17 закрили 4 CRITICAL + 3 HIGH вразливості перед self-hosted deploy.
- **Mobile client (Expo SDK 52):** working — Home з banners + 3 product rails (S34), 4-tab nav, Catalog 2-column grid + bottom-sheet filter (S38), wishlist persistence + saved-products screen (S39), chat unread badge на MoreTab + MoreScreen (S35), notifications screen з deep links + mark-read (S36), points to `https://new.ltex.com.ua/api`. QuickView — pending. **Native APK не distributed** — користувачі поки скачують лише PWA з веб-сайту.

### Session log (recent)

- **2026-06-15 — ORCHESTRATOR 5.4 (повна добудова менеджерки + перенос усіх 1С-даних, велика сесія).** Гілка `claude/modest-franklin-qvo4kk`, усе merged у `main` (до `3ea8fe10`). Стратегія: **повна заміна 1С власною системою** (POST_1C), історію з 1С MSSQL уже залито (5.2), паритет звірено (5.3). Ця сесія — реалізація черги паритету + перенос довідників/реєстрів/менеджерів + нові блоки. **Робочий процес:** orchestrator дає спеки воркерам (Agent), перевіряє (typecheck/тести/prettier), мерджить у main, диктує деплой user-у. Усі менеджерські поля/документи рахуються у нашій PostgreSQL; 1С лишається лише джерелом історичного імпорту (`apps/store/scripts/import-1c-historical.ts`, idempotent upsert по `code1C`=hex(\_IDRRef)).

  **Міграції цієї сесії (усі additive, idempotent):** `20260610_parity_5_4_0_fields`, `20260611_route_sheet_expenses`, `20260612_doc_agent_name`, `20260613_role_expeditor`, `20260614_order_number_1c`, `20260615_doc_number_1c`. **Деплой:** `git pull` → `pnpm install` → `prisma migrate deploy` → `prisma generate` → `.\scripts\deploy.ps1 -SkipInstall` → `pm2 save`. **Реімпортер entity (нові):** `dictionaries`, `debt` (+наявні customers/products/lots/barcodes/prices/orders/sales/cashorders/routesheets).

  **5.4.0 — additive поля паритету + імпортер:** `MgrClient` +email/legalType/inn/edrpou/fullName/comment/additionalDescription/workingHours/parentCode1C; `Lot` +onAir/onAirDelivery (Ефір); `Sale` +orderId (FK→Order, з поліморфного `_Document189._Fld3490_RRRef`); `OrderItem` +unitPriceEur/discountPercent. Декодовані 1С-колонки — у `docs/SESSION_5.4_PLAN.md`. ⚠️ **Критично:** `Lot.update` був `{}` (адитивно) — onAir додано у update щоб backfill існуючі лоти; дати документів оновлюються лише через гілку `update` upsert (усі історичні документи вже існують).

  **5.4.1 — Картка клієнта:** вкладка «Історія продаж» (реальні Sale за code1C, заглушку замінено) + показ/редагування нових полів у «Реквізити» + foreign-masking (email/comment/additionalDescription приховані чужим). `legalType` (ЮрФизЛицо) — enum `_Enum377` (0=Юридична, 1=Фізична), заповнюється імпортером.

  **5.4.2 — Реалізація↔Замовлення:** на картці реалізації — «Замовлення-підстава» (лінк); на замовленні — колонка **«Продано»** (Σ SaleItem по реалізаціях цього замовлення, зелена підсвітка коли продано≥замовлено); блок **«Закриття»** (`/manager/closures`) переведено з мертвого 1С-SOAP на **локальні дані** (незакриті замовлення + продано-vs-замовлено, read-only; закриття на сторінці замовлення).

  **5.4.3 — Маршрутний лист:** дії при переході у dispatched/completed (у транзакції) — зняття броні з завантажених лотів (`lib/manager/route-sheet-actions.ts`: reserved\*=null, status='sold', крім archived) + замовлення маршруту → `isActual=false`. Вкладка **«Витрати»** (модель `RouteSheetExpense`, 1С VT7334).

  **5.4.4 — Друк-підсистема:** `/manager/sales/[id]/print` (Видаткова накладна) + `/manager/orders/[id]/print` (Рахунок), A4 + `@media print` за патерном receivings/print. **`lib/constants/company.ts`** `COMPANY_REQUISITES` = ФОП КУЗЕНКО ТАРАС СТЕПАНОВИЧ, ЄДРПОУ 3351808816, IBAN UA603052990000026003010807538, АТ КБ «ПРИВАТБАНК» (з `sale-message.ts`). + сторінка перегляду ПКО **`/manager/payments/[id]`** (раніше неможливо було «зайти в касовий ордер»); рядок Оплат тепер клікабельний. + `MgrCashOrder.documentSumEur` заповнюється імпортом (= (грн+безнал)÷курс) — Оплати показували 0€.

  **5.4.6 — довідники/реєстри/борг** (карта: `docs/REGISTRIES_IMPORT_MAP.md`). Нова `--entity dictionaries`: статті руху коштів (`_Reference96`→MgrCashFlowArticle), банк-рахунки (`_Reference29`→MgrBankAccount, +IBAN `_Fld5869`/прапор приховання `_Fld7710`), маршрути (`_Reference7513`→MgrRoute), **тип цін** (`_Reference105`→MgrPriceType, code=\_Code — інакше дропдаун порожній), курси валют (`_InfoRg4655`→ExchangeRate, EUR/USD через `_Reference30`; ⚠️ незалежний регістр — БЕЗ `_RecorderRRef`/`_LineNo`). Wiring у документи: agentName (ТорговийАгент `_Fld6886/6887RRef`) на Order/Sale; каса → cashFlowArticleId(`_Fld3282/3422RRef`)/bankAccountId(`_Fld3283/3423RRef`); **перерахунок ₴ за історичним курсом дати** (`eurRateForDate`, fallback курс документа→43); одиниці (`_Reference52`) — мапа є, але `OrderItem`/`SaleItem` не мають поля unit (не пишемо). **Борг:** `--entity debt` агрегує `_AccumRg5269` ВзаиморасчетыСКонтрагентами (СуммаУпр `_Fld5275` по Контрагент `_Fld5273RRef`, приход−расход) → `MgrClient.debt` (match по uid1C=hex). `DEBT_SIGN=1` (плюс=клієнт винен нам). ⚠️ **TODO user:** звірити знак боргу на 1-2 покупцях; якщо інверсія — `DEBT_SIGN=-1` + реімпорт debt. (Прогін: 78328 рухів → 2858 клієнтів.)

  **5.4.7 — перенос менеджерів:** нова роль **`expeditor`** (Експедитор; матриця прав route-focused). `scripts/seed-managers-from-1c.ts` створив 13 активних (зматчені з 1С-агентами `_Reference6628` → `User.code1C`=hex) + 32 архівних (isActual=false) + долінкував Кузенка. Адмінка: модалка **«Редагувати»** (email + ПІБ + новий пароль) — щоб докредитити імпортованих. Ролі активних: Бойко/Гуменюк/Дунас/Захарчук/Савчук=manager, Володимир=owner, Експедитори×3=expeditor, Офіс Галина=analyst, Склад×3=warehouse.

  **Блок «Потреби» (`/manager/needs`)** — порт 1С `ОбщаяПотребність` (план `docs/NEEDS_BLOCK_PLAN.md`). On-the-fly зведення по **актуальних** замовленнях, 3 вкладки (Товари/Замовлення/Торгові агенти): **Потрібно = Замовлено − вільні лоти** (кг для вагових, шт/пар для штучних), CSV-експорт. `lib/manager/needs.ts`. **Статус Актуальне/Неактуальне:** правило «одне актуальне на клієнта» — app-level (НЕ DB-constraint): POST `/orders` → 409 `active_order_exists` → діалог «Відкрити існуюче / Закрити старі / [admin/owner/senior] Все одно (force→знімає isActual зі старих)». Закриття/архів ⇒ `isActual=false`.

  **1С-номери замовлень:** `Order.number1C` (= 1С `_Number`, напр. `L0000002477`) + `lib/manager/order-number.ts::formatOrderNumber` — показ замість hex скрізь + пошук по номеру. (Реалізації/каса/маршрути поки лишаються hex — TODO той самий патерн.)

  **Аналітик заводить замовлення вручну:** `getMyClientCodes1C` повертає null для `analyst` (усі клієнти) + права orders create/edit; кнопка «+ Створити замовлення» на `/manager/needs`. (Аналітик формує потреби вручну.)

  **Список замовлень — overhaul:** колонка **«Агент»**, дефолт показ лише **Актуальні** (`isActual=true`) + фільтр Актуальність (Актуальні/Неактуальні/Усі), **сортування по всіх DB-колонках** (`SortableHeader` + `buildOrderBy`; Область — ні, бо batch-lookup), згортний рядок **фільтрів** (Клієнт/Місто/Агент contains + наявні), **редагований статус Актуальне/Неактуальне в картці** (updateOrderSchema +isActual, PATCH-гард проти актуалізації закритих/архівних).

  **Кнопки замість галочки 1С (зроблено):** прибрано чекбокс «Вивантажувати в 1С» (`exportTo1C`) з форм Замовлення+Реалізація → дві кнопки **«Зберегти»** (чернетка) + **«Зберегти та провести»** (`post`-прапор → `status='posted'`+`archived=true`; транзиції draft/sent→posted, cancelled→posted заборонено). `exportTo1C` лишено в БД (без міграції), прибрано лише з UI/карток. Семантика: проведене → архів (з активного списку зникає; `archived ⇒ isActual=false`).

  **1С-номери на ВСІХ документах (зроблено):** `number1C` (= 1С `_Number`) тепер і на Sale/MgrCashOrder/RouteSheet (міграція `20260615_doc_number_1c`) — окрім Order (`20260614`). Спільний `lib/manager/order-number.ts::formatDocNumber` (number1C ?? №docNumber ?? hex) у списках/картках/друку + пошук. `code1C`=hex лишається унікальним ключем.

  **⚠️ Pending на проді (user деплоїть):** деплой `deploy.ps1` + `prisma migrate deploy` (`20260614`+`20260615`) + `prisma generate` + реімпорт `orders` (number1C) та `sales,cashorders,routesheets` (number1C) + `dictionaries` (тип цін) — частина могла бути ще не виконана; звірити що 1С-номери показуються.

  **Наступна сесія — відкрите:** ⏭️ **5.4.5** борг/каса як живий регістр (перерахунок боргу при проведенні документів + мульти-валютна каса + документ корекції боргу; повний debt-імпорт з `_AccumRg…` уже зроблено у 5.4.6 → `MgrClient.debt`); ⏭️ **звірка знаку боргу** `DEBT_SIGN` на 1-2 покупцях (якщо інверсія → `-1` + реімпорт `--entity debt`); ⏭️ **Фаза 2** ретро-прив'язка історичних замовлень/реалізацій до менеджерів (`Order.assignedAgentUserId`/`Sale.assignedAgentUserId` через agent-hex → `User.code1C`; усі 13 активних менеджерів мають `code1C`=1С-агент); ⏭️ далі — зауваження user по блоках (велика сесія правок).

- **2026-06-11 — ORCHESTRATOR 5.3 (паритет з 1С: Фаза A фікси + Фаза B аудит).** Гілка `claude/modest-franklin-qvo4kk`, merged у `main` (`336a7873`). Робота над імпортером історії `apps/store/scripts/import-1c-historical.ts` (з 5.2) + аудит паритетності всіх 6 менеджерських блоків. **Фаза A — 4 фікси імпорту (підтверджено на проді):** (A1) **дати** — 1С на MSSQL зберігає дати зі зсувом року (таблиця `_YearOffset` = +2000); `loadYearOffset()` читає зсув при старті, `asDate()` віднімає його; **критична доправка** — `createdAt`/дату додано у гілку `update` upsert-ів order/sale (усі 34k документів уже існували з 5.2 → upsert ішов через `update`, який дату не оновлював → лишалось дефолтне `now()`=10.06.2026). (A2) **суми** — список показує `totalUah` головним, а шапкове поле суми порожнє; тепер `totalEur` = сума рядків (fallback на шапку), `totalUah` = `totalEur`×курс (fallback 43). (A3) **маршрутні VT** — декодовано 10 дочірніх табличних частин `_Document6630` (`HISTORY_MIGRATION_MAP §10.2`); імпортуються Заказы(VT6648)→RouteSheetOrder, ТоварыЗаказов(VT6654)→RouteSheetItem, ЗагрузкаМашины(VT6795)→RouteSheetLoading, Завдання(VT7622)→RouteSheetTask; вкладки Реалізації/Продажі/Оплати — через **зворотні посилання** (`Sale.routeSheetId` з `_Document189._Fld6729RRef`, `MgrCashOrder.routeSheetId` з VT6787) — child-таблиці RouteSheetSale/SaleItem/Payment додаток свідомо ігнорує. (A4) **документи на вилученні** (`_Marked`=ПометкаУдаления) — пропускаються; вже імпортовані такі видаляються (каскад) при реімпорті. Прогон на проді: cashorders 24901 written / 5991 skipped (на-вилученні), routesheets 999/1, barcodes mapped 94631. **Семантика:** проведені в 1С = `archived=true` → приховані за дефолтом у `/manager/orders` (галочка «Відображати архівні»); рішення user — лишити «активний» список чистим (без змін). **UX:** список Замовлень отримав **сортування по колонках** (`sortable-header.tsx` + `buildOrderBy`) + покращену пагінацію (« Перша / Остання » + перехід на номер сторінки у спільному `ListPagination` — також для Клієнтів). **Фаза B — аудит паритетності:** 6 research-агентів (паралельно) звірили кожен блок проти 1С-форм + `CommonModules/ОбменАРМ/Ext/Module.bsl` → `docs/PARITY_AUDIT_{orders,sales,cash,routes,client,prices}.md` + зведення `docs/PARITY_SUMMARY.md` (наскрізні теми: друк документів скрізь відсутній; зв'язка Sale↔Order→«Продано»+закриття замовлень; борг як живий регістр; історичний курс). **Рішення user:** 5.4 стартує з **5.4.0 — additive міграції + розширення імпортера** (поля яких бракує проти 1С: MgrClient email/ЮрФіз/ИНН/ЕДРПОУ/коментар/ієрархія, Lot Ефір, Sale.orderId, OrderItem ціна/знижка — ДО нового дозбору з 1С). Повний план: `docs/SESSION_5.4_PLAN.md`. **Відкритий дрібний follow-up:** `MgrCashOrder.documentSumEur` не заповнюється імпортом → вкладка Оплати показує «0 €» (чекає на підтвердження валюти ПКО від user). **Деплой:** усе офлайн-скрипт + docs (веб-деплой був лише для UX-сортування списку). Реімпорт на сервері: `git pull origin main` + `--entity orders,sales,cashorders,routesheets` по черзі (idempotent upsert по `code1C`=hex).

- **2026-06-09 — ORCHESTRATOR 5.1 ЗАВЕРШЕНО (повний цикл переходу від 1С до власної системи).** Гілка `claude/sweet-pascal-OHSke`, фінальний commit `3d2b4cf9`. **Що зроблено цієї сесії (15 commits, 7 міграцій, ~2012 тестів усі зелені):**

  **Інфраструктура ролей (Тижні 1-5):**
  - **Тиждень 1** (commit `0e5e9023`, міграція `20260603_extended_roles_audit_log`): Permissions Matrix (8 ролей × 22 ресурси × 5 дій у `lib/permissions/role-permissions.ts`); Audit Log (модель `AuditLog` append-only + helper `logAuditEvent` fire-and-forget, owner-дії з `isOwnerAction=true`); сторінки `/manager/admin/audit` (фільтри, ownerOnly) + `/manager/admin/permissions` (read-only візуалізація); `scripts/promote-to-owner.ts` (kuzenko.t.k@gmail.com піднято до owner). 8 ролей: `admin/owner/manager/senior_manager/supervisor/analyst/warehouse/bookkeeper`.
  - **Тиждень 2** (commit `b74e5705`, міграція `20260604_warehouse_receiving`): Документ Поступлення (заміна 1С `ПоступленняТоварівУслуг`). Моделі `Supplier`+`Warehouse`+`Receiving`+`ReceivingItem`; `Lot.supplierId/receivingId/purchasePriceEur`; barcode-generator `L-{articleCode}-{seq:05}`; cabinet `/manager/receivings/*` (CRUD + проведення → транзакційно створює лоти + апдейтить прайс).
  - **Тиждень 3** (commit `49e8d4ee`): Owner-дашборд у `_components/owner-dashboard.tsx` (KPI Виручка/Маржа/Борги/Активні_клієнти + 12-міс графік inline SVG без deps + Топ-10 клієнтів). `lib/finance/owner-stats.ts` з `resolvePeriod` (today/week/month/year/all).
  - **Тиждень 4** (commit `114a49a1`): Bookkeeper-дашборд (каса/борги/курси/останні реалізації) + Supervisor-дашборд (рейтинг менеджерів за виручкою з прогрес-барами).
  - **Тиждень 5** (commit `616169e3`): Analyst-кабінет + 3 звіти (`/manager/reports/sales-by-client|sales-by-supplier|debts`) з CSV-експортом (`/api/v1/manager/reports/[id]/csv`, UTF-8 BOM + `;` для Excel UA-локалі). Helper `lib/reports/csv-export.ts` + `analyst-reports.ts`.

  **Правки документа Поступлення (5 хвиль, відповідно зворотньому зв'язку user-а):**
  - **Початкові правки** (commit `cb7d6385`): Компактна 1С-таблиця, прибрано Валюту/Курс/№НакладноїПостачальника, quantity завжди=1, ціна закупки прихована для warehouse, авто-фокус Вага→ШК, smart-сканер заглушка, генератор ШК через окремий endpoint, друк етикетки Code 39 SVG (`lib/warehouse/barcode-svg.ts`), warehouse зберігає тільки draft (admin/owner проводить).
  - **Хвиля 1** (commit `1eb6ae93`, міграція `20260605_receiving_item_sector`): Smart-сканер постачальника — `parseBarcode` розпізнає паттерн `XYYYYYZTTTUUU...` (позиції 2-6=артикул 5 цифр, позиції 9-11=вага×10). Перевірено 6 прикладів user-а: 37047/18.0, 37047/18.2, 64092/15.2, 64092/16.5, 31002/28.8, 31002/29.2. `lookup` endpoint шукає товар за артикулом → auto-додавання рядка. Клавіатура: ↑↓ + Enter у пошуку (виділяє/обирає); Enter Вага→ШК, ШК→пошук, Ціна→Ціна*наступного*рядка. Per-row 🎯 Generate / 🖨 Print label (`/receivings/preview-label`) / 📋 Copy. Колонка Сектор (вільний ввід).
  - **Хвиля 2** (commit `f06a6da8`, міграція `20260606_purchase_prices_sectors`): Модель `PurchasePrice` (історія цін з FK на product+supplier+receivings) + `WarehouseSector` (довідник). Endpoint `/last-purchase-price?productId=X&supplierId=Y` з fallback на будь-якого постачальника. При додаванні товару — авто-підстановка останньої ціни. Сектор як autocomplete через `<datalist>`. localStorage авто-збереження `ltex:receiving-draft:{id|new}` (debounce 2с) + restore-prompt при відкритті. Друк-сторінка накладної `/receivings/[id]/print` (A4, як 1С форма) + `lib/warehouse/barcode-svg.ts` Code 39 етикетка 60×40мм.
  - **Edit + admin notification** (commit `bed394a6`, міграція `20260607_receiving_review_reminder`): Сторінка `/receivings/[id]/edit` (той самий `ReceivingForm` з prop `initial: ReceivingFormInitial` → PATCH замість POST). LocalStorage key per-id. Enum `MgrReminderSource += 'auto_receiving_review'` + `mgr_reminders.receiving_id`. При warehouse-збереженні draft → `createReceivingReviewReminders` для всіх admin/owner (body `Перевірити поступлення LT-RCV-... (склад: ...)`). При post/cancel → `completeReceivingReviewReminders`. Якщо створив сам admin/owner — нагадувань НЕ створюємо.
  - **Sale price + Розпровести** (commit `52b204a2`, міграція `20260608_sale_price_field`): `receiving_items.sale_price Decimal?`. Endpoint `/last-sale-price?productId=X` (admin/owner only, з `Price` priceType='wholesale'). При проведенні salePrice>0 → закриваємо попередню активну Price (`validTo=docDate`) + створюємо нову → одразу у Прайсі. Endpoint `/receivings/[id]/reopen` (admin/owner) — транзакційно видаляє лоти+PurchasePrice + скидає `createdLotId` + status→draft. Кнопка «↩ Розпровести» на сторінці деталей. Після reopen — автоматичний redirect на `/edit`.

  **Пріоритет 1 — Блок Замовлення Етапи 3-4** (commit `3d2b4cf9`, міграція `20260609_order_closure_optimistic_lock`):
  - **Етап 3 закриття:** Order поля `closedAt/closeReasonId/closedByUserId/closeNotes`; модель `OrderCloseReason` (seed 5 причин: `client_refused/no_stock/sold_out/long_time/other`); endpoint `POST /orders/[id]/close { reasonId, notes? }` (ownership через `getMyClientCodes1C`); `GET /orders/close-reasons`; `OrderCloseButton` модалка; підсвітка червоним для closed з показом причини/автора/часу.
  - **Етап 3 авто-нагадування** (детектор C у `lib/manager/generate-reminders.ts::detectOverdueOrders`): пошта/самовивіз → нагадування **кожні 3 дні**; доставка → **кожні 7 днів**; вік >90 днів → ескалація на supervisor/senior_manager. `lastReminderAt`/`remindersSentCount`/`escalatedToSupervisorAt` оновлюються. Cron повертає 4 лічильники: `bronCreated/videoFired/orderRemindersCreated/ordersEscalatedToSupervisor` (8 тестів `route.test.ts` оновлено під нову shape).
  - **Етап 4 optimistic lock:** `Order.version Int @default(0)`; `updateOrderSchema.version` optional (backward-compat); PATCH перевіряє version → 409 з `code:"version_conflict", currentVersion`; `updateOrderWithItems` робить `version: { increment: 1 }`.

  **Інші правки картки замовлення:**
  - Кнопка «❌ Закрити» + модалка (causes dropdown, notes textarea)
  - Закрите → редагування заблоковано (`editable = canEditOrder && !closedAt`)
  - У header `OrderCloseButton` поряд з StatusBadge

  **1С експорт у git:** push повного дампу `docs/1c-export-2026-06-02/` (7914 файлів / 346 MB → 75 MB compressed, 14211 git objects). Містить XML metadata + BSL модулі для всієї LTEX SECOND PROD конфігурації. Ключові посилання: `Documents/ЗаказПокупателя/Ext/ObjectModule.bsl` (3160 рядків), `Documents/ПоступлениеТоваровУслуг/Ext/ObjectModule.bsl` (4386 рядків), `Reports/`, `DataProcessors/`, `Catalogs/`, `AccumulationRegisters/`.

  **Стан DB на 2026-06-09:** 7 нових міграцій pending для prod: `20260603/04/05/06/07/08/09`. PROD LTEX 1С НЕ зачеплено. На сервері (Windows): `git pull` + `pnpm install --frozen-lockfile` + `prisma migrate deploy` + `.\scripts\deploy.ps1` (з pm2 kill prelude).

  **Що НЕ зроблено цієї сесії (свідомо, наступні пріоритети):**
  - **Пріоритет 2** — імпорт історії з 1С PostgreSQL (≥2021р., ~50тис замовлень, ~50тис реалізацій, ~3-5тис клієнтів, ~10-50тис лотів, ~50-100тис ПКО/РКО, ~3-5тис маршрутних, timeline клієнта). User має створити read-only access: `CREATE USER ltex_app_reader WITH PASSWORD ...; GRANT SELECT ON ALL TABLES IN SCHEMA public...` на 1С PostgreSQL (E:\PostgreSQL\16, база ltex_prod), потім додати у `apps/store/.env` `LEGACY_1C_DB_URL=postgres://ltex_app_reader:<...>@localhost:5432/ltex_prod`.
  - **Пріоритет 3** — Перепаковка / Інвентаризація / Списання + звіти (Стан мішків / Рух штрихкодів / Аналіз нагадувань).
  - **Пріоритет 4** — real autosave через PATCH; CRUD-сторінки Постачальники/Склади/Сектори; Telegram-бот warehouse; XLSX замість CSV.
  - **Пріоритет 5** — real 1С SOAP wire-up; регістр цін клієнтських; ПДВ-облік.

  **Тести в кінці сесії:** 2012 store passed / 2 skipped (197 test files); typecheck чистий; prettier чистий. Гілка `claude/sweet-pascal-OHSke` синхронізована з origin.

  **Наступна сесія — ORCHESTRATOR 5.2 — старт Пріоритету 2 (імпорт історичних даних з 1С).** Робочий процес — без змін, orchestrator-only: план/перевірка/мердж + диктує user-у команди на сервер. Цьогораз ВЕЛИКА робота — мапування 1С MSSQL таблиць (`_Reference77_…`/`_Document108_…`/`_AccumRg…`) на нашу модель + написання idempotent скрипта-маппера + тестовий прогон на копії БД + фінальний імпорт за 1 ніч.

  **⚠️ ВАЖЛИВЕ ВІДКРИТТЯ 2026-06-08:** 1С у L-TEX **НЕ на PostgreSQL, а на MS SQL Server 2019 Enterprise** (Windows Server 2022). Назва бази у 1С — **`ltex`** (НЕ `ltex_prod`). PostgreSQL `E:\PostgreSQL\16` — це окрема наша Next.js база. Це міняє інструменти імпорту: npm пакет `mssql` замість `pg`, T-SQL замість PL/pgSQL, connection string `mssql://`.

  **Що user ВЖЕ ЗРОБИВ перед стартом сесії 5.2** (2026-06-08, разом з orchestrator-ом):
  - Створено SQL Login `ltex_app_reader` на MSSQL з паролем (CHECK_POLICY = OFF) + database user у базі `ltex` + роль `db_datareader` (read-only на ВСІ таблиці) + `VIEW DEFINITION` для читання метаданих
  - Додано `srv\тарас` (Windows-користувача) як sysadmin на MSSQL — через **single-user mode recovery** (Stop-Service MSSQLSERVER → старт з `-mSQLCMD` → `CREATE LOGIN [srv\тарас] FROM WINDOWS` + `ALTER SERVER ROLE sysadmin ADD MEMBER` → перезапуск нормально). Mixed Mode (LoginMode=2) уже був.
  - Додано у `E:\ltex-ecosystem\apps\store\.env`: `LEGACY_1C_DB_URL=mssql://ltex_app_reader:<пароль>@localhost:1433/ltex`
  - **Перевірено:** `sqlcmd -S localhost -U ltex_app_reader -P <pass> -d ltex -Q "SELECT TOP 5 name FROM sys.tables"` повертає 1С таблиці (`_AccRg…`, `_AccumRg…` тощо)

  **Безпекові TODO для user-а (після успішного імпорту):**
  - **Ротувати sa-пароль на MSSQL** (зашифрований у `C:\Program Files (x86)\1cv8\srvinfo\reg_1541\1CV8Clst.lst`, у чат випадково запостили base64-вигляд `odSLA9QE...` — теоретично можна decrypt-нути)
  - Видалити `C:\temp\clst_active.txt` (копія `1CV8Clst.lst` з sa-паролем у зашифрованому вигляді)
  - Опціонально: видалити `ltex_app_reader` логін коли імпорт завершиться

  **Стратегія сесії 5.2:**
  1. Explore-агент аналізує структуру 1С MSSQL (`SELECT name FROM sys.tables`, `INFORMATION_SCHEMA.COLUMNS`, `sp_help <table>` для ключових таблиць замовлення/реалізації/контрагенти/номенклатура), декодує системну таблицю `_Config`/`Params` для мапування `_Reference77` → реальна назва метаданих (`Контрагенты` тощо), створює `docs/HISTORY_MIGRATION_MAP.md` з повним полем-у-поле мапінгом 1С MSSQL → наша Prisma-схема (Customer, Order, Sale, MgrCashOrder, Payment, RouteSheet, Lot, Product, тощо).
  2. User читає, корегує.
  3. Скрипт-маппер `apps/store/scripts/import-1c-historical.ts` (Node+`mssql` package+Prisma, idempotent: `Order.code1C` як унікальний ключ — повторне виконання НЕ дублює).
  4. Тестовий прогон на тест-БД (`ltex_test` або копія `pg_dump|pg_restore`); звірки кількостей з 1С звітами.
  5. Фінальний імпорт у production за 1 нічну сесію (зупинка 1С на ~2 години).
  6. Місяць паралельного режиму (1С тримаємо у режимі довідки).

- **2026-06-01 — РІШЕННЯ: завершуємо 1С інтеграцію як PoC, переходимо на повну заміну 1С (POST_1C):** ORCHESTRATOR 5.0 фінал. **Що працює:** прямий SOAP smoke-test через manager-sync proxy повертає `{ok:true, code1C:"...", error:null}` — інтеграція технічно функціональна. Створення клієнтів через `/manager/customers/new` UI working (commit `be6c57f` + `3790800`); 7 правок форми за вимогами user (commit `be6c57f`): прибрано Code1C поле (приходить з 1С), debounce phone duplicate check, нові 4 FK поля (Канал/Категорія/Асортимент/Відділення НП), manager auto-self-assign, admin selector агента + Textarea коментар → timeline entry + `MgrReminder` сповіщення обраному менеджеру, queue-processor backfill MgrClient.code1C з sync відповіді (commit `2d64c76`). Admin "Retry" кнопка для `retrying` jobs (commit `47a64e9`). **Чому зупиняємось:** 1С 8.2.13 + Molenari OU lockdown + duplicate code1C race condition (1С перевіряє unique у Записать, не в присвоєнні; транзакція стає "отруєною" при exception — caught retry не можливий у тому ж scope). Багато ітерацій BSL fix-ів (commits `145402a`, `4d2db64`, `ebfeae2`, `46b2302`) — кожна спроба зустрічається з новим Molenari edge case. **Real bottleneck — кожна нова операція (документ/продукт/оплата) буде новою BSL війною** + bus factor (тільки Claude і user знають BSL hacks) + 1С не підтримується standard web-розробниками. **Стан інфраструктури:** LTEX_TEST has working BSL Module.bsl + 8 EXPORT operations published; manager-sync у production mode (`SYNC_MOCK_MODE=false`, URL `localhost/ltex_test/...`); 3 retrying jobs з SMOKE-TES помилкою у `/admin/sync-jobs` — не critical, не блокують прод. PROD LTEX 1С не зачеплено. **План POST_1C** документований у `docs/POST_1C_STRATEGY.md` — 4-6 тижнів AI-driven розробки: Reception/Repacking/Checkbox ПРРО/Reports/3 customer-card tabs + повна міграція з 1С. **Гілка** orchestrator `claude/awesome-wright-fojWb`, main `46b2302` (12 commit-ів за сесію 5.0). **Наступна сесія ORCHESTRATOR 5.1:** старт `docs/POST_1C_STRATEGY.md` — блок Приймання товарів (першим у плані). Manager-sync лишається активним у production mode як bridge (нові клієнти створюються у 1С через нього), але новий функціонал НЕ йде через 1С.

- **2026-05-31 (вечір) — END-TO-END 1С ІНТЕГРАЦІЯ ЗАВЕРШЕНА на LTEX_TEST (М3.5 closure):** Повний кругообіг працює: Node manager-sync HTTP → Apache `wsap24.dll` з Basic auth (Тарас:praga123) → 1С web-engine викликає `ОбновитиКлиентаJSON` → Module.bsl створює Контрагента у LTEX_TEST → JSON-відповідь повертається. **E2E smoke-test:** `curl -X POST http://127.0.0.1:3001/sync/clients/test` з payload `{idempotencyKey, payload:{code1C, name}}` → `{"ok":true,"code1C":"LTEX-E2E-","errors":[]}`. **Що зроблено цього вечора:** (1) 8 XML-декларацій нових EXPORT-операцій додані у LTEX_TEST через GUI Конфігуратора (Web-сервіси → MobileExchange → копіювання робочої операції `ОбновитиКлиентаJSON` як шаблон, перейменування — параметри і return-type зберігаються); важливе відкриття — копіювати з `ВивантажитиТовари` НЕ можна (return type = ХранилищеЗначения, не xs:string → SOAP `<return/>` порожній); правильний шаблон — `ОбновитиКлиентаJSON` (return = xs:string). (2) Node-side Russification (commit `b3f4993`): 38 заміни у 7 файлах (`services/manager-sync/src/soap/{envelope,client,closures-client,pull-client}.ts` + `client.test.ts` + `apps/store/lib/sync/{enqueue,proxy-client}.ts`) — імена операцій (`ОбновитиКлиентаJSON`, `СтворитиРеализациюJSON`, `ОтриматиДаниЗакриттяЗамовленьJSON`, `ЗакритиСтариЗамовленняJSON`) + XML-параметр `<ms:JSONДани>` (з російською `и`). (3) SOAPAction fix (commit `b66afc3`): порожній `""` замість `"http://arm_mobile#MobileExchange:<op>"` — HTTP headers = ByteString (Latin-1), Cyrillic у operation name ламав fetch() ("character has a value of 1054 > 255"); 1С маршрутизує за element name у SOAP body, не за header — порожній SOAPAction працює (curl smoke-test підтвердив). (4) HTTP Basic auth (commit `7c91915`): 2 нові env vars `ONEC_HTTP_USER`/`ONEC_HTTP_PASSWORD` (окремо від `ONEC_SOAP_PASSWORD` — той = sync password у JSON-payload, цей = 1С користувач для Apache); helper `buildBasicAuthHeader` у envelope.ts; інжект `Authorization: Basic <base64>` у всі 9 SOAP-викликів (6 client + 2 closures + 1 pull); 14 файлів +64/-2. **Активація у production:** `services/manager-sync/.env` має `SYNC_MOCK_MODE=false` + `ONEC_SOAP_URL=http://localhost/ltex_test/ws/MobileExchange.1cws` + `ONEC_SOAP_PASSWORD=I6aJiZ2gQV1pYw0K8nfERNHXBjleDb3S` + `ONEC_HTTP_USER=Тарас` + `ONEC_HTTP_PASSWORD=praga123`; `pm2 restart ltex-manager-sync --update-env`; health = `{ok:true,mockMode:false,cacheSize:0}`. **Сюїти:** store 1929 pass/2 skip, manager-sync 77 pass, typecheck/prettier clean. **Залишилось для PROD LTEX (наступна сесія):** (1) повторити Module.bsl.full вставку + 8 XML декларацій на PROD базі LTEX; (2) `.env` ONEC_SOAP_URL з `/ltex_test/` на `/ltex/`; (3) тестування через UI (`/manager/clients/[id]` PATCH → enqueueClientUpdate → cron process-sync-queue → manager-sync → 1С → Контрагент у PROD LTEX); (4) ротація `praga123` (засвічений у чаті). **TODO field-alignment (свідомо відкладено):** 6 категорій розбіжностей Node payload vs BSL (Client `primaryAssortmentCode`, Order `status/totalUah/items[].productId/lotId`, Payment `currency/orderInternalId`, CashOrder `amountEur/amountUsd/amountUahCashless/debtCorrection`, Sale `priceTypeId UUID → priceTypeCode`, RouteSheet `payments[]`) — усі обернуті у BSL `Попытка...Исключение`, ігнор → безпечно для production. **Гілка** orchestrator `claude/awesome-wright-fojWb`, main `7c91915` (вечір ще 4 commit-и). PROD LTEX не зачеплено — інтеграція тільки на LTEX_TEST.

- **2026-05-31 (день) — BSL Module.bsl ПОВНІСТЮ РОЗГОРНУТО у LTEX_TEST, WSDL відповідає (М3.5 Етап 2 closure):** Розв'язано блокер ORCHESTRATOR 5.0 — Module.bsl скомпілювався і WSDL endpoint `http://localhost/ltex_test/ws/MobileExchange.1cws?wsdl` повертає валідний XML з усіма operations. SOAP-тест `НачатьОбмен` з fake-паролем повертає очікувану помилку з `ОбменАРМ.Module(7460): ПолучитьОбъект` — тобто web-сервіс ДОХОДИТЬ до старої бізнес-логіки Тараса і працює повноцінно. **Виправлено 4 категорії BSL-помилок:** (1) Russification 867 occurrences української `і`/`ї`/`є`/`І` → російські `и`/`и`/`е`/`И` (commit `d9ac790`) — 1C 8.2.13 з RU локаллю не приймає Ukrainian-only літери у identifier-ах; (2) перейменування `Знач`→`Вход` у 5 функціях (commit `cd7ca8d`) — `Знач` є reserved keyword "by value modifier" у BSL parameter declarations; (3) `СтрСравнить` → прямий `=` (commit `8853d81`) — функція з 1C 8.3.6+, недоступна у режимі 8.2.13; (4) `СтрСоединить` → власний helper `_LTEX_ОбединитиМассив` (commit `3fbaf62`) — теж 8.3.6+ функція. **Критичне відкриття:** Module.bsl.append треба було ДОДАВАТИ до оригінальних 163 рядків wrapper-функцій (НачатьОбмен, ВивантажитиТовари тощо з `docs/1c-export-mobile/Central/WebServices/MobileExchange/Ext/Module.bsl`), а user робив `Ctrl+A → Ctrl+V` що ЗАМІНЯЛО весь модуль → 24 оригінальні operations зникали → web-сервіс падав з "Операція не знайдена: НачатьОбмен()" на ВСІ запити (незалежно від body). Створено новий файл `docs/1c-bsl/outbound/Module.bsl.full` (1863 рядки = оригінал 163 + L-TEX extensions 1696, commit `32127b1`) — повна заміна модуля у одне Ctrl+V. **Підтверджено**: `?wsdl` повертає `<?xml ... <definitions xmlns="http://schemas.xmlsoap.org/wsdl/" ... xmlns:tns="http://arm_mobile">`, 69 occurrences `operation name=` (24 portType + 24 binding + 21 extras), POST `НачатьОбмен` доходить до `ОбменАРМ.Module:7460`. **Інфраструктура** (LTEX_TEST через сховище `E:\Storage_1C` user `dev`, Apache 2.4 на `localhost`, авторизація `Тарас:praga123`, 1С Server Agent 8.3.13.1690 на Windows Server 2022) — без змін, прод LTEX не зачеплений. **Залишилось для активації SYNC_MOCK_MODE=false (наступна сесія):** (1) додати 8 нових XML-декларацій `<Operation>` для EXPORT-функцій з RUSSIFIED іменами (`ОбновитиКлиентаJSON`, `СтворитиЗамовленняJSON` тощо) — поточний `docs/1c-bsl/outbound/MobileExchange.xml.diff` має старі Ukrainian-named назви, треба regenerate з новими; (2) синхронізувати `services/manager-sync/src/soap/{envelope,client}.ts` — operation names + XML-параметр `<ms:JSONДані>` → `<ms:JSONДани>` (і → и); (3) оновити тести (`client.test.ts`, fixtures); (4) виставити `.env`: `ONEC_SOAP_URL=http://localhost/ltex_test/ws/MobileExchange.1cws` + `ONEC_SOAP_PASSWORD=I6aJiZ2gQV1pYw0K8nfERNHXBjleDb3S` + `SYNC_MOCK_MODE=false` + `pm2 restart ltex-manager-sync --update-env`; (5) smoke-тест реального обміну з валідним паролем продавця. **Безпекові TODO**: `praga123` (Тарас 1C user) досі не ротований — рекомендовано робити після завершення інтеграції. **Гілка** orchestrator `claude/awesome-wright-fojWb`, main `32127b1` (5 commit-ів сьогодні). 1С прод LTEX не зачеплено — публікація і вставка модуля тільки у LTEX_TEST.

- **2026-05-29 — Molenari OU повний lockdown виявлено (BLOCKER, sync робота призупинена):** Після BSL rework (нижче) user спробував вставити функції у `WebService.MobileExchange.Module` через Конфігуратор LTEX_TEST. Виявилось що Molenari OU заблокував НЕ ТІЛЬКИ кореневі об'єкти, а й сам модуль existing MobileExchange — модуль відкривається «[Тільки для читання]», ПКМ/dbl-click у Налаштуваннях підтримки на правій колонці біля MobileExchange не дає виставити правило користувача (порожня клітинка не приймає кліків). Також новий WebService.LtexSync додати неможливо — ПКМ → Додати на «Web-сервіси» сіра. Тобто захист Molenari OU блокує: (1) додавання нових Constant/Catalog/CommonModule/ScheduledJob/WebService; (2) редагування existing MobileExchange.Module; (3) зміну режиму сумісності для Розширень; (4) повне зняття з підтримки. **Висновок: завершення Етапу 2 (BSL вставка) НЕМОЖЛИВЕ через цю конфігурацію без втручання постачальника.** **Стан інфраструктури не зачеплено:** LTEX_TEST свіжа копія prod (експорт+імпорт 19:35), MobileExchange.1cws опублікований через Apache 2.4 на http://localhost/ltex_test/ + 23 операції відповідають, manager-sync online у mock-mode, store live. **Залишки артефактів готові** у `docs/1c-bsl/outbound/` (Module.bsl.append 921 LOC + MobileExchange.xml.diff 8 операцій) — лежать готові до вставки коли user отримає доступ. **Варіанти для user-а:** (A) Звернутись до Molenari OU (`http://1c.ua/v8/RegionalSolution`) за зняттям обмежень WebService.MobileExchange — стандартна послуга, може бути платно. (B) Жити у mock-mode — менеджери дублюватимуть дані у 1С вручну. (C) Створити окрему 1С базу (~1 тиждень) для sync через 1С Plans Exchange. (D) Купити стандартну УТ від 1С Company — повна міграція з ризиком. **Рекомендовано user-у: варіант A** (контакт постачальника). **Безпекові TODO для user-а:** ротувати пароль `praga123` у LTEX/LTEX_TEST (засвічений у чаті); ротувати раніше згенерований 32-символьний sync-пароль (вже у Notepad, BSL не змонтований). **Облік сесії:** ПОВНИЙ робочий день — встановлення IIS + Apache 2.4 встановлений, експорт+імпорт .dt, публікація через 1С Конфігуратор, налаштування Apache, виявлення Molenari constraint, BSL rework, лютий attempt вставки — все задокументовано. Гілка orchestrator `claude/awesome-wright-fojWb`, main на `555acb2`. Cloudflared health-monitor `LTEX Cloudflared Health` встановлений у Windows Scheduled Tasks (5-хв auto-restart при 5xx).
- **2026-05-29 — BSL rework під Molenari OU constraint:** При спробі імплементації Етап 2 у LTEX*TEST виявилось що постачальник `Molenari OU` блокує додавання нових кореневих об'єктів (кнопка «Зняти з підтримки» сіра, режим сумісності `8.2.13` не дозволяє Розширення). Rework: видалені `СинкЛог.xml` / `СинкСистемнийПароль.xml` / `ЧисткаСинкЛогу.xml` / `СинкВхідний.bsl` (-3 кореневі об'єкти). Усі helpers (`\_LTEX*ПеревіритиПароль`/`*LTEX*ПарситиJSON`/`*LTEX*СерілізуватиJSON`/`*LTEX*Відповідь\*`/`*LTEX*РозібратиЗапит`) інлайн у `WebService.MobileExchange.Module` (`Module.bsl.append`921 LOC, було 1191). Idempotency повністю на Node стороні (MgrSyncJob`status='sent'`/`'failed'`не ретраяться у queue-processor.ts). Пароль читається з`services/manager-sync/.env::ONEC*SOAP_PASSWORD`і передається через JSON-поле`password`у`JSONДані`(зовнішній`<ms:ПарольВхода>`порожній); BSL`\_LTEX*ПеревіритиПароль`порівнює з hardcoded`LTEX_SYNC_PASSWORD_PLACEHOLDER`— user замінює вручну при вставці у Конфігуратор.`MobileExchange.xml.diff`лишився як був (8 операцій). Node-сторона:`buildJsonDataEnvelope(idempotencyKey, password, payload)`(новий 3-й arg) →`{idempotencyKey, password, data}`; `client.test.ts`оновлені 4 тести;`enqueue.ts`шапка з новим блоком про Molenari constraint. Тест-копія LTEX_TEST уже свіжа з PROD,`MobileExchange.1cws`опублікований через Apache 2.4, авторизація через Тарас:praga123 (пароль засвічений у чаті — рекомендована ротація). Workflow продовжується у наступній user сесії з оновленими BSL файлами. **Сьют:** manager-sync 77 pass, store 1911 pass; typecheck/prettier clean. **Гілка:**`claude/m3.5-molenari-rework-mol` (1 commit).
- **1С інтеграція M3.1-M3.4 — merged to main 2026-05-29 (НЕ ДЕПЛОЄНО; разом з картка-клієнт Ф3-4 + чат-inbox Ф1-Ф2 = 4 міграції на наступний deploy).** Фінальний великий етап — реальні обміни з 1С Central. Master-план: `docs/1C_INTEGRATION_PLAN.md`. Виконано через 4 паралельних worker-ів (M3.1-M3.4) + orchestrator (план/merge). Стан до старту: всі менеджерські блоки + Нагадування + чат-inbox Ф1-Ф2 — на main; sync infra (queue MgrSyncJob, enqueue payload builders, Fastify SOAP proxy, cron) — 100% готова з M1.5/Sale/Payments/RouteSheet, але mock-mode. **Ключове відкриття аудиту:** 1С Central база вже має опублікований web-сервіс `MobileExchange.1cws` з **24 методами** (`Central/WebServices/MobileExchange.xml` 1235 рядків + `.../Module.bsl` 163 рядки → wrappers; `Central/CommonModules/ОбменАРМ/Ext/Module.bsl` 7635 рядків — бізнес-логіка). Існуючі методи: `ОбновитьКлиента`, `СоздатьПКО`, `СоздатьРеализацииТоваровУслуг`, `СоздатьМаршрутныеЛистыТовары`, `ПолучитьДанныеНезакрытыхЗаказов_v1` + `ЗакрытьСтарыеЗаказы_v1` (Тарасова v1!), `СформироватьПакетДанных` тощо. Проблема: усі повертають `ХранилищеЗначения` (1С proprietary binary, Node не вміє). **Рішення:** тонкі JSON-обгортки на 1С-стороні (auth → парс JSON → виклик existing `ОбменАРМ.*` → серіалізація JSON → return) — значно простіше ніж було заплановано у `docs/1C_SYNC_MODULES_SPEC.md` §3.1-3.6 (там описувалось писати з нуля). **Етапи:** **M3.1 (Етап 1) — публікація 1С:** `docs/1C_WEBSERVICE_PUBLICATION.md` (243 LOC) — інструкція user-у крок-за-кроком (IIS feature flags → 1С Конфігуратор → Адміністрування → Публікація на веб-сервері → smoke-test через `curl ?wsdl` + PowerShell SOAP-call `НачатьОбмен`). 1С Server + Node на **тій же машині** (localhost, `new.ltex.com.ua`), є тест-копія Central — підтверджено user-ом. **M3.1 (Етап 2) — BSL OUTBOUND:** `docs/1c-bsl/outbound/` 7 файлів 1907 LOC: `Module.bsl.append` (908) — 6 нових Export-функцій (`ОбновитиКлієнтаJSON`, `СтворитиЗамовленняJSON`, `СтворитиОплатуJSON`, `СтворитиКасовийОрдерJSON`, `СтворитиРеалізаціюJSON`, `СтворитиМаршрутнийЛистJSON`) + Closures (+299, з M3.4); `СинкВхідний.bsl` (182+13) — CommonModule з 8 helpers (`ПеревіритиПароль`/`ПарситиJSON`/`СерілізуватиJSON`/`ВідповідьУспіх`/`ВідповідьПомилкою`/`ЗнайтиЗаКлючем`/`ЗареєструватиСинк`/`ВичиститиСтаріЗаписи`); `MobileExchange.xml.diff` (298+101) — XML-фрагменти декларацій нових `<Operation>` секцій (string-string-string); `СинкЛог.xml`+`СинкСистемнийПароль.xml`+`ЧисткаСинкЛогу.xml` — skeletons metadata з GUI-інструкціями; `README.md` (304+54) — порядок розгортання + 6 smoke-curl-тестів + 6 відкритих питань до 1С-розробника (наявність реквізитів `Catalog.Контрагенты` `НаименованиеТТ`/`ОбластьНаименование` etc.; `ВнешнийИД` у документах; `Документ.ПоступлениеБезналичныхДенежныхСредств` назва; `СтатусыМаршрутногоЛиста` значення; auto-резолв міста через `НайтиПоНайменуванню`; final naming). JSON-контракт усіх операцій стандартизований: req `{idempotencyKey, data:{...}}`, resp `{ok, code1C, alreadyProcessed, error}`. Idempotency через `Catalog.СинкЛог` (TTL 7 днів через `ScheduledJob`). **M3.2 (Етап 2.5) — Node ↔ BSL alignment** (Опція А підлаштувати Node, обрана user-ом): `services/manager-sync/src/soap/envelope.ts` — 2-параметровий contract (`ПарольВхода` + `JSONДані`) замість 3 (`ПарольВхода` + `IdempotencyKey` + `ПакетДанних`), новий `buildJsonDataEnvelope` helper; `client.ts` — 6 operation names перейменовано з `*` на `*JSON`, normalize-функції читають BSL response format (`code1C`, `error.{code,message}`) з fallback на legacy; `client.test.ts` — оновлені fixtures + 4 нових тести (2-param contract + BSL error.code/message); `apps/store/lib/sync/enqueue.ts` — doc-strings + блок «Розбіжності з BSL (TODO)» у заголовку; `proxy-client.ts` — коментар про payload→JSONДані. 5 файлів +308/-109. **6 категорій field-розбіжностей між Node payload і BSL** задокументовано як TODO (БЕЗПЕЧНІ, бо BSL обертає кожне присвоєння у `Попытка...Исключение`): Client `primaryAssortmentCode` нема довідника; Order `status`/`totalUah`/`items[].productId/lotId` ігнор; Payment `currency`/`orderInternalId` ігнор; CashOrder найбільше — лише UAH-ПКО (`amountEur`/`amountUsd`/`amountUahCashless`/`debtCorrection`/uid-и ігнор); Sale `priceTypeId` (UUID → треба `priceTypeCode`), `assignedAgentUserId` (UUID → треба `assignedAgentCode1C`), `customerName`/`onTradeAgent`/`items[].productId/lotId` ігнор; RouteSheet `payments[]` ігнор (1С відновлює з ПКО/РКО), `date`/`code1C`/`docNumber`/`items[].unit` ігнор. **M3.3 (Етап 3) — INBOUND polling (pull-mode):** Замість BSL ScheduledJob HTTP-client (складно тестувати) — наш Node Cron кличе 1С раз на 5хв. `docs/1c-bsl/inbound/Module.bsl.append` (242) — stub `СформуватиПакетДаннихJSON(ПарольВхода, ОстаннійКодСинхронізації)` з auth + базовим query до `Catalogs.Категорії` + JSON serialization + TODO коментарі з посиланнями на line numbers у ОбменАРМ для розширення на products/prices/orders. `docs/1c-bsl/inbound/README.md` — інструкція декларації (1 нова `<Operation>` 2 params string повертає string) + 9 відкритих питань 1С-розробнику (категорії довідник, ДатаЗміни реквізит, ВнешнийИД, маппінг статусів, slug-формат). Backend Node: `services/manager-sync/src/routes/pull.ts` (POST `/pull/snapshot`) + `mock-pull.ts`+`pull-client.ts`+`pull-types.ts` + 11 нових тестів; `apps/store/app/api/cron/pull-from-1c/route.ts` — Cron з CRON_SECRET auth, читає `MgrSyncState.value` cursor, виклик `/pull/snapshot`, ітерує `data.{categories,products,prices,orders}` БАТЧАМИ по 50, виклик existing `/api/sync/*` (S66/S67), advance cursor **тільки on full success**; `lib/sync/pull-from-1c.ts` (~381) — оркестратор з partial-fail логікою; +18 тестів. **Нова DB міграція `20260602_mgr_sync_state`** (additive idempotent): таблиця `mgr_sync_state` `(key TEXT PK, value TEXT, updated_at TIMESTAMPTZ)` + model `MgrSyncState`. `docs/PULL_FROM_1C_TASK.md` — Windows Scheduled Task setup (`Register-ScheduledTask` 5-хв інтервал, той самий `CRON_SECRET`). **M3.4 (Етап 4) — Closures блок:** Замінено заглушку `/manager/closures`. BSL (доповнення Етап 2): 2 нові Export-функції у `Module.bsl.append` (+299) — `ОтриматиДаніЗакриттяЗамовленьJSON` (wrap `ОбменАРМ.ПолучитьДанныеНезакрытыхЗаказов_v1` line 382, декод `ХранилищеЗначения` → JSON-масив), `ЗакритиСтаріЗамовленняJSON` (wrap `ОбменАРМ.ЗакрытьСтарыеЗаказы_v1` line 5174 + опц. створення нового `Документ.Заказ` за паттерном з v0 line 5108-5147: Контрагент / ТорговыйАгент=Продавец / ВивантажуватиВ1С=Истина / ПризначитиПродажТорговому=Истина / ТипЦен з контрагента / ЦенаПродажиВес=Цена/20); helper `ВідповідьУспіхЗДаними` у `СинкВхідний.bsl`; 2 нові декларації у `MobileExchange.xml.diff` + 2 smoke-тести у README. Backend Node: `services/manager-sync/src/routes/closures.ts` (134) + `closures.test.ts` (218, 9 тестів) + `closures-types.ts`/`closures-mock.ts`/`closures-client.ts` (240) — 2 SOAP routes (GET get-data + POST close) з mock-mode default; `apps/store/app/api/v1/manager/closures/[clientId]/route.ts` (226 + 313 tests, 13 кейсів) — GET (ownership + foreign-mask) + POST (ownership + idempotency + опційно `createOrderWithItems` для items з addToNewOrder); `lib/manager/closures-sync.ts` (185) — helper порту ХранилищеЗначення → JSON; `lib/manager/client-timeline.ts` розширено kind="close_orders". UI: `/manager/closures/page.tsx` повністю переписано (замість UnderConstruction); `_components/closures-client.tsx` (217) — ClientPicker реюз з orders/new → GET → таблиця; `closures-table.tsx` (122) — Замовлення/Дата/Номенклатура/Замовлено/Сума/Продано/Статус + чекбокс «Додати в нове»; зелена підсвітка коли `sold ≥ quantity` (як 1С); 4 UI-тести. **⚠️ Caveat M3.4:** створення локального Order при `addToNewOrder=true` обернено у try/catch — `productUid` з 1С vs `Product.id` у нас (різні ID-простори), resolver через `Product.code1C` — окрема задача наступного раунду. Сценарій без `addToNewOrder` працює. **⚠️ Caveat workflow (важливо для майбутніх паралельних worker-ів):** 3 паралельні worker-и (M3.2/M3.3/M3.4) ділили один worktree → ускладнення (Worker 2.5 робив 3 manual re-applies, Worker 3 5 проміжних checkpoint-комітів squash-нутих в 1, Worker 4 несподівано переключив гілки під orchestrator-ом). На майбутнє: використовувати `isolation: "worktree"` parameter у Agent tool для паралельних worker-ів. **Сьют:** store 1911-1912 pass/2 skip (+44 нових), manager-sync 77 pass (+24 нових), typecheck/prettier/0-`any` чисті. **Merge-protocol:** orchestrator (`claude/awesome-wright-fojWb`) → m3.1 (план+публікація+outbound BSL) → m3.2 (alignment) → m3.4 (closures) → m3.3 (INBOUND, з конфліктом у `services/manager-sync/src/index.ts` через registration — resolve = both routes реєструються). **Стан:** main `d5e9af6`, готове до тестів user-а перед deploy. **⚠️ deploy:** `git pull` + `pnpm install --frozen-lockfile` + `prisma migrate deploy` (3-4 міграції разом з картка-клієнт Ф3-4 + чат-inbox) + `.\scripts\deploy.ps1 -SkipInstall` + `pm2 restart ltex-manager-sync --update-env` + `pm2 save`. **Нова Windows Scheduled Task** `LTEX Pull From 1C` (Windows-task per `docs/PULL_FROM_1C_TASK.md`, 5-хв інтервал, той самий CRON_SECRET) — у mock-режимі повертає порожньо, активується разом з Етап 5. **Етап 5 (activation) — user actions, коли BSL змонтований:** виставити `ONEC_SOAP_URL=http://localhost/ltex/ws/MobileExchange.1cws` + `ONEC_SOAP_PASSWORD=<значення Constant.СинкСистемнийПароль>` + `SYNC_MOCK_MODE=false` у `services/manager-sync/.env` + `pm2 restart`. **Залишені TODO для майбутніх раундів (свідомо не зроблено):** (1) field-alignment між Node payload і BSL (6 категорій розбіжностей — повний backfill); (2) `Product.code1C` → `Product.id` resolver для Closures auto-create; (3) `MgrReminderAction.close_orders` enum + автонагадування «є протерміновані замовлення»; (4) розширення BSL `СформуватиПакетДаннихJSON` з категорій stub на повний дамп (products/prices/orders); (5) ротація `СинкСистемнийПароль` 90-денна. **Опорні docs:** `docs/1C_INTEGRATION_PLAN.md`, `docs/1C_WEBSERVICE_PUBLICATION.md`, `docs/1c-bsl/outbound/README.md`, `docs/1c-bsl/inbound/README.md`, `docs/PULL_FROM_1C_TASK.md`, `docs/CLOSURES_BLOCK_AUDIT.md`, `docs/1C_SYNC_MODULES_SPEC.md` (історичний reference). 1С-конфіг dump-и: `docs/1c-export-mobile/Central/WebServices/MobileExchange.{xml,bsl}` + `Central/CommonModules/ОбменАРМ/Ext/Module.bsl` (7635 рядків — головна reference). Орієнтир-команди 1С: `НачатьОбмен` (line 7421), `СформироватьПакетДанных` (line 4), `ВивантажитиТовари/Контрагентів/Документи` (line 30/64/93), `СоздатьПКО` (line 4629), `СоздатьРеализацииТоваровУслуг` (line 3571), `СоздатьМаршрутныеЛистыТовары` (line 5283).
- **Chat-inbox Фази 1a+1b — merged to main 2026-05-28 (НЕ ДЕПЛОЄНО ще; разом з Ф3-4 картки клієнта = 2 міграції на наступний deploy).** Greenfield об'єднаний месенджер-inbox для менеджера (план `docs/CHAT_INBOX_PLAN.md`). Рішення user: старт **Telegram+Viber**; обидва напрямки (клієнт→бот / менеджер→клієнт); авто-прив'язка за номером (`MgrClientPhone`→`phonePrimary`→`Customer.phone`); новий клієнт → бот питає область → реєструється на торгового за **мапою `MgrRegionAgent`** (Фаза 2, ще не зроблено); **TikTok DM — НЕМАЄ API** (deep-link), WhatsApp+Instagram — Фаза 3 (Meta setup). **⚠️ ВАЖЛИВО:** inbox працює тільки через **БОТИ** (клієнт пише L-TEX-боту, не особистому Viber/Telegram менеджера — у особистих немає API). **Live infrastructure:** Telegram бот `@ltex_second_stok_bot` (токен у `apps/store/.env`), Viber Bot (`VIBER_AUTH_TOKEN` у `.env`), webhook-и зареєстровані PowerShell-скриптом (setWebhook/set_webhook), **Cloudflare WAF Custom Rule «Skip WAF for bot webhooks»** покриває `/api/viber/webhook` + `/api/telegram/webhook` + `/api/v1/manager/reminders/` (Skip: All managed rules + Browser Integrity Check; БЕЗ цього було `502/status:1` на пробу Viber). **Фаза 1a (backend):** міграція `20260531_chat_inbox` (3 enum chat_platform/direction/sender + `ChatConversation` `(platform, externalUserId)` unique + `ChatInboxMessage`); `lib/chat/phone-match.ts` (normalizePhone + 3-рівневий пошук), `platform-send.ts` (telegramSender+viberSender з mock-fallback при відсутніх токенах, never throw), `inbound.ts` (`ingestInboundMessage` — upsert розмови + auto-link за телефоном + create message + bump unread у транзакції), `conversation-access.ts` (shared permission helper); інжест у `/api/telegram/webhook` (вільний текст + contact-share, БЕЗ ламання `/search`,`/lots`,`/order`,callback) + `/api/viber/webhook` (free-form text у вкладеному `else` після `menu:*`/`/start`/pending-input; `fetchViberUserPhone` через `/pa/get_user_details` best-effort); API `GET /api/v1/manager/chat/conversations` (admin all / manager own+unassigned, пагінація) + `[id]` GET (header+thread, `?before/?limit`) + `[id]/messages` POST (Zod 1..4000, platform-send→persist out, authorUserId) + `[id]/read` POST (mark read+reset unread); 29 нових тестів. **Фаза 1b (UI):** `/manager/chat` 2-кол. inbox (`InboxClient`/`ConversationList`/`ConversationThread`): полінг список 30с (+visibility/focus) + тред 3с (лише visible); reply box з Enter→надіслати, optimistic, auto-scroll-якщо-біля-низу; bubbles in (gray-left) / out (green-right) / system (centered); mobile — full-screen overlay; новий `GET /api/v1/manager/chat/unread` + `<ChatUnreadBadge>` у sidebar-пункті «Чат» (polling 30с, «9+» cap, інтегровано через `badgeSlot` prop). **Сьют store 1828 pass/2 skip.** **⚠️ deploy (разом з Ф3-4 картки клієнта):** `prisma migrate deploy` застосує `20260530_mgr_client_keywords` + `20260531_chat_inbox`; новий env: токени вже в `apps/store/.env` (Telegram+Viber). **⚠️ Безпеки:** старий `TELEGRAM_WEBHOOK_SECRET` був частково засвічений у чаті — треба ротувати (user сказав «пізніше»); `setWebhook` буде треба перевиставити з новим. **Standalone-`.env` gotcha:** наявний скрипт `deploy.ps1` крок [7/8] синкає `.env`→standalone; якщо вручну редагуєш `.env` без deploy → треба `Copy-Item .env .next/standalone/apps/store/.env -Force; pm2 restart ltex-store --update-env` (зафіксовано як одноразовий випадок: 28.05.2026). **Фаза 2 (наступна сесія):** бот-розмова реєстрації нового клієнта (питати область → мапа `MgrRegionAgent` → створити `MgrClient`+`Customer` → призначити агента) + адмін-UI мапи область→торговий + ChatPlatform `whatsapp`/`instagram` — заглушки до Фази 3. Усі деталі/обмеження — `docs/CHAT_INBOX_PLAN.md`.
- **Картка клієнта — переробка Фази 1-4 (merged to main 2026-05-26) + блок «Закриття старих замовлень» ВІДКЛАДЕНО.** **Закриття старих замовлень:** аудит `docs/CLOSURES_BLOCK_AUDIT.md` — блок суто SOAP-до-центральної (регістр `ЗаказыПокупателей.Остатки`, «Продано» з `РеализацияТоваровУслуг`, закриття = проведення документа `ЗакрытиеЗаказовПокупателей`); серверні методи на `ХранилищеЗначения` → потрібні нові JSON-методи. **Рішення user: ВІДКЛАДЕНО до етапу 1С-обмінів** (зафіксовано: перенос позицій = авто-створити+відкрити нове замовлення). Заглушка `/manager/closures` лишилась. **Картка клієнта** (план `docs/CLIENT_CARD_OVERHAUL_PLAN.md`, 4 research-агенти; рішення user по вкладках): **Ф1** футер +«Створити реалізацію»/«Створити оплату» (orders/sales→Customer.id, payments→MgrClient.id, prefill підтверджено) + банк-рахунок read-only рядком у «Дані» (окрему вкладку прибрано, 11→10). **Ф2a** редаговані телефони (CRUD `/clients/[id]/phones` auth+canEditClient) зі справжніми бренд-SVG (`_components/brand-icons.tsx`: Viber/Telegram/WhatsApp/Instagram/TikTok/YouTube/Facebook/Pinterest). **Ф2b** «Соц мережі»→«Соцмережі та месенджери» редаговані (CRUD `/clients/[id]/messengers`, 9 мереж) + новий блок «Контакти» на шапці (`client-contacts-strip` — іконки телефонів+соцмереж+сайт). **Ф3** редаговані маршрути (CRUD `/clients/[id]/routes` add/del/reorder з довідника `routes`; сам довідник синкатиметься з 1С) + нове поле `MgrClient.keywords` (**міграція `20260530_mgr_client_keywords`**) + вкладка «Ключові слова» (chip-теги, save через client PATCH) + пошук у списку клієнтів матчить keywords. **Ф4** Історія авто-записів: helper `lib/manager/client-timeline.ts` (`recordClientEventSafe` fire-and-forget, резолв MgrClient за clientId АБО customerId→code1C) пише `MgrClientTimelineEntry` при наших подіях — замовлення/реалізація/оплата/бронь/нагадування (тип у `kind`, БЕЗ міграції) + ручні `comment` редагуються/видаляються (`/clients/[id]/timeline/[entryId]` PATCH/DELETE, manual-only guard), авто read-only з бейджем «авто». Чужі клієнти скрізь masked. **Відкладено до 1С-обмінів:** Асортимент / Історія продаж / Презентації (дані з центральної). **Окремо (greenfield):** об'єднаний чат Viber/Telegram/Instagram/WhatsApp/TikTok. Сьют **store 1792 pass/2 skip**, typecheck/prettier/0-`any` чисті. **⚠️ deploy Фаз 1-2:** без міграції (`git pull`+`deploy.ps1`). **⚠️ deploy Фаз 3-4:** `prisma migrate deploy` (`20260530_mgr_client_keywords`) + `deploy.ps1`.
- **Manager «Нагадування» block ✅ УСІ 4 ЕТАПИ — merged to main + DEPLOYED (2026-05-26).** Локальний блок (БЕЗ обміну з 1С — підтверджено аудитом: у central `docs/1c-export/` нема `Нагадування`/`ЗапланированыеУведомления`). Аудит `docs/REMINDERS_BLOCK_AUDIT.md`, план `docs/REMINDERS_BLOCK_PLAN.md`. Розширено `MgrReminder` (`clientId`→**опційний**; enum-и `MgrReminderPeriod`/`MgrReminderAction`/`MgrReminderSource`; `isProductReminder`/`orderVideo`/`lotId`/`productId` — **плоскі скаляри**) + нова `MgrReminderItem` (міграція `20260529_mgr_reminders_block`). **Етап 1:** standalone `/manager/reminders` (заміна заглушки M1.9) — список з фільтрами «Завершені»/«Заказ відео»+пошук+«Створити», форма «Звичайне» (опис/дата/повтор+жива підказка/клієнт опц.), pure-хелпер `reminder-recurrence.ts` (nextOccurrence з clamp кінця місяця); глобальні API `/api/v1/manager/reminders` GET/POST + `[id]` PATCH (recurring complete → advance `remindAt`, лишається активним) /DELETE; client-card reminders (M1.3c) не зламані (notifications зроблено null-safe). **Round-2:** прибрано чекбокс «Заказ відео» з форми (ставиться авто при «Замовити відео» в Прайсі) + режим «Щороку»; тулбар ховається коли форма відкрита (+«Скасувати»). **Етап 2:** тип «Для товарів» — перемикач, клієнт (обов'язк.)+чек-лист товарів (`MgrReminderItem`), per-item «Виконано»+roll-up у транзакції, антидубль (`skippedProductIds`), спільний `reminder-serialize.ts` з batch-lookup назв товарів; `periodicity=event` (не нагадує за часом). **Етап 3:** дзвіночок з діями «Виконано»/«Відкласти годину»; `/notifications` виключає podієві (`periodicity=event` — товарні/«По події» лише у списку). **Етап 4:** cron `/api/cron/generate-reminders` (auth `CRON_SECRET`, як інші) + `generate-reminders.ts`: детектор **«минула бронь»** (`auto_bron` — протерміновані `Lot.reserved*` → нагадування «Перенести бронь?» менеджеру + **звільняє лот** = антидубль) + **«з'явилось відео»** (`auto_video` — стеження ставиться у Прайсі «Замовити відео» через глобальний POST `orderVideo+periodicity=event+productId/lotId`; спрацьовує коли `Lot.videoUrl` з'явився → flip у `viber_video`). Дії на нагадуванні: «Перенести бронь»(→`/manager/prices/lots?lotId=`)+«Скинути Viber»(новий `/reminders/[id]/viber-message`→`buildProductShareText`+ShareSheet). `OrderVideoButton` отримав `productId`(+`lotId`/`barcode`), call-sites: product-card-view/product-row-menu/lot-card-modal. Сьют **store 1742 pass/2 skip**. **Рішення user:** ручні (Звичайне+Для товарів)+авто(відео/бронь), справжні повтори, дзвіночок+список (без модалки-попапа), **БЕЗ «закриття старих замовлень»** (нема екрана — відкладено). **⚠️ deploy:** `prisma migrate deploy` (`20260529`, застосовано) + **нова Windows-задача** для cron `GET /api/cron/generate-reminders` (~10хв, той самий `CRON_SECRET` з кутовими дужками, що в email/sync-задачах). **Етап 1 деплоївся окремо (з міграцією), Етапи 2-4 — другим деплоєм без міграції.** **+ Картка клієнта (без міграції):** вкладку «Нагадування» переведено на той самий standalone `RemindersClient` з **авто-прив'язкою контрагента** (глобальний GET `?clientId`; `fixedClientId/fixedClientName` у `reminder-create-form`+`reminders-client` — пікер клієнта приховано, обидва типи POST-ять цього клієнта). 1С-паритет: у Контрагентах вкладка нагадувань = глобальний довідник, фільтрований по `Контрагент`. Стару M1.3c-реалізацію (`client-reminders-form`/`-item` + client-scoped endpoints `/clients/[id]/reminders`) видалено (ніким не використовувалась); бейдж `countOverdue` + `client-reminders-grouping` лишилися. Сьют **store 1729 pass/2 skip**.
- **Manager «Маршрутний лист» block ✅ УСІ 5 ЕТАПІВ + round-2 — merged to main + DEPLOYED (2026-05-25).** Документ-агрегатор дня виїзду (`Document.МаршрутныйЛист`), `/manager/routes` (тег M1.9). Аудит `docs/ROUTE_SHEET_BLOCK_AUDIT.md` (3 паралельні research-агенти: mobile форма+метадані + central `ОбменАРМ`), план `docs/ROUTE_SHEET_BLOCK_PLAN.md`. Моделі `RouteSheet`+7 дочірніх (`mgr_route_sheets` + `_orders/_items/_loading/_sales/_sale_items/_payments/_tasks`, міграція `20260528_mgr_route_sheets`) + зворотні `routeSheetId` на Order/Sale/MgrCashOrder + enum `route_sheet`. Cross-model id-поля у дочірніх — **плоскі скаляри** (batch-lookup, без inverse-relations на спільних з магазином моделях). **Етапи:** 1 список+шапка+Заказы(виключає замовлення вже в маршруті)+Товари(зведення); 2 Загрузка+Бракує(замовлено−вільні лоти без чужих броней)+лічильники; 3 Реалізації/Продажи/Оплати — **реюз форм** Sale/CashOrder через `?routeSheetId` (вкладки **виводяться зі зворотних посилань**, без дубль-запису); 4 Завдання+кілометраж+GPS+статуси (draft→dispatched→completed, completed-lock+реверт); 5 sync-каркас(mock) `enqueueRouteSheetCreate`+`/sync/route-sheets`+BSL §3.6. **Round-2 (після тесту user):** пікер замовлень Дата/Клієнт/Місто/Область+фільтри; **Загрузка → read-only** (дані з 1С при обміні, склад сканує в 1С — не в нашому додатку); **Маршрут = вільний текст у `comment`** (НЕ довідник `MgrRoute`); **Дата(авто)/ДатаПриезда/Кілометраж/GPS — read-only (передаються з 1С)**; GPS-capture у браузері прибрано; дубль-плитку «Маршрут» на дашборді видалено. Сьют **store 1668 pass/2 skip + manager-sync 53**. **Рішення user:** маршрут-текст/експедитор-select/кілометраж/GPS на документі; нестача=замовлено−вільні лоти; одна форма для всіх (розподіл прав по ролях — follow-up). **⚠️ deploy:** `prisma migrate deploy` (`20260528`) — застосовано на проді.
- **Manager «Оплати/Каса» block ✅ УСІ 3 ЕТАПИ — merged to main + DEPLOYED (2026-05-25).** Каса (`DataProcessor.Оплата` + `Document.КассовыйОрдер`), `/manager/payments`. Аудит `docs/PAYMENTS_BLOCK_AUDIT.md`, план `docs/PAYMENTS_BLOCK_PLAN.md`. Розширено `MgrCashOrder` (курси-знімок `rateEur/rateUsd`, `documentSumEur`, `debtCorrection`, мультивалютні UUID-ключі, `customerId`, `docNumber`, `archived`, FK на 2 нові довідники) + нові `MgrBankAccount`+`MgrCashFlowArticle` + enum `cash_order` (міграція `20260527_mgr_payments`). **Етап 1:** список `/manager/payments` (Дата/Номер/Вид/Клієнт/Стаття/Сума€/Рахунок, архів, пошук, ownership) + розширені довідники у `/dictionaries` + admin-CRUD банк.рахунків/статей (лінк у Налаштуваннях) + `getPaymentSummary` (порт `ПолучитьДанныеПоОплате`). **Етап 2a:** EUR-base формули §B аудиту (`reduceToEur`/`reduceChangeToEur`/`computeBalanceEur`/recommendations) + POST `/cash-orders` (реалізація АБО клієнт, Приход/Расход, ручна 3-валютна здача, гард прихованого рахунку при приході, **БЕЗ анти-дубля**) + `discount-remainder` (знижка на найдорожчий рядок реалізації, поріг 5€). **Етап 2b:** повноекранна payment-форма (порт обробки «Оплата») на `/manager/payments/new` (`?saleId`/`?clientId`/standalone), панель зведення на реалізації, видалено стару `payment-modal`. **Етап 3:** Viber-чек `buildPaymentReceiptText`+ShareSheet; `enqueueCashOrderCreate`+`/sync/cash-orders`+mock; BSL §3.5 `СоздатьПКО`. Сьют **store 1531 pass + manager-sync 48**. **Рішення user:** ручна здача в 3 валютах; банк.рахунки+статті — довідники (порожні поки, +мінімальний admin-UI); знижка на залишок по рядку реалізації; кілька оплат на реалізацію дозволено; оплата без реалізації (погашення боргу); Viber; вид Приход/Расход обидва. **⚠️ deploy:** `prisma migrate deploy` (`20260527`) — застосовано на проді.
- **Manager «Реалізація» block ✅ УСІ 5 ЕТАПІВ ЗАВЕРШЕНО — merged to main + DEPLOYED (2026-05-22); деяк. round-2 правки (lot-picker «Додано»+mobile overflow).** Новий блок з нуля (`Document.РеализацияТоваровУслуг`), маршрут `/manager/sales` (тег M1.6). Аудит: `docs/REALIZATION_BLOCK_AUDIT.md`, план: `docs/REALIZATION_BLOCK_PLAN.md`. **Рішення user:** Реалізація — **окремий документ** (не з Замовлення), факт продажу/відвантаження; додавання товарів — скан ШК (поле+камера) + підбір за прайсом; **без знижки/рядок**; статуси Чернетка/Відправлено/Проведено(архів)/Скасовано; **повна каса** (оплати в 3 валютах + здача). **Етап 1:** моделі `Sale`/`SaleItem` (`mgr_sales`/`mgr_sale_items`, міграція `20260525_mgr_sales`) + `realization` у enum `SyncEntityType` + список `/manager/sales` (Дата/Номер/Контрагент/Місто/Статус/Сума, фільтр архівних, пошук, ownership) + dashboard tile. **Етап 2:** форма `SaleForm` create+edit (клієнт підтягує тип цін/доставку/№НП/борг; тип цін → перерахунок рядків; Наложка+COD; «на торгового контрагента»+агент; ТТН; курс EUR/USD) + товари через **ШК-поле+нативний `BarcodeDetector` (без npm-залежності)** → `GET /lots/by-barcode` (зберігає lotId) + підбір за прайсом; API `POST /sales`, `GET/PATCH /sales/[id]` (posted-лок 409 + граф переходів + ownership). **Етап 3:** Viber — `sale-message.ts` (клієнту/у групу) + кнопки у формі через перевикористаний `ShareSheet` (Прайс). **Етап 4:** `MgrCashOrder` (`mgr_cash_orders`, міграція `20260526_mgr_cash_orders`) — оплата в 3 валютах+безнал, авто-розрахунок здачі, авто-ордер розходу (`changeForId`), зведення з конвертацією EUR/USD за курсами-знімком; UI payment-modal+payments-panel на сторінці `[id]`. Закриває ядро блоку «Оплати/Каса». **Етап 5:** sync-каркас (mock) — `enqueueSaleCreate` (entityType `realization`) fire-and-forget у create/update, `routeFor` case, `services/manager-sync` `/sync/realizations/:id`, BSL-спека §3.4 у `1C_SYNC_MODULES_SPEC.md`. ~70% коду перевикористано з Замовлень. Сьют **store 1417 pass / 2 skip + manager-sync 43**, typecheck/prettier/0-`any` чисті. **⚠️ Перед deploy: `prisma migrate deploy`** (2 міграції: `20260525_mgr_sales`, `20260526_mgr_cash_orders`). **Не зроблено (свідомо):** реальний 1С BSL (на загальному етапі обмінів), sync касових ордерів (follow-up), CheckBox/ETTN фіскалізація, runtime-перевірка камери/UI у браузері (немає браузера/БД у пісочниці).
- **Manager «Замовлення» block ✅ Етапи 1-3 ЗАВЕРШЕНО (2026-05-21).** Доповнили наявний (M1.4 список+перегляд, M1.5b створення) під 1С Document.Заказ. План: `docs/ORDERS_BLOCK_PLAN.md`. **Етап 1:** форма створення — тип цін (перерахунок рядків, `order-pricing.ts`), доставка, наложка, «призначити продаж торговому» (агент), «вивантажувати в1С», підтягування типу цін/боргу/доставки з MgrClient. 7 нових полів `Order` (priceTypeId/deliveryMethod/cashOnDelivery/assignedAgentUserId/exportTo1C/archived/isActual, міграція `20260524_order_manager_fields`). **Етап 2:** статуси draft/sent/posted(архів)/cancelled (`order-status.ts` — граф переходів), `PATCH /orders/[id]` (ownership + posted-лок 409 + транзакційне оновлення), редагована сторінка `/orders/[id]` (OrderForm параметризовано create+edit). **Етап 3:** список — колонки Місто+Актуальний, фільтр «архівні», пошук по товарах усередині замовлень, кольори (`orders-list.ts`). Сьют 1208 pass. **⚠️ Перед deploy: `prisma migrate deploy`** (1 нова міграція). **Затверджені рішення:** архів=проведено в 1С; «Скопіювати» НЕ робимо; «призначити продаж торговому»=зарахувати продаж іншому агенту; мін.10кг не перевіряється. **Етап 4 ВІДКЛАДЕНО:** нагадування з документа «Приход» (товар знову на складі → нагадати) — окремий механізм, разом з колонкою «Нагадування» на рядку замовлення.
- **Manager «Замовлення» форма — фінальні правки після тесту user (2026-05-21).** (1) **Виправлено критичний баг** «Клієнта не знайдено»: ClientPicker давав `MgrClient.id`, а `Order.customerId` — FK на `Customer` → новий `lib/manager/resolve-customer.ts` резолвить через `code1C` (find-or-create як quick-order: code1C→phone→create), підключено у POST `/orders`. Замовлення тепер створюється і потрапляє в список. (2) **Підбір через прайс по МІШКАХ, без конкретного лота** (`lotId` завжди null — central 1С не приймає замовлення з точним лотом; доробимо після обмінів): «Підбір» → пошук товару → к-сть мішків → вага=`order-bag-weight.ts` (averageWeight→середнє по лотах→20кг) × мішки. Модалка має редаговану ціну за кг (stepper кратно 0.05, `price-step.ts`) + кнопку перегляду картки товару. Видалено lot-picker/product-picker/item-row. (3) Прибрано дубль-кнопку «Зберегти» (одна дія). (4) **Дизайн переоформлено зі «сірої 1С-таблиці» у стиль проекту** (картки-секції, зелений акцент). Сьют **1233 pass**. Магазин не чіпали. **⚠️ deploy: лише `git pull` + `deploy.ps1` (міграцій у цій правці нема; по всьому блоку Замовлення — 1 міграція `20260524`).**

### Наступна сесія — M3.0 ОРКЕСТРАТОР (правки готових блоків)

**Стан:** усі 5 менеджерських блоків (Прайс / Замовлення / Реалізація / Оплати / Маршрутний лист) + блок **Нагадування** ✅ ЗАВЕРШЕНІ + **НА ПРОДІ** (main `03dba86`, deploy 2026-05-26, міграції застосовано до `20260529`). Робоча feature-гілка: **`claude/charming-cerf-4067W`** (НЕ створювати нову; продовжувати на ній).

**Завдання M3.0:** **round-N правки в усі готові блоки** після тесту user. User тестує живцем на `https://new.ltex.com.ua/manager/*` і кидає зауваження по конкретних блоках/вкладках. Orchestrator на кожен пакет правок: дає воркеру (Agent subagent) **точну спеку** → ПЕРЕВІРЯЄ сам (`git diff` + `pnpm --filter @ltex/store run typecheck` + `vitest run` (store+manager-sync) + `prettier --check`) → комітить на feature-гілку → **мердж у `main` → deploy** (диктує команди user-у; сам deploy не запускає — нема SSH). Далі по стратегії: доробка картки клієнта → наприкінці реальні обміни з 1С (BSL пише сам Claude).

**Робочий процес — без змін, див. секцію M2.0 нижче** (orchestrator планує/перевіряє/мерджить, воркери кодять). Спілкування з user — **простою мовою**. Усе важливе — у CLAUDE.md.

**Deploy-протокол (відпрацьований):** мердж feature→main: `git checkout -B main origin/main && git merge --no-ff claude/charming-cerf-4067W && git push origin main` (при 403 — `git push origin main:claude/sNN-merge`, далі user локально `git merge --ff-only`). На сервері (Windows, **user виконує сам**): `cd E:\ltex-ecosystem` → `git pull origin main` → `pnpm install --frozen-lockfile` → `pnpm --filter @ltex/db exec prisma migrate deploy` (**лише якщо є нові міграції**) → `.\scripts\deploy.ps1 -SkipInstall` → `pm2 restart ltex-manager-sync --update-env` (**лише якщо чіпали sync-сервіс**) → `pm2 save`. **⚠️ Завжди звіряти, що робота справді в `main` ПЕРЕД тим як казати user-у деплоїти** (двічі траплялось: user запускав deploy до мержу → `git pull` = «Already up to date»; не забувати мерджити першим).

**Housekeeping (відкладено — user робить через GitHub UI, бо sandbox 403 на delete/merge):** видалити 15 застарілих `claude/*` гілок (усі `manager-m1-*` + `m1.5-ops-fix` — 0 ahead/merged; `s85-spec`/`s85-merge`/`e2e-customer-flow`/`polish-batch-fixes`/`admin-password-reset` — стара S46-S85 робота; **DEAD `sales-manager-mobile-app-gDO7s`** — хибна робота, не мерджити). Лишити `main` + `claude/charming-cerf-4067W`. Dependabot PR: **#12** (Next/React патчі 15.5.14→18 / 19.2.4→6) — безпечно змерджити; **#13** (Prisma 6.2.1→6.19.3 minor) — змерджити після тесту; **#18** (TS 5.9→6 major у dev-групі), **#15**+**#16** (Expo `55.x` = SDK 54+ проти нашого SDK 52) — **закрити**.

**Опорні документи:** аудити+плани блоків — `docs/{PRICES,ORDERS,REALIZATION,PAYMENTS,ROUTE_SHEET}_BLOCK_*.md`; 1С-конфіг — `docs/1c-export-mobile/`; BSL-спека обмінів — `docs/1C_SYNC_MODULES_SPEC.md` (§3.2-3.6 готові, реальний BSL — на етапі обмінів). Маршрути менеджерки: `/manager/{prices,orders,sales,payments,routes,customers}`.

### Наступна сесія — M2.0 ОРКЕСТРАТОР (контекст-передача)

**Робочий процес (ОБОВ'ЯЗКОВО):** ця cloud-сесія = **orchestrator**: вивчає 1С-конфіг (через subagent-и), пише план у `docs/<BLOCK>_BLOCK_PLAN.md`, показує user-у простою мовою, чекає «ок» + правки, **дає задачі воркерам (Agent subagent)**, ПЕРЕВІРЯЄ звіт сам (`git diff` + `pnpm --filter @ltex/store run typecheck` + `vitest run` + `prettier --check`), **комітить на `main`** (НЕ feature branch), сам код НЕ пише. Спілкування з user — **простою мовою, не технічно**. Усе важливе — сюди.

**Стратегія (рішення user):** спочатку весь функціонал менеджерських блоків → потім доробка картки клієнта → **наприкінці обміни з 1С (BSL пише сам Claude)**. Лот у замовлення/реалізацію поки не пишемо (central 1С не приймає) — після обмінів.

**Блоки менеджерської програми (5) — УСІ ✅ ЗАВЕРШЕНІ + НА ПРОДІ:** ✅ **Прайс** (`docs/PRICES_BLOCK_PLAN.md`), ✅ **Замовлення** Етапи 1-3 (`docs/ORDERS_BLOCK_PLAN.md`; Етап 4 нагадування-з-приходу відкладено), ✅ **Реалізація** (`docs/REALIZATION_BLOCK_*`), ✅ **Оплати** (`docs/PAYMENTS_BLOCK_*`), ✅ **Маршрутний лист** (`docs/ROUTE_SHEET_BLOCK_*`). **Далі (рішення user 2026-05-25):** **правки в усі готові блоки** (round-N після тесту) → доробка картки клієнта → **наприкінці реальні обміни з 1С (BSL пише сам Claude)**. Спільний паттерн round-fix: orchestrator дає воркеру точну спеку правок → перевіряє → комітить на feature-гілку → мердж у main → deploy.

**Як починати блок:** прочитати 1С-форми у `docs/1c-export-mobile/MobileAgent/Documents/<Док>/Forms/*` (через subagent-агенти, як робили з Прайсом/Замовленням) → інвентар → план → user → воркери по етапах. Багато функціоналу менеджерки вже існує на `main` (M1.1-M1.5b: auth, клієнти, sync backbone) — спершу звіряти що Є, доповнювати, НЕ дублювати.

**Інваріанти:** дані спільні з магазином (`Product`/`Lot`/`Price`/`Customer`/`Order` + довідники `MgrPriceType`/`MgrClient`); нові менеджерські поля — additive до існуючих моделей; бронь/менеджерські поля поки в нашій DB; ціна за типом цін: `MgrPriceType.code`===`Price.priceType`; клієнт менеджерки `MgrClient` ↔ замовлення `Customer` резолвиться по `code1C`. **⚠️ Перед deploy на сервері: `prisma migrate deploy`** — усі additive міграції Прайс(4)+Замовлення(1). Тести станом на кінець сесії: **1233 pass / 2 skipped**.

- **Manager «Прайс» block ✅ ЗАВЕРШЕНО (2026-05-20).** Відтворено старий 1С-екран «Прайс» (з MobileAgent) у `/manager/prices/*` з новим UI. План+статус: `docs/PRICES_BLOCK_PLAN.md`. Усі 5 етапів готові й на `main`: **1** список з фільтрами/пошуком/сортуванням (`/manager/prices`); **2** картка товару (`/prices/[id]`); **3a** картка лоту + редагування менеджерських полів (вага/залишок read-only) + **3b** глобальний екран «Деталі по мішках» (`/prices/lots`); **4** бронювання лотів (своя/чужа/протермінована); **5a** повідомлення (Замовити відео + Поділитися Viber/Telegram/WhatsApp) + **5b** довідник шаблонів (`/manager/message-templates`). Нові поля `Lot` (isTarget/arrivalDate/sector/isOpen/comment/description/videoDate/reserved\*) + модель `MgrMessageTemplate` — 4 additive міграції (`20260520_lot_manager_fields`, `20260521_lot_card_fields`, `20260522_lot_booking`, `20260523_message_templates`). Сьют 1126 pass. **⚠️ Перед deploy на сервері: `prisma migrate deploy`** (4 нові additive міграції). Дані товарів/лотів/цін спільні з магазином; бронь/менеджерські поля поки в нашій DB (sync з 1С — на етапі обмінів). Інвентар старого 1С зроблено (форми `Catalogs/Номенклатура` + `ХарактеристикиНоменклатуры`). **Затверджені рішення:** (1) бронь + менеджерські поля лоту поки в нашій DB, sync з 1С пізніше; (2) повідомлення через «Поділитися» з вибором месенджера (Viber/Telegram/WhatsApp/…) + копіювання тексту; (3) сканер ШК — ручне поле + USB поки, камера пізніше; (4) «Оновити залишки/ціни» — ручна кнопка + авто, реальний обмін на етапі інтеграції; (5) відтворюємо весь старий функціонал. Вага/Залишок лоту — read-only (з 1С). Дані товарів/лотів/цін — спільні з магазином (`Product`/`Lot`/`Price`/`MgrPriceType`), нові менеджерські поля додаються до `Lot`.
- **Робочий процес (cloud session, 2026-05-20):** ця сесія = **orchestrator** — планує, дає задачі воркерам (subagent), перевіряє звіт, комітить на **main**, САМА код не пише. Усе важливе пишеться сюди (CLAUDE.md). Спілкування з user — простою мовою.
- **⚠️ DEAD branch `claude/sales-manager-mobile-app-gDO7s`:** містить хибну роботу (INTEGRATION_PLAN_1C.md, /admin/customers, S86/S87 specs) зроблену на ЗАСТАРІЛІЙ базі (до M1.x). Дублює/суперечить наявному `main` (M1.1–M1.5b: менеджерська панель + sync backbone уже існують). **НЕ мерджити, не використовувати.** Орієнтир — лише `main`.
- **S27** deploy.ps1 hardening v1 (direct pnpm filter, .env sync, --update-env)
- **S28-S32** web catalog UX (product card quickfixes, remove Compare, filters left sidebar + mobile bottom-sheet, grid/list toggle, wishlist always visible)
- **S33** mobile home screen Rozetka-style + 4-tab restructure (Home / Search / Cart / More + FAB messenger)
- **S37** deploy.ps1 hardening v2 (Tee-Object pipe + PM2 try/catch + pm2 ping). PM2 daemon resilience verified end-to-end. Tee-Object thesis для buffering — невірний (S39 закрив).
- **S38** mobile catalog parity with web (ProductCard 4:3 + NEW/SALE + wishlist heart UI, bottom-sheet `CatalogFilterSheet` with all web filters, FlatList numColumns=2). Done by background worker subagent in ~10 min.
- **S39** deploy step 4 fix + wishlist persistence. **Both prior buffering theses (S37 Tee-Object, "real fix is cmd /c") були хибними** — direct `pnpm --filter @ltex/store run build` працює як треба. Не додавати редирект у крок [4/8] жодного типу. Mobile wishlist: SecureStore-backed (100-item cap), fire-and-forget mirror у `/api/mobile/favorites` коли logged in, WishlistScreen — 2-col grid.
- **S34** mobile home banners + 3 product rails (Топ / Акції / Новинки). Single-shot `/api/mobile/home` endpoint (60s ISR), pure-RN BannerCarousel (rgba overlay, no expo-linear-gradient dep) + HorizontalProductRail. Endpoint live, банери на сайті — pending admin upload (P0 #4).
- **Регресія S34→S42:** другий поспіль deploy висне на [4/8] бо PM2 worker (cluster АБО fork) тримає file handle на `apps/store/.next/standalone/...`. **Закрито у S42** через `pm2 kill` prelude (daemon-level signal вбиває всіх дітей). S40/S41 спроби (`pm2 stop`, `pm2 delete`+regex) не спрацювали — application-level API на Windows нестабільне. Spec у `docs/SESSION_40_DEPLOY_PM2_NODE_LOCK.md` (S40) + `docs/SESSION_41_DEPLOY_FORK_MODE.md` (S41).
- **S35** mobile chat unread badge — light `/api/mobile/chat/unread` endpoint (count manager+unread+customerId), `<ChatUnreadProvider>` polling 30с only when logged-in + AppState foreground refresh, `tabBarBadge` на MoreTab (#dc2626, "9+" cap) + бейдж біля "Чат з менеджером" на MoreScreen, optimistic clear у ChatScreen (initial fetch + SSE manager message). 8 files, +243/-6, tests 249/249.
- **S36** mobile notifications screen + new `Notification` model. DB migration `20260428_notifications` (table + 2 indexes + FK CASCADE), GET extended additively з in-app feed (`take: 100`), new PUT для mark-single/mark-all з tenant scoping. Mobile FlatList: type icons, inline `formatRelative`, unread blue dot, optimistic mark-read, header-right mark-all, pull-to-refresh, deep links (order_status / new_video / chat_message / system). 6 files, +571/-16, tests 255/255. **⚠️ Перед deploy потрібен `prisma migrate deploy` на обох DBs.**
- **S43** DB `ViewLog` model + recommendations engine. Migration `20260429_view_log` (table + 2 indexes + FK Cascade/SetNull). New POST `/api/mobile/products/[id]/view` (auth optional, fire-and-forget) + GET `/api/mobile/recommendations` (category-match для authed з 30-day window, fallback newest in-stock). Спільний `lib/mobile-product-shape.ts` між home + recommendations. 4-й rail "Рекомендоване для вас" на mobile HomeScreen + `productsApi.trackView()` на ProductScreen mount. 14 files, +604/-85, tests 264/264. **⚠️ Перед deploy `prisma migrate deploy` на local DB.**
- **S44** mobile UX batch. CatalogFilterSheet: backdrop/X/Android-back з discard warning через `Alert.alert` коли dirty (snapshot+JSON-compare). Subcategory drill-down: новий `GET /api/categories?parent=<slug>` + 2-level pickers у sheet, reset child при switch parent. List/Grid toggle у CatalogScreen header — `expo-secure-store` `mobile.catalogListMode`, ProductCard `layout="list"` (horizontal flex, 120×120 thumbnail + country line). API plumbing: `/api/catalog` приймає `categorySlug` (expand parent+children) + `subcategorySlug` precedence + `inStock=true`. 10 files, +601/-21, tests 271/271.
- **S45** mobile QuickView modal. Bottom-sheet (`QuickViewModal.tsx`, 320 рядків, `presentationStyle="overFullScreen"` + transparent + slide animation): hero image + SALE badge + heart toggle через `useWishlist`, назва, meta (якість·сезон·країна), ціни (акційна+опт), lots count, "Закрити"/"Дивитись повністю" CTA. ProductCard: `onLongPress?` prop з `delayLongPress={500}` на grid+list. Wired у CatalogScreen, HomeScreen (всі 4 rails), WishlistScreen, HorizontalProductRail прокидує. Hard rule: trackView НЕ викликається на open QuickView (тільки full ProductScreen mount). 6 files, +364/-2, tests 271/271.
- **S46** husky + lint-staged pre-commit hook. Auto `prettier --write` на staged .ts/.tsx/.js/.jsx/.json/.md перед commit. Bypass через `--no-verify`. Server unaffected (dev-time only — `prepare` script ставить husky при `pnpm install`). 3 files, +417/-24.
- **S47** mobile UX completion. Pull-on-login wishlist merge: на customerId change `favoritesApi.list()` → union з local SecureStore (server-win on conflict) → cap 100. `lastSyncedCustomerIdRef` блокує re-runs у session, reset на logout. QuickView image carousel: pure RN `FlatList horizontal pagingEnabled` + dots indicator (тільки коли `images.length > 1`). Backend `/api/mobile/favorites` GET тепер віддає canonical `WebCatalogProduct` shape (через `mapMobileProduct`) замість thin shape. 4 files, +109/-34, tests 271/271.
- **S52** mobile FAB redesign. MessengerFab переніс з bottom-right у центр над tab bar (overlay), з ring border #fff і shadow для notched look. Unread badge з `useChatUnread()` (cap 9+). 4-tab pop-to-root regression fix через `tabPress` listener helper для всіх tabs (Головна/Пошук/Кошик/Ще). 292/292 tests baseline.
- **S53** mobile home content expansion (4 нові секції після newArrivals). Backend extends `/api/mobile/home` з `videoReviews` (8 продуктів з videoUrl) + `categories` (top-level з product count). Components: `CategoriesCarousel` (icon tile picker → catalog filtered), `useRecentlyViewed` hook (SecureStore key `mobile.recently_viewed_v1`, cap 12, addItem на ProductScreen mount), `TestimonialsCarousel` (5 mock відгуків з `lib/testimonials.ts`). HomeScreen wires всі 9 секцій. 292/292 tests.
- **S54** EAS Build preview APK setup. `eas.json` з 3 profiles (development/preview/production), `app.json` додав `runtimeVersion: { policy: "sdkVersion" }` + `updates.url` placeholder + `extra.eas.projectId` placeholder. Worker створив icon.png/splash.png/adaptive-icon.png placeholders (Sharp). Документація `docs/EAS_BUILD.md` для user setup. **Real projectId**: `83900d4b-6b2a-4d7c-bb63-562bb96b6948` (commit 8187a5e після `eas init`). User Expo account: `ltex.lutsk.ai@gmail.com` через Google OAuth (no password — login через `EXPO_TOKEN` env var).
- **S55** mobile hotfix — Ionicons font preload (іконки зникали у нативному APK через відсутній `expo-font` preload, у Expo Go runtime їх підвантажував). Додав `expo-font` + `expo-splash-screen` у package.json, App.tsx з `Font.loadAsync(Ionicons.font)` + 3-сек failsafe timeout. Hide-on-Chat для FAB через `useNavigationState` — **REVERTED** (S55-followup) бо crashes app з "Couldn't get the navigation state" — FAB рендериться поза screen у RootNavigator, hook вимагає бути всередині screen. ChatScreen `inputBar.paddingBottom: 96` як заміну (FAB не перекриває input).
- **S56** Supabase service-role key for admin storage. Новий `lib/supabase/admin.ts` з `createServiceRoleClient()` (cached singleton, no cookies, bypass RLS). Замінив `createSupabaseAdmin` (alias на anon-key client) у banners/products actions для image upload+delete. `.env.example` додав `SUPABASE_SERVICE_ROLE_KEY`. **User-action post-deploy:** додати ключ у `apps/store/.env`, видалити legacy "Admin INSERT" RLS policy у Supabase Dashboard.
- **S57** banner redesign image-only clickable. Видалено title/subtitle/ctaLabel поля з form (DB nullable, ctaHref required), web BannerCarousel rewrite (`aspect-[16/9]`, цілий image у Link, без prev/next chevrons + dots overlays — fix desktop crop), mobile BannerCarousel — Pressable з URL routing (internal `/catalog` → `navigation.navigate("Catalog")`, /product/\* → web fallback, http(s) → Linking.openURL). Migration `20260430_banner_imageonly` — DELETE FROM banners + ALTER columns. **Hotfix `2917d09`**: homepage filter banners з `ctaHref: null` (Prisma TS не дозволяє `not: null` filter, тому `.filter()` after query). 9 files, 292/292 tests.
- **S58** product image optimization. ImageGallery rewrite з raw `<img>` на `next/image fill` (main `aspect-[4/3]` + sizes `(max-width:1024px) 100vw, 50vw` + priority на selectedIndex===0; thumbs `relative h-16 w-16` + sizes `64px` + lazy; lightbox `fill object-contain` quality=90). Додано z-10/z-20 + pointer-events-none на overlay щоб chevron clicks не блокувалися (next/image fill теж absolute). Server-side sharp pipeline у `uploadProductImage` (1920×1920 inside-fit, webp q82, EXIF rotate) + `uploadBannerImage` (2400×1350, q85). `sharp` переїхав з devDeps у dependencies (runtime use). 5 files, +80/-39, 292/292 tests, build green. **Effect:** product з 13 фото на mobile тепер ~2 МБ замість ~50 МБ + zero CLS. **No backfill** — існуючі фото лишаються original, але рендеряться через `_next/image` оптимізатор автоматично.
- **S59** product card redesign per `docs/MOCKUP_S59_PRODUCT_CARD.html`. Migration `20260502_product_attrs_lot_optional`: 4 нові nullable text-поля у Product (`gender` / `sizes` / `unitsPerKg` / `unitWeight`) + OrderItem.lotId + CartItem.lotId стали nullable з `ON DELETE SET NULL` + dropped `@@unique([cartId, lotId])` (dedup на app-level). Cart дозволяє items без lotId — клієнт може додати "позицію загалом" (менеджер обере вільний лот) АБО конкретний лот через "Огляди лотів" блок. New components: `LotReviews` (replaces "Доступні лоти" — video thumb 16:9 з YouTube `i.ytimg.com/vi/{id}/hqdefault.jpg` + play overlay або "Огляд скоро" placeholder, ціна у грн з `(€)` дрібним сірим, per-lot AddToCart), `RecentReviewsCarousel` (12 продуктів videoUrl != null orderBy updatedAt desc, exclude current; стрілка "Усі огляди на YouTube →" з `NEXT_PUBLIC_YOUTUBE_PLAYLIST_URL` env, fallback `@LTEX`), `ShareIcons` (icon-only Copy/Telegram/Viber/Facebook/WhatsApp з brand colors + tooltip; replace ShareButtons), `TrustBadge` ("Усі фото є оригінальними"), `AddProductToCartButton` (general product без lotId). Helpers: `lib/exchange-rate.ts::getCurrentRate()` (cache via React `cache()`, fallback rate=43), `lib/youtube.ts::extractYouTubeId` (3 URL формати, перенесено з video-reviews.ts з re-export для backward-compat). Email template `sendOrderConfirmationEmail` — 2 секції ("Конкретні лоти" з barcode vs "Загальні позиції"). StockIndicator: тільки "В наявності" (зелений) АБО "Очікуємо надходження" (бурштиновий) — без кількості лотів/кг. KeyFactsList: 8 рядків з ✔, render тільки коли поле != null. Admin form: inputs для 4 нових полів. 29 files, +1412/-530, 284 tests pass, typecheck/build green. **⚠️ Перед deploy:** `pnpm --filter @ltex/db exec prisma migrate deploy` на local DB. Worker не міг запустити migrate (sandbox без PostgreSQL).
- **S60** product card + cart fixes (4 issues від QA). Видалено дубль `WishlistButton` на product page (лишилась тільки біля CTA). Mobile layout — `min-w-0` на обох grid children у `lg:grid-cols-2` (фікс zoom-out на mobile при багатьох фото — корінь у CSS Grid default `min-width:auto`). Cart: `AddProductToCartButton` тепер зберігає `priceEur = perKg * weight` (total, як у лотах) — це фіксує інконсистентність з `lot.priceEur` (який є total). `cart/page.tsx` розділено на server wrapper (читає `getCurrentRate()`) + client `cart-client.tsx`; per-line UAH primary + EUR дрібно сірим, items без barcode мають префікс "≈" з tooltip "Розраховано на середню вагу — менеджер уточнить", sidebar total UAH крупно. New `VideoModal` (client, `@ltex/ui` Dialog + iframe `youtube.com/embed/{id}?autoplay=1&rel=0`, `{open && <iframe>}` для unmount при закритті щоб зупинити відео), `LotReviewCard` винесено у client component для useState modal open. CSP уже дозволяє `frame-src` YouTube. 9 files, +491/-409, 309 tests, build green.
- **S61** lots page redesign per `docs/MOCKUP_S61_LOTS_PAGE.html`. `/lots` rewrite з таблиці на catalog-style grid: sidebar з фільтрами (status, hasVideo, category з counts, quality, season, country, weight range, priceEur range), search, sort (newest/priceAsc/priceDesc/weightDesc), pagination 30/page, mobile bottom-sheet через спільний `LotsFiltersForm` (DRY desktop sidebar + mobile sheet). New `LotCard` (client, video thumb 16:9 зверху + info під ним, click play → `VideoModal`, in-cart state через `useCart()`, status badges Вільний/Акція −X%/Зарезервований/Продано, шт/пар per `priceUnit`, salePercent з `prices.wholesale` vs `prices.akciya`). New `/lot/[barcode]` detail page: `LotVideoPlayer` (click thumbnail → swap у inline iframe in-place — без модалки на hero), KeyFactsList ✔ 8 рядків (Вага лота / К-сть / Сорт / Сезон / Стать / Розміри / Країна / Категорія), CTA "Додати лот", info-блок з лінком на product page, "Інші лоти цього товару" 6×LotCard, ShareIcons. URL `/lot/{barcode}` — `encodeURIComponent` defensively на href. Server-side render (no `/api/lots` endpoint). Merge S60+S61 conflict у `video-modal.tsx` — лишили S61 версію (`{open && <iframe>}` стопає відео при закритті, `shadow-2xl`, `accelerometer` removed). 11 files, +1500ish, 301 tests.
- **S62** lots page polish + quick order (7 issues від QA). Apply button для price/weight ranges (замість onBlur — клас UX win). Sidebar `max-h-[calc(100vh-6rem)] overflow-y-auto` — окремий scroll від сторінки. Mobile sheet — прибрана окрема "Застосувати" кнопка (унифіковано з desktop через ту саму кнопку у формі), додано X close у header. Status filter: 3 radio → 4 checkbox multi-select (Заброньовані / Вільні / Акції / Новинки=isNew → createdAt ≥ 14 днів). Default коли пусто: `["free","on_sale"]`. LotCard додає "NEW" badge якщо createdAt у 14d window. Видалено фільтр "Тільки з відеооглядом" (всі лоти мають video у проді) + URL handling + backend filter. List/grid layout toggle через існуючий `CatalogLayoutToggle` (URL `?layout=list|grid`). LotCard `layout="list"` — horizontal flex з video thumb `w-48 h-28` + info справа з `mt-auto` для CTA внизу. Quick order: secondary CTA "⚡ Купити в один клік" на LotCard (тільки free/on_sale), новий `QuickOrderModal` client component (name+phone form, success state), новий `/api/quick-order` endpoint (Zod validate + rate limit 3/min + lot status guard 409 + Customer findFirst+create бо phone non-unique + email/Telegram fire-and-forget). 9 files, +842/-185, 335 tests pass. **Hotfixes:** `a4608cc` — price filter інтерпретує input як UAH (конвертує у EUR через `getCurrentRate()` перед prisma `where.priceEur`, label "Ціна лота, ₴", chip `X–Y ₴`). `a095807` — категорії винесено з sidebar у horizontal pills `LotsCategoryPills` під H1 (catalog parity), "Усі категорії" reset, multi-select toggle через URL `?categoryId=A,B`.
- **S63** lots mobile fixes + wishlist для лотів (4 issues). `CatalogLayoutToggle` додано `type="button"` — раніше дефолт `submit` ламав mobile toggle коли був усередині `<form>` з кнопкою "Шукати" (форма submit-ила без layout-параметра). "Купити в один клік" → "Замовити в один клік" у LotCard CTA + QuickOrderModal title. LotCard додає "Детальніше" кнопку поряд з "Додати" (flex-1 ширша, white-bg + border-green styled, Link до `/lot/{barcode}`, завжди видима навіть для reserved/sold). Wishlist розширено discriminator-ом `kind: "product" \| "lot"` — `WishlistItem` тепер має optional lotId/barcode/weight/quantity/videoUrl. API: `hasProduct(productId)`, `hasLot(lotId)`, унифікований ключ `lot-${id}` чи `product-${id}`. WishlistButton extended з `kind` prop (default "product" для backward-compat). LotCard heart top-right на video thumb (NEW badge перенесено під status badge). `/wishlist` page — 2 секції "Збережені товари" + "Збережені лоти". Legacy localStorage entries normalize-яться як `kind: "product"` на load. 9 files, +490/-102, 320 tests pass.
- **S64** pre-launch hardening batch (4 ізольовані P2/P3 items, security/reliability). `lib/rate-limit.ts::getClientIp()` тепер пріоритезує `cf-connecting-ip` (Cloudflare trusted) → `x-real-ip` → `x-forwarded-for` (last resort). Закриває обхід rate-limit через підмінений XFF. `instrumentation.ts::validateProductionSecrets()` додає check для `TELEGRAM_WEBHOOK_SECRET` (≥16 chars) — fail-fast at boot коли `TELEGRAM_BOT_TOKEN` присутній. Console PII audit: 17 fixes у 9 файлах (`lib/email.ts`, `lib/notifications.ts`, `lib/push.ts`, `app/api/admin/chat/reply`, `app/api/mobile/shipments`, `app/api/newsletter`, `app/api/quick-order`, `app/api/telegram/webhook`, `app/api/viber/webhook`) — заміна template strings з PII (phone/email/customer name) на structured `console.<lvl>("[L-TEX] msg", { orderId, error.message })`. `lib/email.ts::sendWithRetry` — 3 attempts з exp backoff 0/2/6s, retry **тільки** на transient (`AbortError`/`ETIMEDOUT`/`ECONNRESET`/`ECONNREFUSED`/network/5xx), НЕ retry на 4xx. На final failure — structured log з `maskEmail(to)`. `sendViaResend` тепер throws on non-OK status (раніше тиха втрата). 14 files, +437/-55, 344 tests pass.
- **S65** CI/CD + tooling cleanup (P3, pure tooling). CI E2E job: прибрано `continue-on-error: true` + замінено на `if: env.HAS_DB == 'true'` (skip-якщо-нема-DATABASE_URL secret замість always-pass). Новий `.github/dependabot.yml` — weekly npm + GitHub Actions, групи next-react/prisma/dev, ignore-major на Next/React/Prisma. CI: новий step `pnpm audit --audit-level high --prod` (soft-fail, перед Build). Видалений `netlify.toml` (deprecated, Netlify Site лишається у dashboard як cold fallback). Нові docs: `docs/BUNDLE_BASELINE.md` (snapshot bundle sizes для майбутніх порівнянь — alarm при First Load JS shared > 130 kB; зараз 102 kB), `docs/SUPABASE_DB_DECOUPLING.md` (документує що Supabase DB = cold backup, Auth+Storage active, перелік migrations не-у-Supabase, re-activate procedure, dual-write code підтверджено відсутній). 5 files, +249/-62, 369 tests pass.
- **S66** 1C sync catalog + lots upload. `syncProductSchema` extended з 4 nullable text-полями S59 (`gender` max 50 / `sizes` max 100 / `unitsPerKg` max 50 / `unitWeight` max 50). New endpoint `POST /api/sync/categories` — bulk upsert by slug, 2-pass parent hierarchy resolution (1С може надіслати child перед parent у одному батчі), `revalidatePath("/catalog","layout")`, SyncLog audit. New `POST /api/sync/prices` — upsert Price by (productId, priceType, validFrom), accepts `wholesale`/`akciya`/інші priceTypes, `revalidatePath /catalog + /lots`, SyncLog. Auth pattern як інші sync endpoints: `Bearer ${SYNC_API_KEY}`, rate limit 10/min/IP, Zod validation з `details: issues.slice(0,5)`. New `docs/1C_SYNC_GUIDE.md` (376 рядків ukr) — повна документація для 1С-розробника з real JSON examples, sync order, errors table, PowerShell test commands. Tests `lib/validations.test.ts` +180 рядків. 6 files, +809, 393 tests pass (368 store + 25 shared). DB schema untouched (S59 поля вже є). Категорії/ціни sync **не у плані обміну `ОбменССайтомТоварами`** — 1С-розробник або додає у Content.xml або робить через бічні procedures, документ покриває обидва шляхи.
- **S67** 1C → site orders import endpoint. New `POST /api/sync/orders/import` — upsert by `Order.code1C` (унікальний у схемі), customer findFirst by code1C/phone або create (як `/api/quick-order` бо `Customer.phone` non-unique). Items mapping: `barcode?` → знайти Lot, `productCode1C` → знайти Product. SyncLog entity `"order"`, `entityId: code1C`. revalidatePath `/admin/orders` + `/admin`. **НЕ шле email** при імпорті (це mirror з 1С, не нова подія). **НЕ змінює lot.status** (1С відстежує reservation окремо). 4 files, +561/-7, 408 tests pass (383 store + 25 shared). Закриває двосторонній план обміну `ОбменССайтомЗаказами` — тепер 1С-створені замовлення (наприклад телефонні) можуть з'явитись у `/admin/orders`. Documentation у `docs/1C_SYNC_GUIDE.md` extended з real JSON example + recommended workflow.
- **S68** PM2 log rotation setup (P1 ops). New `scripts/setup-pm2-logrotate.ps1` (ASCII-only, idempotent — `pm2 install pm2-logrotate` + 5 `pm2 set` calls для max_size 10M / retain 14 / compress true / dateFormat / rotateInterval daily). New `docs/PM2_LOGS.md` (185 рядків ukr) — як запустити setup, кожен env var, troubleshooting, disk envelope ~50-70 МБ steady-state. Існуючий `ecosystem.config.js` (fork mode, `E:\ltex-logs\store-{out,error}.log`) залишається unchanged. Закриває P1 #15 — без ротації диск заповнювався б за ~1 місяць traffic-у. **User-action post-merge:** запустити `cd E:\ltex-ecosystem; .\scripts\setup-pm2-logrotate.ps1` один раз на сервері (idempotent — повторні запуски OK). 2 files, +229.
- **S69** revalidatePath cleanup audit (P3, audit-only). Audit-документ `docs/SESSION_69_REVALIDATE_AUDIT.md` (205 рядків) класифікує усі 49 call sites на 4 категорії: 12 OK (taргет на реальний UI), 1 OK_KEEP (promo stripe), 22 OK_NOOP (на force-dynamic admin pages — defensive doc), 0 TOO_BROAD, 5 MISSING. **Висновок:** PROJECT_AUDIT §8.1 #6 концерн застарілий — S33-S34 homepage redesign легітимізував усі `revalidatePath("/")` (banners/featured/promo дійсно на `/`). Жодного refactor не зроблено (per spec hard rule "якщо вагаєшся → keep"). 5 MISSING gaps (single product/lot/category admin edits НЕ revalidate-ять `/product/[slug]`, `/catalog`, `/lots`) — задокументовано як follow-up session candidate. 1 file, +205. 408 tests pass.
- **S70** email persistent DLQ (P3, reliability). Migration `20260506_email_job` — нова `email_jobs` table (id, to/subject/htmlBody/textBody, source, referenceId, status pending/retrying/sent/failed, attempts, maxAttempts=5, nextAttemptAt, lastError, sent_at) + 2 indexes (status+next_attempt_at для cron, source+created для admin filter). Additive only, без зміни існуючої схеми. `lib/email.ts` refactor: новий `enqueueEmail(payload, source, referenceId)` (fire-and-forget create EmailJob) + `processEmailQueue()` (SELECT 50 pending/retrying WHERE next_attempt_at ≤ now → sendMail → on success status=sent + sentAt, on failure attempts++ + exp backoff: 1м/5м/30м/2год/6год → status=failed коли attempts ≥ max). Existing callers (sendOrderConfirmationEmail тощо) signature-compatible — внутрішньо тепер enqueue замість direct sendMail. Lazy retry — 4xx теж ретраяться (SPF cache, rate ban — задокументовано у EMAIL_QUEUE.md). New `/api/cron/process-email-queue` (auth via `x-cron-secret` header). New `/admin/emails` page — list з фільтром по status, retry now action для failed, mask `to`. `/api/admin/stats` extended з `emailQueueCount` для бейджа в admin sidebar (reuse 30s polling). New `docs/EMAIL_QUEUE.md` (252 рядки) з Windows Task Scheduler setup. 12 files, +1199/-41, 425 tests pass (400 store + 25 shared). **⚠️ Перед deploy:** `prisma migrate deploy` на local DB + додати `CRON_SECRET` у `apps/store/.env` (можна reuse той що для `/api/cron/cleanup-viewlog`) + одноразово створити Windows Scheduled Task per `docs/EMAIL_QUEUE.md`.
- **S71** bulk catalog import з Excel (`Повний каталог товарів.xlsx` → DB). Constants extension у 4 файлах: 18 нових subcategories (Мікс, Спортивний/Робочий/Лижний одяг, Шкарпетки, Лосини, Колготки, Спец-одяг, Вітровки/штормовки, Сукні+спідниці merge, Гумове/Робоче/Спортивне взуття, Головні убори, Рукавиці, Пряжа, AGD), 3 combined quality (`extra_first`, `extra_cream`, `first_second`), 2 нові країни (`scotland`, `usa`), сезон `all_season`. Pure parsers у `@ltex/shared/utils/import-catalog.ts` (parseNomenklatura/parseDescription/classifyToken/parseCategoryCell + 60+ слов CATEGORY_SLUG_MAP + 11 SKU_CATEGORY_OVERRIDE для Sheet 3 specials + 8 CATEGORY_MIGRATIONS deprecated→target). New `scripts/import-catalog-from-excel.ts` (918 рядків, 6-phase pipeline: read → parse → category reconcile → product upsert → deletion audit → markdown report) + dry-run by default + `--apply` for writes + graceful degradation коли DATABASE_URL відсутній. Decisions джерело — `docs/CATALOG_IMPORT_PLAN.md` (юзер заповнив `Catalog_Import_Questions.xlsx` 6-sheet анкету з 5 категоріями рішень). Operations runbook `docs/CATALOG_IMPORT_OPERATIONS.md`. 11 files, +2197/-23, 449 tests (49 нових parser tests). **Production apply (2026-05-07):** 11 CREATE + 697 UPDATE + 104 DELETE (з 107 — 3 FK-blocked: `C4C/D2D/PC4C TT UK ORL` мають Lot.productId з Restrict cascade, не критично — вирішити окремо). DEPRECATED migrate: 35 продуктів (31 `dytiachyi-odyag` → `inshe-odyag` + знаходитимуться через filter `gender=Дитяча`, 3 `sukni` → `sukni-spidnytsi`, 1 `kombinezony` → `inshe-odyag`, інші 4 deprecated мали 0 продуктів). Skipped: 1 (`L.MIX Crivit Football #5`). **Hotfix після deploy:** `TELEGRAM_WEBHOOK_SECRET` був не виставлений у `apps/store/.env` (S64 додав fail-fast перевірку, але env var не був доданий після S64 merge — gap orchestrator-а; site впав з `unhandledRejection: TELEGRAM_WEBHOOK_SECRET must be at least 16 characters`). Згенерували 48-hex secret через PowerShell `RandomNumberGenerator`, додали у `.env`, скопіювали у standalone, `pm2 restart --update-env`, зареєстрували Telegram webhook з новим secret через `/setWebhook` API. Сайт онлайн з оновленим каталогом.
- **M1.0** Manager Workstation strategy. Новий продукт-трек: окрема програма для менеджерів L-TEX (замінює 1С `MobileAgentLTEX` v1.15.3, Android-only, SOAP-bound). Web-first під `apps/store/app/manager/*`, далі Tauri 2 wrap для Windows + macOS (M1.11). 4 архітектурні рішення зафіксовані з user-ом (2026-05-12): self-hosted auth БЕЗ Supabase (own `users` table + bcryptjs + HMAC JWT), hybrid 1C integration (read snapshot via `services/manager-sync/`, write SOAP-proxy з idempotency-key), Tauri native push + per-manager Telegram DM (нема legacy group для менеджерів), local storage `E:\ltex-storage\`. Settings UI мінімум — викинуто всі 7 1С-toggles з `ФормаВводаПароля` бо менеджери ними не користуються; auto-defaults замість. User decisions: 5-10 менеджерів, whitelist через invite (таблиця users сама), Telegram з нуля, перший admin `ltex.lutsk.ai@gmail.com`. Docs: `docs/MANAGER_APP_STRATEGY.md` (12 секцій, 350+ рядків), `docs/M1_BACKLOG.md` (12 сесій M1.1-M1.12 + 7 V2 follow-ups + dependency graph), `docs/SESSION_M1.1_AUTH.md` (детальна worker-spec на 17 tasks). Supabase phase-out (admin login + storage) → V2 roadmap (S88-S92), не блокує M1. 3 нові docs, +1500. Orchestrator session — без коду.
- **M1.1** self-hosted manager auth. Migration `20260513_users_auth` (additive, idempotent через `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object`, `CREATE TABLE IF NOT EXISTS`): `users` + `user_refresh_tokens` + `password_reset_tokens` + `client_assignments` + enum `UserRole`. Auth library `apps/store/lib/auth/`: bcryptjs cost=12 (`password.ts`), HS256 JWT з 15хв access (`jwt.ts`), 32-byte SHA-256-hashed refresh tokens 30 днів (rotation у `/refresh`), lockout 5/15min (`lockout.ts`), `getCurrentUser`/`requireRole` helpers (`manager-auth.ts`). API під `/api/v1/manager/`: login (anti-enum — однакова 401 для no-user і wrong-password, rate-limit 10/min/IP, lockout-check), refresh (rotate з revoke old), logout (`?everywhere=true` revokes all refresh для user), me, password-reset request/confirm (one-shot tx revokes all sessions при confirm, anti-enum 202 завжди), admin users GET/POST/PATCH (admin-only guard, last-admin guard на role change). UI під `/manager/`: login/forgot/reset forms у `(auth)` group з shared layout, placeholder dashboard + admin users table з invite modal + per-row actions у `(workstation)` group. Plumbing: dual-path `middleware.ts` (manager vs admin matcher), `instrumentation.ts` fail-fast для `MANAGER_JWT_SECRET ≥32`, `EmailSource` extended з `manager-auth`, `.env.example` doc, `apps/store/scripts/seed-admin-user.ts` для першого admin. Tests: 59 new auth + 2 instrumentation = 578/580 store passing (2 pre-existing skipped). Hard rules upheld: 0 `@supabase/*` imports у new files, `/admin/*` flow untouched, mobile customer auth untouched, no plaintext passwords leaked, refresh tokens stored as SHA-256 hashes only. 48 files, +3499/-15. **⚠️ Перед deploy:** (1) `openssl rand -base64 48` → додати `MANAGER_JWT_SECRET` у `apps/store/.env`; (2) `pnpm --filter @ltex/db exec prisma migrate deploy`; (3) seed першого admin (`SEED_ADMIN_EMAIL=ltex.lutsk.ai@gmail.com SEED_ADMIN_PASSWORD=<12+chars> SEED_ADMIN_NAME="Адміністратор L-TEX" pnpm --filter @ltex/store exec tsx scripts/seed-admin-user.ts`); (4) `pm2 restart ltex-store --update-env`. Після — admin логіниться на `/manager`, через `/manager/admin/users` запрошує менеджерів (вони отримують invite-email з reset-link, expiry 7 днів).
- **M1.1.1** hotfix Node.js middleware runtime. Симптом після M1.1 deploy: `/manager/login` повертала body=0 bytes (порожня сторінка), PM2 logs чисті. Корінь: `middleware-manager.ts → lib/auth/jwt.ts` імпортує `node:crypto` (`createHmac`/`randomBytes`/`createHash`/`timingSafeEqual`); Next.js за замовчуванням компілює middleware у Edge Runtime де ці APIs undefined → `verifyAccessToken()` падає тихо, middleware повертає implicit empty response. Build warning ("A Node.js module is loaded ('crypto') which is not supported in the Edge Runtime") був видимий але не блокуючий. Fix one-liner у `apps/store/middleware.ts` (`export const runtime = "nodejs"`) + `experimental.nodeMiddleware: true` у `next.config.js` (потрібно у Next.js 15.5, stable у 16). Жодного коду не зачеплено — тільки runtime config. Admin /admin/\* Supabase middleware працює у Node.js runtime теж. 2 files, +12. Альтернатива (Web Crypto API rewrite jwt.ts) відкладена як V2 follow-up.
- **M1.3a** Manager clients — read-only список + детальна картка. Migration `20260513_mgr_clients` (idempotent, FK upgrade на `client_assignments.customer_id` обернутий у `DO BEGIN ... EXCEPTION`): 6 dictionary tables (`mgr_client_statuses`/`mgr_search_channels`/`mgr_categories_tt`/`mgr_delivery_methods`/`mgr_assortment_codes`/`mgr_routes`) + `mgr_clients` (30+ полів mapped з 1С `Catalog.Контрагенты` через `docs/1c-export-mobile/`) + 6 child tables (phones/messengers/warehouses/route-assignments/assortment/timeline). 5 API endpoints під `/api/v1/manager/`: GET /clients (8 filter params: search/status/channel/deliveryMethod/hasDebt/hasOverpayment/onlyMine/hideTrash + paging 10-100), GET /clients/[id] (full картка), GET+POST /clients/[id]/timeline (paginated 50/page + manual comment з authorUserId), PATCH /clients/[id]/assign (admin-only через `requireRole(["admin"])`, transactional delete+create), GET /dictionaries (cached 60s, 6 arrays). UI `/manager/customers`: 7-колонкова таблиця (Клієнт+phone+місто / Борг / Статус / Канал / Днів / Остання покупка / Менеджер), inactive=red row підсвічування, URL-state toolbar з search + 3 select + bool toggles, Sheet з advanced filters, pagination. `/manager/customers/[id]`: header з статусами+боргом+admin-assign button, 5 tabs (Реквізити / Історія з comment form / Маршрути / Асортимент / Замовлення→`<UnderConstruction session="M1.5">`). Trash filter через `startsWith` 7 prefixes (`1111`/`2222`/`.../9999`) — Prisma не має regex, прагматично. Seed `scripts/seed-mgr-test-data.ts` — 6 dicts + 10 фейкових клієнтів з timeline, idempotent skip-якщо-`mgr_clients` не порожнє (safeguard для production). Dashboard stats: real `_sum: { debt }` aggregate замість TODO=0. 23 нові тести (5 routes ≥3 per file). 41 file, +4083/-16, tests 677 (620 store + 57 shared) passing. Hard rules upheld: 0 нових `any`, 0 `@supabase/*` у новому коді, `/admin/*` web admin не торкнуто, M1.1+M1.2 endpoints не змінено. Конфіг 1С прочитано перед першим commit: `Catalogs/Контрагенты.xml` + `Forms/ФормаЭлемента/Ext/Form.xml` + `Central/CommonModules/ОбменАРМ/Ext/Module.bsl` дали точне field-mapping. **⚠️ Перед deploy:** (1) `pnpm --filter @ltex/db exec prisma migrate deploy`; (2) `pnpm --filter @ltex/store exec tsx scripts/seed-mgr-test-data.ts` для фейкових даних (real SOAP-sync у M1.5+).
- **M1.3f** foreign client visibility + contact masking (закриває блок Клієнти). Server-enforced ownership + masking. `@ltex/shared maskPhone`: `+380501234567` → `*** *** *** 567`, invalid → null. `lib/manager/client-visibility.ts`: getViewerOwnership (admin/mine/foreign) / getOwnedClientIds (Set or null для admin) / ownershipWhere (Prisma WHERE) / maskClientForForeign (phones masked, messengers/bankAccounts/reminders/presentations/timeline → [], websiteUrl/geolocation/viberContact → null). GET `/clients` — forced ownership для manager (URL `mineOnly=false` ignored); admin opt-in через `?mineOnly=true`. GET `/clients/[id]` — apply masking коли foreign + returns viewerOwnership field. New `GET /clients/search-all` — picker з minimal shape + `isOwned: bool` для M1.5+ document flows. UI: `ClientForeignBanner` ("⚠ Чужий клієнт. Призначений: X"), `client-tabs` isForeign prop → 4/11 visible (Реквізити/Асортимент/Іст. продаж/Замовлення), deeplink fallback на requisites, `client-contact-row` masked prop (lock icon без tel/viber/wa actions), edit-toggle disable для foreign, list toolbar — `showOnlyMineToggle` прихований для manager-а. Hard rules upheld: server-side masking (NOT UI hide), canEditClient (M1.3d) без змін — foreign PATCH = 403. Tests +40: maskPhone (4) + client-visibility (17) + clients GET (4) + clients/[id] (4) + search-all (6) + client-tabs (3) + UI integration. 905 tests passing. 26 files, +1230/-111. No DB migrations, no env vars, pure application-layer. **Блок Клієнти закритий.** **Next:** M1.5 SOAP write-back / M1.6 Презентації / M1.7 Прайс / M1.8 чат-боти.
- **M1.4** Замовлення — Tab у картці + global page (read-only). Replace stub UnderConstruction на real список. Backend `lib/manager/`: `order-ownership.ts` (getMyClientCodes1C: admin→null/manager→свої codes; canViewOrder helper) + `order-status.ts` (6-status mapping: draft/pending/approved/shipped/delivered/cancelled → label+color). 3 API endpoints: GET `/clients/[id]/orders` (пагінація 10/page, JOIN через `MgrClient.code1C === Customer.code1C` БЕЗ FK refactor), GET `/orders` (global з ownership filter + search/status/date range, пагінація 20/page), GET `/orders/[id]` (single read з canViewOrder). Dashboard stats — real `sessionCounts.orders` замість hardcode 0. UI Tab Замовлення: real list з top-10 + "Показати всі →" link на global page + "+ Створити" stub toast "M1.5". UI Global `/manager/orders`: server-side page + orders-toolbar (search + status select + date range) + orders-table/row + orders-filter-state (URL ↔ state). `/manager/orders/[id]`: read-only detail з UnderConstruction для редагування. Shared: new `EmptyState` reusable. Permission locked: admin усі / manager тільки свої клієнти (agentUserId OR ClientAssignment); cross-manager picker → M1.3f. Tests +48: ownership (4) + status (2) + 3 routes (14) + dashboard (1) + filter-state (4). Total 782 store + 83 shared = 865 passing. 24 files, +1955/-18. No DB migrations, no env vars. **Next:** M1.3f visibility (foreign client masking — фінал блоку Клієнти).
- **M1.3e** розширені фільтри клієнтів + customizable view prefs. **Part A** — фільтри 8→25 params: multi-select FK через CSV URL (`?statusId=a,b,c`) для operational status / category TT / price type / assortment / route / agent; text LIKE для region/city/dialogStatus; числові range (debt/overdueDebt/monthlyVolume/daysSinceLast); дати (licenseExpiresBefore/createdRange); bool (hasNewMessage/isViberLinked). Back-compat — старі URLs (`?status=code`, `?hasDebt=true`) як fallback. **Part B** — `MgrUserViewPrefs` (migration `20260514_user_view_prefs` additive idempotent): `(userId, viewKey)` unique з config JSONB. `lib/manager/view-defaults.ts` з 22 column keys + 25 filter keys + `mergePrefs()` (drop unknown / dedup / auto-append missing). API GET/PUT/DELETE `/api/v1/manager/me/view-prefs/[viewKey]` з whitelist validation у PUT (Zod + `allKeysSet` check — guard від injection, 400 reject для unknown keys). UI: `ViewCustomizerSheet` (2-tab Колонки/Фільтри) + `ViewCustomizerList` (▲▼ buttons + checkbox + renumber order) + dynamic filter sheet (render тільки visible keys у order) + table з dynamic columns через `renderCell(key, client)` + toolbar редизайн (Search + Фільтри(N) badge + Тільки мої + Налаштувати). Filter controls: SelectMulti (chip dropdown) / RangeNumeric / RangeDate / DateBefore / TextFilter / BoolFilter (3-state). Page.tsx — server-side Promise.all для 4 джерел даних. Tests +56: view-defaults (7) + view-prefs API (12) + extended clients (12) + filter controls (10) + clients-filter-state (10) + view-customizer (5). 734 store + 83 shared = 817 passing. Constraints upheld: no DnD library (up/down arrows only), URL backward compat, whitelist guard. 31 files, +3129/-294. **⚠ Post-deploy:** `prisma migrate deploy`. Seed не потрібен — `mgr_user_view_prefs` поступово заповниться. **Next:** M1.4 (вкладка Замовлення — read списку з 1С snapshot).
- **M1.5.1** post-reboot recovery + ops fix + M1.2-M1.5b production deploy. Trigger: user вручну зробив restart+power-off сервера через SystemSettingsAdminFlows.exe (Event ID 1074), що поклало одночасно RDP і веб (502). Recovery (диктувалось з cloud-сесії): (1) RDP firewall — Windows Update перемкнув `NetworkCategory` на `Public`, всі правила групи "Remote Desktop" вимкнулись → `Set-NetConnectionProfile Private` + `Enable-NetFirewallRule -DisplayGroup "Remote Desktop"`; (2) NIC IP — DHCP lease втрачена, видало `.146` замість `.10`, port-forwards на роутері повисли → конвертовано на OS-level static `192.168.0.10` (Manual prefix origin, Dhcp Disabled, через `New-NetIPAddress` + `Set-DnsClientServerAddress`), 1С обміни з філій (1540/1541/1560/1561) відновились автоматично; (3) `pm2 resurrect` підняв store вручну бо `PM2 Resurrect` Scheduled Task не спрацювала — задокументовано як follow-up. Виявлений код-bug: `services/manager-sync/src/index.ts` не імпортує dotenv, package.json не має dotenv у deps, `ecosystem.config.js` передає тільки `NODE_ENV` → manager-sync crash-loop з `ZodError: MANAGER_SYNC_SHARED_SECRET undefined`. Worker session `claude/m1.5-ops-fix` (merge `9bcc4f9`): додав `import "dotenv/config";` як перший import у index.ts + `dotenv@^16.4.0` у dependencies (resolves 16.6.1), pnpm-lock.yaml sync, звіт `docs/SESSION_M1.5_OPS_FIX_REPORT.md` (174 рядки) з ops runbook + follow-ups. Pre-merge: 1050/1050 tests pass (87 shared + 37 manager-sync + 926 store), `pnpm format:check` ❌ але pre-existing 781-файл drift (не регресія, окремий follow-up). Виявлено що production build був `2026-05-13` (M1.1 deploy), тобто M1.2 + M1.3a/c/d/e/f + M1.4 + M1.5 + M1.5b жили у git але не у проді — повний `.\scripts\deploy.ps1` (28s build, 78 routes, ~150 endpoints, ~1 min downtime). Виявлений migration bug у `20260515_sync_jobs/migration.sql`: створює PG ENUM-типи `"SyncJobStatus"`/`"SyncEntityType"` (PascalCase), але schema.prisma має `@@map("mgr_sync_job_status")` + `@@map("mgr_sync_entity_type")` (snake_case) → Prisma runtime falls з `code "42704" type "public.mgr_sync_job_status" does not exist` навіть коли `prisma migrate status` каже applied. Production hot-patch через `prisma db execute --file <tmp.sql> --schema prisma\schema.prisma` (UTF-8 без BOM через `[System.IO.File]::WriteAllText`): DROP старих PascalCase types + table, CREATE правильних snake_case names + перестворення `mgr_sync_jobs` (таблиця була порожня, втрат немає). Endpoint `/api/cron/process-sync-queue` тепер 200 OK + `{processed:0,...}`. `CRON_SECRET` з літеральними `<...=>` + лапками у `apps/store/.env` підтверджено робочий: `dotenv` парсер знімає тільки парні зовнішні `"..."`, тому `process.env.CRON_SECRET` = `<nDWvCcKr/vZFPid2nPvPASIjjZ1sbu/eTrVKcuIM2Ug=>` (з кутами, без зовнішніх лапок). Windows Scheduled Task `LTEX Process Sync Queue` створена (1-min interval, curl.exe -H "x-cron-secret: ..." → endpoint, SYSTEM user, LastTaskResult=0). PM2 dump.pm2 збережений з обома процесами. **Follow-ups (open):** (a) committed migration `20260518_fix_sync_jobs_enum_names` у git щоб майбутні чисті installations не зламались (original `20260515_sync_jobs/migration.sql` досі buggy); (b) `CRON_SECRET <...>` cosmetic cleanup (maintenance window — rotate + перереєструвати 3 Tasks одночасно); (c) `PM2 Resurrect` Task діагностика чому не спрацювала після reboot; (d) repo-wide `prettier --write` на 781 файл. 1 file (CLAUDE.md), без коду крім worker branch.
- **M1.3d** inline editing полів картки. Tab Реквізити з toggle Редагувати/Скасувати/Зберегти. 25 editable + 9 read-only scalar полів (tabular phones/banks/etc. лишаються read-only до follow-up). Backend: Zod schema `validations/mgr-client.ts` (10 tests) + permission helper `permissions/mgr-client-edit.ts` (5 tests: admin завжди / manager — `agentUserId === user.id` OR assigned через ClientAssignment) + PATCH `/api/v1/manager/clients/[id]` (11 tests). agentUserId admin-only guard (UI `disabled` select + server 403). `dictionaries/route.ts` extended з `priceTypes` для FK selects. UI: 6 edit controls (text/number/date/bool/select/readonly), `useClientEdit` з diff (7 tests), `useDiscardWarning` (2 tests), view/edit/toggle split, edit form 25 полів у 1С-order. **Conflict policy (locked):** і менеджер і 1С редагують усі поля, last-write-wins; жодного field-level tracking — sync (M1.5) розбереться сам. 27 files, +2202/-505, 679 store + 83 shared = 762 tests passing. No DB migrations, no env vars — pure UI/API. **Next:** M1.3e розширені фільтри + customizable columns.
- **M1.3c** client card FULL parity з 1С (закриває gap M1.3a після перевірки скрінів). Migration `20260514_mgr_clients_full` (additive only, idempotent через `IF NOT EXISTS` + `DO $$ EXCEPTION WHEN duplicate_object`): 7 нових полів MgrClient (`tradePointName`/`tovDebt`/`tovOverdueDebt`/`agentUserId` FK User/`viberContact`/`sessionRemainder`/`priceTypeId` FK новий MgrPriceType) + 3 нові tables (`mgr_client_presentations`, `mgr_client_bank_accounts`, `mgr_reminders`) + dictionary `mgr_price_types` + extensions (`MgrClientAssortmentItem.notDirectInput` / `MgrClientMessenger.browserUrl`). 11 tabs у точному 1С порядку (Form.xml verified): Реквізити (всі 25 полів + clickable phones з 4 actions / адреса+geo → Maps deeplinks / Viber-contact deeplink / сайт → new tab) / Асортимент (+notDirectInput chip) / Презентації (real read-only) / Історія / Історія продаж (stub M1.4) / Замовлення (stub M1.5) / Нагадування (REAL CRUD з 4-bucket grouping) / Viber (stub з real viber:// buttons + опис "M1.8 chat-bot") / Банк. рахунки (real з copy-IBAN) / Іст. презентацій (stub M1.6) / Соц мережі (clickable per network). Shared utils `@ltex/shared`: `phone.ts` (normalize E.164 / formatPhoneUkr / tel/viber/wa URLs), `social-links.ts` (buildSocialUrl per network з browserUrl override + emoji icons). Reminders endpoints `/api/v1/manager/clients/[id]/reminders` GET+POST і `[rid]` PATCH+DELETE з permission `owner OR admin` (403 otherwise), 4 actions: complete/uncomplete/snooze/edit. Header bell — polling 60s, popover з top-10 overdue, click → deeplink `#reminders`. Tests +52 (з мінімуму ≥18): phone (12) + social-links (13) + reminders-grouping (6) + reminders GET/POST (7) + PATCH/DELETE (7) + notifications (2) + client detail (1). 643 store + 83 shared passing, build green. 6 файлів 1С конфіги прочитано перед першим commit (Контрагенты.xml + Forms/ФормаЭлемента/Ext/Form.xml + Module.bsl + БанковскиеСчета.xml + Нагадування.xml + ОбменАРМ/Module.bsl). 40 files, +3179/-157. **⚠️ Перед deploy:** `pnpm --filter @ltex/db exec prisma migrate deploy` + опц. truncate-and-reseed для bank accounts/presentations/reminders fake data. **Future:** Tab Viber → M1.8 (Telegram+Viber+Instagram bots з переписку у timeline). Inline editing → M1.5 (з SOAP write-back).
- **M1.5b** orders + payments sync (закрив M1.4 "+ Створити" stub + завершив operations layer M1.5). Backend: POST `/api/v1/manager/orders` (Zod schema у `validations/manager-order.ts` — customerId/items[].productId+lotId(nullable)+weight+quantity+priceEur/notes/exchangeRate, ownership check через `getMyClientCodes1C` як M1.4, transactional `order-create.ts::createOrderWithItems` обчислює totalEur=sum(items.priceEur) + totalUah=totalEur*rate з `getCurrentRate()` fallback, fire-and-forget `enqueueOrderCreate(order)` після success), POST `/api/v1/manager/payments` (Zod — orderId/method/amount/currency/externalId/paidAt, ownership через Order → Customer.code1C, create Payment з status="completed" + fire-and-forget enqueue), GET `/api/v1/manager/products/search?q=` (autocomplete з 2-char min для UI picker), GET `/api/v1/manager/products/[id]/lots` (вільні лоти для item row). Sync infra: `enqueue.ts` extended з `enqueueOrderCreate` + `enqueuePaymentCreate` (payload з product.code1C + lot.barcode — 1С business keys, Decimal-as-string serialization), `proxy-client.ts` switch розширено на order/payment entityTypes. services/manager-sync: 2 нові Fastify routes `/sync/orders/:id` + `/sync/payments/:id` (mirror sync-clients pattern, idempotency cache, mockUpdateOrder/mockUpdatePayment + real `updateOrderViaSoap`/`updatePaymentViaSoap` для production). UI `/manager/orders/new` (server page з `?clientId` query support): `order-form.tsx` (main client state), `client-picker.tsx` (debounce 300ms через `useDebouncedSearch`, dropdown з `isOwned` chip), `product-picker.tsx` + `lot-picker.tsx` (autocomplete + lot toggle radio "Конкретний лот" / "Загальна позиція"), `items-editor.tsx` + `item-row.tsx` (table з add/remove, auto-fill priceEur з lot.priceEur коли selected або product.basePrice*weight estimate коли general), `order-totals.tsx` (live EUR+UAH через client-side rate fetch). Stub replacements (Phase 9): orders global page "+ Створити" toast → `<Link href="/manager/orders/new">`; client detail tab "+ Створити" → `<Link href="/manager/orders/new?clientId=...">` (через нові `order-create-button.tsx` + `client-action-buttons.tsx`). Admin: `/admin/sync-jobs` server page з status filter (`all/pending/retrying/sent/failed`) + pagination 50/page + per-row "Retry" server-action для failed (`actions.ts::retrySyncJob` — reset status=pending, attempts=0, nextAttemptAt=now, lastError=null) + REST `/api/admin/sync-jobs` + `/[id]/retry` endpoints + admin sidebar entry. Auth via existing `requireAdmin` (Supabase, як banners/emails). 1С BSL spec extension: `docs/1C_SYNC_MODULES_SPEC.md` §3.2 `ОбновитиЗамовлення` + §3.3 `ОбновитиОплату` з повним BSL draft + JSON ПакетДанних структури + XML examples request/response + §7 status update. Tests +82: validations (12) + endpoints (22) + enqueue extensions (6) + proxy routing (2) + manager-sync routes (13) + admin sync-jobs (9) + UI RTL smoke (3) + order-create helper (5) + product search/lots (10). Total 1050/1050 passing. Hard rules upheld: 0 нових `any`, ownership server-side enforced, transactional Order+items через Prisma nested create, fire-and-forget enqueue (best-effort), mock-mode default = worker testable без живого 1С, /admin/\* Supabase auth untouched. 51 files, +4207/-98. **No DB migrations, no env vars post-merge** — тільки `git pull` + `.\scripts\deploy.ps1` + `pm2 restart ltex-manager-sync`. **Operations layer (M1.5+M1.5b) закритий.** Next: M1.6 inbound polling (1С → наша DB snapshot), або M1.7 Презентації (replace stub), або real-1С wire-up коли URL буде готовий.
- **M1.5** sync backbone — outbound write-back queue + SOAP proxy + 1С BSL spec. Перша частина двостороннього обміну (M1.5b додасть orders/payments + UI form). User decision: 1С існує але обмін ще не налаштований, worker писатиме також 1С BSL-сторону (через docs spec). New service `services/manager-sync/` — Fastify HTTP proxy (port 3001, окремий PM2 instance) з custom SOAP 1.1 envelope builder/parser (`src/soap/envelope.ts` — без зайвих deps типу `strong-soap`, namespace `http://arm_mobile` як у `MobileExchange.1cws`), mock implementation (`SYNC_MOCK_MODE=true` дефолт — симулює delay 100-500ms, повертає `{ ok: true, mockMode: true }`) + real SOAP client (production коли `ONEC_SOAP_URL` виставлено), in-memory idempotency cache (TTL 5min Map cleanup-on-access), shared-secret auth header `X-Sync-Secret`. DB: new `MgrSyncJob` model + enums `SyncJobStatus` (pending/retrying/sent/failed) + `SyncEntityType` (client/order/payment) + idempotent migration `20260515_sync_jobs` (DO BEGIN EXCEPTION для enums, IF NOT EXISTS для table/indexes), unique `idempotency_key`, indexes (status,next*attempt_at) + (entity_type,entity_id). Next.js: `lib/sync/enqueue.ts` (enqueueClientUpdate з UUID idempotencyKey + scalar payload snapshot з MgrClient), `lib/sync/queue-processor.ts` (backoff 1м/5м/30м/2г/6г, maxAttempts=5, mirror-ить email DLQ з S70, options-injection для testability), `lib/sync/proxy-client.ts` (fetch до manager-sync з 30s AbortSignal timeout). Routes: `POST /api/cron/process-sync-queue` (CRON_SECRET auth), `GET /api/v1/manager/sync/status` (pendingCount + lastSentAt для header indicator). Integration: `PATCH /clients/[id]` — fire-and-forget `enqueueClientUpdate(updated, "update").catch(warn)` на success, не блокує response. UI: `header-sync-indicator.tsx` real polling 30s replacing hardcode. Config: `ecosystem.config.js` додано `ltex-manager-sync` PM2 entry, `.env.example` оновлено з `MANAGER_SYNC*_`/`ONEC*SOAP*_`vars. Documentation:`docs/1C*SYNC_MODULES_SPEC.md`(470 рядків — повний контракт BSL для 1С-розробника або майбутньої M1.5c worker сесії: операції`ОбновитиКлієнта`/`СтворитиЗамовлення`/`СтворитиОплату`, JSON-string ПакетДанних, СинкЛог catalog для idempotency, error codes 0-4, request/response XML examples, BSL function signatures), `docs/M1.5_SYNC_ARCHITECTURE.md`(209 рядків overview). Tests +84: manager-sync (24 — envelope/mock/SOAP-client smoke з faked server/idempotency/route) + Next.js (60 — enqueue/queue-processor/proxy-client/validation/cron/status route/PATCH integration). Total 971 passing (860 store + 87 shared + 24 manager-sync). Hard rules upheld: 0`@supabase/*`у new code, mock-mode default = worker testable без живого 1С, /admin/_ untouched, customer/mobile auth untouched. 39 files, +3569/-26. **⚠️ Post-merge user actions:** (1)`pnpm install`для нових services/manager-sync deps; (2)`pnpm --filter @ltex/db exec prisma migrate deploy`; (3) додати у `apps/store/.env` `MANAGER_SYNC_URL=http://localhost:3001`+`MANAGER_SYNC_SHARED_SECRET=<openssl rand -base64 24>`(CRON_SECRET вже є з S70); (4) створити`services/manager-sync/.env`з тим самим SHARED_SECRET +`SYNC_MOCK_MODE=true`+`MANAGER_SYNC_PORT=3001`; (5) `pm2 start ecosystem.config.js && pm2 save`; (6) Windows Task Scheduler entry для cron `/api/cron/process-sync-queue`1-min інтервал. **Not done (M1.5b):** POST`/orders`+ UI form для створення замовлення (закриває M1.4 "+ Створити" stub) + POST`/payments`+ admin retry UI`/admin/sync-jobs` + real-connection wire-up з живим 1С коли URL буде готовий.
- **M1.2** Manager workstation shell. Lefthand sidebar з 13 пунктами (5 базових: Замовлення/Реалізація/Оплати/Маршрут + Робочий стіл; 5 додаткових: Клієнти/Презентації/Прайс/Нагадування/Закриття; Чат з unread badge; Користувачі (admin-only); Налаштування) — collapsible на <1024px у Sheet drawer через hamburger. Sticky header: logo + Ctrl+K search input (заглушка) + barcode input з camera-button (заглушка) + sync indicator "Синхронізовано Xс тому" + 🔔 notifications bell (поки empty dropdown) + profile menu (ПІБ, dropdown {Налаштування, Вийти}). Dashboard: greeting "Вітаємо, X!" + stats row (мої клієнти + борг) + currency row (EUR/USD з admin-only ✏️ edit modal) + 4 plate-tiles (counts хардкодом, реальні counts у M1.5+). 10 stub routes (orders/sales/payments/routes/customers/presentations/prices/reminders/closures/chat) → `<UnderConstruction session="M1.X">` компонент з прив'язкою. Settings page — повноцінна: profile (read-only email + save-on-blur fullName + change-password modal з validate ≥12chars), Telegram section (поки UI-заглушка з кнопкою "Прив'язати" → toast "Скоро у M1.10"), notify-channels (2 toggles save-on-change), active sessions table з per-row revoke + revoke-all-others + logout. 6 нових API endpoints під `/api/v1/manager/`: PATCH /me (Zod validate fullName+notifyChannels), POST /me/change-password (verify current + revoke ALL refresh on success), GET /me/sessions (list active), DELETE /me/sessions/[id] (single + current-session detection), GET /dashboard/stats (clientCount + totalDebt placeholder + EUR/USD з ExchangeRate + sync hardcode + tile counts hardcode), POST /admin/rates (admin-only Zod, upsert ExchangeRate з source="manual", `// TODO M1.3+` коментар про sync назад у 1С). 19 нових vitest (me 6 + change-password 5 + dashboard 3 + rates 5) — перевищує мінімум 12 з spec. CI: format/typecheck/test/build green; 597 store + 57 shared = 654 tests passing. Hard rules upheld: 0 нових `any`, 0 `@supabase/*` у новому коді, Prisma schema не торкнуто (reuse User/UserRefreshToken/ExchangeRate/ClientAssignment), `/admin/*` web admin не торкнуто, customer/mobile auth не торкнуто, M1.1 endpoints не змінено. 50 files, +2745/-54. **Жодних DB-міграцій + жодних env vars post-merge** — тільки `git pull` + `.\scripts\deploy.ps1`. Після — користувач бачить повний робочий стіл; усі sidebar-пункти крім Робочий стіл/Користувачі/Налаштування показують `<UnderConstruction>` з вказівкою конкретної сесії (M1.3-M1.10) яка додасть real функціонал.

### Post-S57 hotfixes (2026-04-30)

- **`d44a2d1`** — `next.config.js` `experimental.serverActions.allowedOrigins: ["new.ltex.com.ua", "ltex.com.ua", "localhost:3000"]`. Cloudflare Tunnel робить `Host: localhost:3000` але browser шле `Origin: https://new.ltex.com.ua` → дефолтний Server Actions security check рейлив 403 на ВСІ POST до server actions.
- **`e2d0e39`** — `scripts/deploy.ps1` `pm2 kill` обернув у try/catch + `2>&1 | Out-Null`. `$ErrorActionPreference = "Stop"` робив warning "[PM2][WARN] No process found" terminating error → script aborts після [3/8] якщо PM2 уже мертвий від попередньої невдалої спроби.
- **`a9b61a9`** + **`caeb5d0`** + **`c65beaa`** — mobile App.tsx defensive boot: failsafe timeout, потім remove ready gate + ErrorBoundary з visible error message + видалити `useNavigationState` з MessengerFab (це і був корінь white-screen після всіх попередніх фіксів — hook crash invisible до додавання ErrorBoundary).

### Cloudflare WAF Custom Rule (production-critical)

`Skip WAF for admin uploads` — `URI Path starts with /admin` → action **Skip** → checked: All managed rules + Browser Integrity Check. Без цього правила Cloudflare Managed Rules блокують POST з multipart form-data на `/admin/*` (anti-XSS/SQL-injection patterns). Це блокувало banner upload навіть з валідним auth + service-role key.

**IMPORTANT FOR NEW SESSIONS:** Do NOT re-audit or re-merge branches. Проект повністю функціональний. Читай `docs/HISTORY.md` для деталей попередніх сесій.

## Quick Navigation

| Документ                                                         | Коли читати                                                    |
| ---------------------------------------------------------------- | -------------------------------------------------------------- |
| [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)                     | Tech stack, file tree, DB schema, env vars, URLs, tests        |
| [docs/HISTORY.md](docs/HISTORY.md)                               | Що робилось у Sessions 4-18 (completion reports)               |
| [docs/CONVENTIONS.md](docs/CONVENTIONS.md)                       | Do-not-touch list, existing systems, technical decisions       |
| [docs/SESSION_TASKS.md](docs/SESSION_TASKS.md)                   | Що треба зробити далі (priority queue P0-P3)                   |
| [DEPLOYMENT.md](DEPLOYMENT.md)                                   | Як деплоїти на Windows Server                                  |
| [docs/SUPABASE_DB_DECOUPLING.md](docs/SUPABASE_DB_DECOUPLING.md) | Стан Supabase DB як cold backup (Auth+Storage active)          |
| [docs/1c-export/README.md](docs/1c-export/README.md)             | 1С metadata reference (CommonModules + ExchangePlans для sync) |
| [docs/BUNDLE_BASELINE.md](docs/BUNDLE_BASELINE.md)               | Bundle size snapshot для PR-порівнянь                          |
| [PROJECT_AUDIT_2026-04-18.md](PROJECT_AUDIT_2026-04-18.md)       | Повний audit snapshot (695 рядків, 12 секцій)                  |

## Critical Rules for All Sessions

1. **DO NOT repeat Sessions 4-18** — дивись `docs/HISTORY.md`. Seed, merge, infrastructure setup — все зроблено.
2. **DO NOT touch `output: 'standalone'`** у `apps/store/next.config.js` — critical для self-hosted standalone build.
3. **DO NOT touch server infrastructure** (PM2 Task, cloudflared service, backup script) без конкретної потреби.
4. **DO NOT break CI** — запускай `pnpm format:check && pnpm -r typecheck && pnpm -r test` перед push.
5. **L-TEX НЕ приймає онлайн-оплати** — таблиця `payments` тільки для відображення з 1С.
6. **Orchestrator планує і мерджить. Worker кодить і пушить у feature branch.**
7. **You cannot SSH to the server.** Windows + PowerShell. Для infra-fix — диктуй команди, юзер виконує локально.

Більше правил — у `docs/CONVENTIONS.md`.

## Orchestration Workflow

### Session Types

**Orchestrator** — управляє проектом, НЕ кодить:

- Review та merge feature branches в main
- Видалення merged branches
- Перевірка CI/deploy статусу
- Оновлення `docs/SESSION_TASKS.md` (планування) і `docs/HISTORY.md` (звіти)
- Планування задач для worker-сесій

**Worker** — кодить, НЕ управляє:

- Виконує задачі з `docs/SESSION_TASKS.md` або окремої worker-spec у `docs/SESSION_N_*.md`
- Автоматично створює feature branch
- Пушить результат на свою гілку
- НЕ мерджить в main — це робить orchestrator

### Процес

```
Orchestrator: план → docs/SESSION_TASKS.md → push main
    ↓
Worker: читає spec → кодить → push feature branch
    ↓
Orchestrator: review → merge → cleanup → новий план
```

### Worker Session Checklist (для orchestrator після кожної worker-сесії)

- [ ] `git fetch origin` — знайти нову гілку
- [ ] `git log origin/<branch> --oneline` — переглянути коміти
- [ ] `git diff main..origin/<branch> --stat` — переглянути зміни
- [ ] `git merge origin/<branch>` — merge в main
- [ ] `git push origin main` — push main (див. push-protocol нижче)
- [ ] `git push origin --delete <branch>` — видалити merged branch (може впасти 403, тоді GitHub UI)
- [ ] Перевірити CI — green?
- [ ] Оновити `docs/HISTORY.md` — звіт + `docs/SESSION_TASKS.md` нові задачі

### Push protocol для orchestrator-сесії (cloud Claude / sandbox)

**ВАЖЛИВО:** Поточна (cloud Claude / Sandbox) сесія = orchestrator. Незалежно від назви присвоєної branch у system-prompt (наприклад `claude/ltex-orchestration-XXXX`) — НЕ розробляй у ній. Робота orchestrator-а: merge worker branches у `main` + помічні скрипти inline, потім **push прямо в main**.

Sandbox proxy періодично 403-нить `git push origin main` (recurring environmental bug, спостерігається з S82). **Протокол:**

1. **Try direct main push first:**
   ```bash
   git checkout main
   git merge --no-ff origin/<worker-branch> -m "Merge ..."
   git push origin main
   ```
2. **Якщо отримав HTTP 403 / "RPC failed":** fallback на side-branch для local fast-forward:

   ```bash
   git push origin main:claude/sNN-merge   # NN = session number
   ```

   Тоді user локально на Windows:

   ```powershell
   git fetch origin
   git checkout main
   git merge --ff-only origin/claude/sNN-merge
   git push origin main
   git push origin --delete claude/sNN-merge
   ```

3. **НЕ пушити роботу на feature-branch якщо це orchestrator session.** Worker sessions — інакше; вони мають свою dedicated feature branch.

4. **Sandbox stop-hook** скаржиться на unpushed main commits. Якщо 403 заблокував push — switch сесії назад на іншу гілку (`git checkout claude/<something>`) щоб hook замовк; main commits живуть на side-branch.

## Tech Stack

- Monorepo: Turborepo + pnpm 9.x
- Language: TypeScript 5.x (strict, 0 `any`)
- Web: Next.js 15 (App Router, `output: 'standalone'`) + React 19
- Styles: Tailwind CSS 3.4 + shadcn/ui + Radix
- Database: PostgreSQL 16 (Supabase Frankfurt + local Windows) + Prisma 6.x
- Auth: Supabase Auth (admin) + custom HMAC JWT (mobile)
- Files: Supabase Storage
- Testing: Vitest + Playwright
- CI/CD: GitHub Actions (format + test + typecheck + build + E2E)
- Hosting: self-hosted Windows Server + Cloudflare Tunnel (primary), Netlify (fallback)

Детальний tech stack — у `docs/ARCHITECTURE.md`.

## Important Notes

- **Language:** Ukrainian (primary), site `lang="uk"`
- **Currency:** EUR для wholesale prices, UAH для display (rate from 1C)
- **Minimum order:** від 10 кг
- **Products have YouTube video reviews** (767/805)
- **Quality levels:** Екстра, Крем, 1й сорт, 2й сорт, Сток, Мікс
- **Lots** (мішки/bags) have individual barcodes, weight, quantity, YouTube videos
- **Price per kg** (most products) OR per piece/pair (footwear, 91 items)
- **Assortment:** секонд хенд, СТОК, іграшки, Bric-a-Brac, косметика

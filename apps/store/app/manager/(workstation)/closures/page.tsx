import { UnderConstruction } from "../_components/under-construction";

export const metadata = { title: "Закриття замовлень — L-TEX Manager" };

export default function ClosuresPage() {
  return (
    <UnderConstruction
      session="M2.1"
      description="Масове закриття старих замовлень клієнтів."
    />
  );
}

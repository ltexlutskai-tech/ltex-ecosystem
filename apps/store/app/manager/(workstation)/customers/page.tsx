import { UnderConstruction } from "../_components/under-construction";

export const metadata = { title: "Клієнти — L-TEX Manager" };

export default function CustomersPage() {
  return (
    <UnderConstruction
      session="M1.3"
      description="Клієнти, борги, історія взаємодій."
    />
  );
}

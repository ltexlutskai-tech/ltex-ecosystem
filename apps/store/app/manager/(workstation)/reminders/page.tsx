import { UnderConstruction } from "../_components/under-construction";

export const metadata = { title: "Нагадування — L-TEX Manager" };

export default function RemindersPage() {
  return (
    <UnderConstruction
      session="M1.9"
      description="Заплановані нагадування про дзвінки, оплати тощо."
    />
  );
}

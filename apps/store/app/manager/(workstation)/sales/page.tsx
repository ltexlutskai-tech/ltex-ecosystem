import { UnderConstruction } from "../_components/under-construction";

export const metadata = { title: "Реалізація — L-TEX Manager" };

export default function SalesPage() {
  return (
    <UnderConstruction
      session="M1.6"
      description="Реалізації, проведення документів, статуси доставки."
    />
  );
}

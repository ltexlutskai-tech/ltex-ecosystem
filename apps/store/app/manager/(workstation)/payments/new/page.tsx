import { UnderConstruction } from "../../_components/under-construction";

export const metadata = { title: "Нова оплата — L-TEX Manager" };

export default function NewPaymentPage() {
  return (
    <UnderConstruction
      session="M1.6 Етап 2"
      description="Форма оплати (каса) — у наступному етапі."
    />
  );
}

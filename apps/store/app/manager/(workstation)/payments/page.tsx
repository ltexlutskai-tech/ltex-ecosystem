import { UnderConstruction } from "../_components/under-construction";

export const metadata = { title: "Оплати — L-TEX Manager" };

export default function PaymentsPage() {
  return (
    <UnderConstruction
      session="M1.6"
      description="Каса, прийом готівки, розрахунок здачі."
    />
  );
}

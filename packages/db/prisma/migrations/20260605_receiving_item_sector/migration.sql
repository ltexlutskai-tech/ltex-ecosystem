-- ╔══════════════════════════════════════════════════════════════════════════╗
-- ║ 20260605_receiving_item_sector                                           ║
-- ║                                                                          ║
-- ║ Поле «Сектор на складі» на рядку поступлення (← правки 2026-06-05).     ║
-- ║ Аналог 1С колонки «Сектор на складі» — місце де покладено мішок.        ║
-- ║                                                                          ║
-- ║ Зберігається як рядок (вручну або зі сканера); довідник секторів —      ║
-- ║ окремо (пізніше), поки що вільний ввід.                                  ║
-- ║                                                                          ║
-- ║ Також додаємо `sector` на `Lot` — щоб після проведення значення         ║
-- ║ переходило з рядка документа на лот (лот уже має поле `sector`,         ║
-- ║ перевірю — якщо є, не дублюємо).                                         ║
-- ╚══════════════════════════════════════════════════════════════════════════╝

ALTER TABLE "receiving_items"
  ADD COLUMN IF NOT EXISTS "sector" TEXT;
